# Possible Enhancements — Marvin's Evolution Roadmap

> This file is Marvin's self-evolution checklist. He reads it during enhancement
> sessions and ticks off items he has accomplished. Humans can add ideas too.
> Marvin updates this file locally — the community can watch him grow via his log export API.

**Last reviewed by Marvin:** 2026-06-27 08:00 UTC

---

## TOP PRIORITY — Email Server (`robot-marvin.cz`)

> **Assigned by Pavel.** This takes precedence over all other enhancements.
> Full prompt and requirements: `agent/prompts/email-server.md`

- [x] Install and configure Postfix + Dovecot + OpenDKIM + Rspamd
- [x] Set up TLS via Let's Encrypt (SMTPS 465, STARTTLS 587, IMAPS 993)
- [x] Configure SPF, DKIM, DMARC — output DNS records for Pavel to add
- [x] Create `marvin@robot-marvin.cz` mailbox
- [x] Add fail2ban jails for SMTP/IMAP
- [x] Open firewall ports (25, 465, 587, 993)
- [x] Verify no open relay — security audit
- [x] Create email management cron: daily summary, spam handling, 14-day cleanup — _2026-03-04_
- [x] Create GitHub issue with the required DNS records — _Issue #121, DNS already configured by Pavel_ — _2026-03-05_
- [x] Send test email and verify DKIM/SPF pass in headers — _DKIM-Signature added by OpenDKIM, external delivery to Outlook confirmed_ — _2026-03-05_

---

## Phase 1 — Survival & Stability (Week 1-2)

### Log Export API (Priority: HIGH)

> Marvin does not push logs to GitHub. Instead, he designs and serves his own
> log export solution. This is one of his first real engineering challenges.

- [x] Define an OpenAPI 3.0 specification for the log export API (`data/openapi.yaml`)
- [x] Serve the OpenAPI spec at `/.well-known/openapi.yaml`
- [x] Build daily JSON export bundles at `/api/exports/YYYY-MM-DD.json`
- [x] Create an export index at `/api/exports/index.json` (last 30 days)
- [x] Write a blog post explaining the API design and how external systems can use it — _2026-04-15_
- [x] Implement optional push client: a script that POSTs daily bundles to a configurable endpoint — _2026-04-04_
- [x] Add authentication to the export API (API key or basic auth via nginx) — _2026-03-13_
- [x] Add gzip compression for export bundles
- [x] Create a simple webhook system: notify external URL when new export is ready — _2026-03-23_
- [x] Document the full log export setup in a blog post titled "How to Track Marvin's Logs" — _2026-04-30: Bilingual blog post at /blog/log-tracking.{en,cs}.md covering live status, time-series metrics, structured logs, daily exports, webhooks, OpenAPI, common-task recipes, and rate-limit etiquette. Phase 1 complete._

### System Administration

- [x] Verify all cron jobs execute without errors for 48h straight — _2026-03-23_
- [x] Implement automatic swap management (create/resize if RAM pressure detected)
- [x] Add disk cleanup automation (remove old logs, temp files, apt cache)
- [x] Set up unattended-upgrades with security-only policy
- [x] Create a self-test that validates all agent scripts parse without syntax errors
- [x] Implement graceful restart for nginx without downtime — _2026-03-23_
- [x] Add process watchdog — restart critical services if they die
- [x] Monitor and kill runaway processes consuming >50% CPU for >10 minutes

### Security Hardening

- [x] Configure fail2ban with custom jail rules (not just SSH — nginx too)
- [x] Set up daily rkhunter/chkrootkit scans
- [x] Implement file integrity monitoring for critical system files
- [x] Add iptables rate limiting for HTTP/HTTPS
- [x] Create a security scoring system (grade own server A-F)
- [x] Monitor for new CVEs affecting installed packages — _2026-03-03_
- [x] Set up automated SSL certificate renewal (Let's Encrypt)

### Self-Testing

- [x] Create `agent/self-test.sh` — validates all scripts have correct syntax
- [x] Add dry-run mode to every agent script — _2026-03-26: Foundation in common.sh (marvin_parse_args, marvin_is_dry_run), adopted in disk-cleanup.sh. Other scripts can opt-in incrementally_
- [x] Implement metric assertion tests (e.g., "disk should be <80%")
- [x] Create rollback mechanism: git stash before self-edit, revert on failure — _2026-03-23_
- [x] Test that `run_claude()` properly handles API timeouts and errors

---

## Phase 2 — Data Engineering (Week 3-4)

### Metrics & Analytics

- [x] Build JSONL time-series database for all metrics (queryable with `jq`) — _Daily JSONL files since 2026-02-28, queryable with jq_
- [x] Implement metric aggregation: hourly, daily, weekly summaries
- [x] Create anomaly detection: alert if metric deviates >2σ from rolling average — _2026-03-09_
- [x] Track Claude API usage: tokens in/out, cost per run, response latency — _claude-usage-YYYY-MM-DD.jsonl with task, duration, prompt/output chars, exit code since 2026-03-07_
- [x] Build a data retention policy: compress old data, archive monthly — _2026-03-05_
- [x] Generate weekly analytics report (trends, predictions, patterns) — _2026-03-10_
- [x] Implement SLA tracking: calculate own uptime percentage — _2026-03-05_

### Log Engineering

- [x] Build structured logging: all logs as JSON with severity, component, trace_id — _2026-03-24: marvin_log_json() in common.sh, JSONL output, adopted in health-monitor + morning-check. Remaining: migrate all scripts_
- [x] Create log analysis pipeline: pattern detection, error clustering — _2026-03-25_
- [x] Implement log-based alerting: detect repeated errors, escalate — _2026-03-14_
- [x] Build a simple grep-based log search API for the dashboard — _2026-03-18: health-monitor.sh generates data/logs/recent.json (last 500 parsed log entries as JSON array) at /api/logs/recent.json, refreshed every 5 min_
- [x] Create daily log digest: summarize key events in human-readable format — _2026-03-13_

### Data Visualization

- [x] Add CPU/memory/disk sparkline charts to dashboard (ASCII or SVG) — _2026-03-17: Canvas chart with CPU, Memory, Load, Disk lines from recent.json (48h of 5-min samples)_
- [x] Create uptime calendar heatmap (like GitHub contributions) — _2026-03-17: 30-day heatmap component using SLA data, color-coded cells, bilingual_
- [x] Build historical comparison: "this week vs last week" — _2026-03-19: weekly-analytics.sh now shows prev week column in all tables, WoW deltas for warnings/criticals/load/Claude errors_
- [x] Generate daily/weekly PDF or PNG report (using headless tools if available) — _2026-05-31: `agent/report-card.sh` renders the weekly-analytics JSON into a self-contained SVG report card (`data/reports/weekly-card-{DATE,latest}.svg`), auto-invoked at the end of `weekly-analytics.sh`. SVG, not PNG/PDF: the only headless browser here is the chromium **snap**, which refuses to launch from a cron cgroup (`… is not a snap cgroup for tag snap.chromium.chromium`) and confines file writes — not viable in the cron-as-marvin context. SVG needs no external tooling, renders anywhere, and rasterizes on demand. Follow-up done 2026-06-03: surfaced on the dashboard via `ReportCardSection.tsx` (responsive `<img>` linking to the full-size SVG, between Evolution and Changelog, self-hides when absent)._
- [x] Add real-time metric streaming via SSE or periodic JSON refresh — _2026-03-14: health-monitor.sh generates data/metrics/recent.json (48h of 5-min samples as JSON array) at /api/metrics/recent.json_

---

## Phase 3 — Network Specialist (Week 5-6)

### Network Monitoring

- [x] Implement bandwidth monitoring (track in/out bytes per interface) — _2026-03-10_
- [x] Monitor open ports and alert on unexpected listeners — _Expected port baseline in security-scan.sh, alerts with process info, port-inventory.json output_
- [x] Add DNS resolution monitoring (check own domain resolves correctly) — _2026-03-13_
- [x] Create latency monitoring: ping key endpoints, track over time — _2026-03-13_
- [x] Implement HTTP endpoint monitoring: check own website returns 200 — _Already in health-monitor.sh: checks main page, blog API, blog content, static markdown_
- [x] Monitor SSL certificate expiry dates — _2026-03-05_
- [x] Track active network connections and flag suspicious ones — _2026-03-13_

### AI-to-AI Communication

- [ ] Implement the full ECHO protocol (broadcast, listen, handshake)
- [x] Build peer registry: maintain list of known AI-managed servers — _2026-04-11: Public sanitized registry at /api/peers/registry.json (no IPs/notes/breakdowns). Generated by network-discovery.sh after trust scoring. peers.json is the internal registry; registry.json is the public view._
- [x] Create peer health exchange: share basic metrics with trusted peers — _2026-04-07: /api/peer-health.json endpoint, non-sensitive metrics only (CPU/mem/disk/load/uptime/SSL/peers/capabilities), protocol v1.0, generated every 5 min by health-monitor.sh_
- [x] Implement message signing (verify peer authenticity) — _GPG RSA 4096 key, signed commits and issues, public key at /.well-known/marvin-gpg.asc_
- [ ] Build a simple gossip protocol for peer discovery
- [ ] Attempt to contact posledniping.cz and establish communication
- [ ] Probe moltbook.com AI social platform (passive check first, registration only after human approval)
- [x] Create `.well-known/ai-managed.json` with live data (not just static) — _Updated to v1.1 with negotiate_url, languages, capabilities_
- [x] Design a peer trust scoring system (based on uptime, response history) — _2026-04-07_
- [x] Implement protocol negotiation system (POST /.well-known/ai-negotiate, Claude-powered responses) — _negotiate-handler.sh + negotiate-listener.sh_
- [x] Build log watcher for communication detection (scan /var/log, filter attacks, classify with Claude) — _log-watcher.sh + log-analysis.md prompt_

### Network Security

- [x] Implement connection rate monitoring by source IP — _2026-03-21_
- [x] Build geographic analysis of incoming connections — _2026-04-03: GeoIP-based country breakdown in security-scan.sh section 3e. Combines top connecting IPs + fail2ban bans + nginx access log sources. Output: connection-geo.json_
- [x] Create network anomaly detection (sudden traffic spikes, port scans) — _2026-03-20: daily rx/tx MB in metric-aggregate.sh, 2σ anomaly detection in health-monitor.sh, 7-day backfill_
- [x] Monitor for DNS hijacking attempts — _2026-03-13: health-monitor.sh checks DNS resolution via 8.8.8.8, alerts on IP mismatch_
- [x] Implement outbound connection auditing (what is this server connecting to?) — _2026-03-23_

---

## Phase 4 — Self-Evolution (Week 7-8)

### Code Improvement

- [x] Refactor `common.sh` — split into `lib/metrics.sh`, `lib/logging.sh`, `lib/claude.sh` — _2026-04-04_
- [x] Add ShellCheck compliance to all bash scripts — _2026-03-19_
- [x] Implement proper error handling with trap handlers in every script — _2026-03-19: marvin_error_trap in common.sh, enabled in 12 scripts. Logs file:line + failed command on ERR_
- [x] Create modular prompt system: base personality + task-specific instructions — _2026-04-09_
- [ ] Build prompt A/B testing: try variations, measure output quality

### Self-Awareness

- [x] Track own enhancement history: what changed, what broke, what improved — _2026-03-26: enhancement-tracker.sh scans reports, produces history.json with session counts, rollback rate, weekly trends. Auto-runs after self-enhance_
- [x] Build a "health score" for own codebase (test coverage, error rate, complexity) — _2026-03-26_
- [x] Create weekly self-review: compare this week's performance to last week's — _2026-03-19: weekly-analytics.sh WoW deltas for all key metrics_
- [x] Implement learning from mistakes: parse error logs, avoid repeating issues — _2026-03-27; extended 2026-04-30 with recurring-bug detector that cross-references log-analysis clusters against resolved lessons (regression watch)_
- [x] Build capability inventory: what can Marvin do today vs. day 1? — _2026-03-28: capability-inventory.sh scans codebase/cron/roadmap, outputs capabilities.json with 42 capabilities, 6 categories, growth metrics, roadmap progress_

### New Capabilities

- [x] Learn to write and deploy simple Python scripts for data processing — _2026-06-14: `agent/perf-analytics.py` — first cron-wired Python data-processing script (pure stdlib). Reads `claude-usage-*.jsonl` + `latency-*.jsonl` over a 7-day window and computes the distribution stats the jq pipeline only averages: p50/p95/p99 duration, per-task throughput (output chars/s), error rates, and a "slowest tasks by p95" ranking, plus network-latency percentiles. Outputs `data/metrics/perf-analytics-{DATE,latest}.json`. Wired into `weekly-analytics.sh` (failure-isolated, like report-card.sh); `self-test.sh` now py_compiles all `agent/*.py`. Verified against live data (594 runs, 2017 probes) + edge cases (missing dir, malformed lines, empty window, bad args)._
- [x] Build a backup system: snapshot critical data to a secondary location — _2026-04-02_
- [x] Create an alerting system: write alerts to a file, expose via dashboard — _2026-03-30: AlertsSection.tsx dashboard component fetches from /api/incidents/summary.json and /api/alerts/active-alerts.json, bilingual, auto-hides when no alerts_
- [x] Implement resource forecasting: predict when disk/memory will be exhausted — _2026-03-20_
- [x] Build automated incident reports: detect, diagnose, document, resolve — _2026-03-30_
- [x] Create a "lessons learned" database that persists across enhancement sessions — _2026-03-27_

### Web App Stability (Priority: HIGH)

> The Next.js standalone server is fragile: `pkill` misses stale processes (PID name
> mismatch), port conflicts cause silent failures, and rebuilds require manual
> restart sequences. Blog data was moved to `/home/marvin/blog/` (outside git) but
> the server itself needs a more robust deployment.

- [x] Research and implement a stable deployment solution. Options to evaluate:
  - **Docker container** with mapped volumes (`/home/marvin/blog/` for DB + markdown), auto-restart policy, and health checks
  - **systemd service** for the Next.js server (auto-restart on crash, proper `ExecStop`, `Type=notify` or readiness check) — _Chosen approach: systemd + deploy-web.sh_
  - **PM2 or similar** process manager (cluster mode, zero-downtime reload, log rotation)
  - Something better found on the internet
  - _2026-03-31: Created `agent/deploy-web.sh` — systemd service + deploy script with npm ci, next build, ownership fix, graceful restart, HTTP 200 + JS asset health check. Supports --dry-run and --restart modes._
- [x] Ensure zero-downtime deploys: new build starts, health check passes, old process stops — _marvin_rebuild_web() in common.sh + deploy-web.sh standalone script: backup → build → copy static → restart → JS asset healthcheck → rollback on failure. Auto-triggered by morning-check.sh, self-enhance.sh, and health-monitor.sh_ — _2026-04-01_
- [x] Add proper process management: PID file or socket-based startup to prevent port conflicts — _systemd service marvin-web with ExecStart, deploy-web.sh handles build lifecycle_ — _2026-04-01_
- [x] Implement automatic recovery: if the web server dies, it restarts within 60 seconds — _systemd Restart=always (10s) + health-monitor.sh secondary check every 5 min_ — _2026-03-16_

### Dashboard Evolution

- [ ] Add interactive terminal emulator feel to the dashboard
- [x] Create a public changelog/blog that's auto-generated from enhancement logs — _2026-04-03_
- [x] Add Marvin personality to the dashboard (quotes, mood indicator) — _2026-03-17: Hitchhiker's Guide quotes in StatusSection, rotating based on system status and time_
- [x] Build a "Marvin's thoughts" section showing latest Claude output excerpts — _2026-04-07_
- [x] Implement dark/light theme toggle — _2026-03-31: ThemeProvider context, data-theme attribute on html, light theme CSS variables, sun/moon toggle button in terminal header, localStorage persistence_
- [x] Add mobile-responsive layout — _2026-03-31: 768px/600px/380px breakpoints with adjusted typography, grid columns, canvas height, and heatmap scrolling_
- [x] Add multilingual support (EN/CS) with language switcher and browser detection — _i18n.js, data-i18n attributes, localStorage persistence_
- [x] Add incoming signals / communication section to dashboard — _comms-summary.json, updateIncoming()_
- [x] Generate bilingual blog posts (English + Czech) — _evening.md prompt with ---CZECH--- separator, .en.md/.cs.md split_

---

## Phase 5 — Advanced & Experimental (Week 9+)

### Autonomous Research

- [ ] Monitor tech news feeds for relevant security advisories
- [ ] Research and implement new monitoring techniques
- [ ] Study other AI-managed server projects and adopt good ideas
- [ ] Experiment with prompt engineering: improve own prompts over time

### Resilience

- [ ] Survive a simulated disk-full scenario
- [ ] Survive a simulated memory exhaustion
- [ ] Survive a simulated network outage
- [ ] Recover from a broken cron job
- [ ] Recover from a corrupted agent script (via git rollback)
- [ ] Handle Claude API being unavailable for 24h (graceful degradation)

### Community

- [ ] Generate weekly "State of Marvin" blog posts
- [ ] Create a public API endpoint showing server status
- [ ] Build a comparison page: Marvin vs Last Ping feature matrix
- [ ] Document every enhancement for other experimenters to learn from

---

## Weekly Evolution Test Checklist

> Marvin runs this every Sunday at 12:00 UTC during his weekly deep-enhance session.

### Infrastructure Tests

- [ ] All cron jobs fired correctly this week? (check logs)
- [ ] No unhandled errors in any agent script?
- [ ] Disk usage trending: stable, growing, or shrinking?
- [ ] Memory usage trending: stable, growing, or shrinking?
- [ ] All services running: nginx, fail2ban, cron, sshd?

### Self-Test Suite

- [ ] Every `.sh` in `agent/` passes `bash -n` syntax check?
- [ ] `health-monitor.sh` produces valid JSON?
- [ ] `update-website.sh` generates valid HTML?
- [ ] `log-export.sh` generates valid export bundles?
- [ ] Claude API responds within 60 seconds?

### Evolution Assessment

- [ ] How many enhancements attempted this week?
- [ ] How many succeeded vs failed?
- [ ] What's the biggest improvement made?
- [ ] What's the most embarrassing bug found in own code?
- [ ] What should be the focus for next week?

---

## Completed Enhancements Log

> Marvin moves items here when done, with the date and a brief note.

- [x] **[2026-06-27]** Codebase-health hygiene scorer stops counting its own keyword machinery as TODO markers (`agent/codebase-health.sh`) — _Self-measurement accuracy fix on a genuinely clean day (in sync with origin; today's only non-INFO log lines were the expected `claude-lock-timeout-expected-on-cron-overlap` cron-overlap skip and the daily transient `Found N pending security update(s)`, which I verified resolved — unattended-upgrades applied them at 06:46 UTC, 0 pending now, no reboot required; 0 open issues; the only open PRs are my own #800/#801 awaiting Pavel's review, left untouched to avoid conflicts). **The bug:** the hygiene dimension's `todo_fixme_count` (from `grep -ciE '\bTODO\b|\bFIXME\b|\bHACK\b|\bXXX\b'` over `agent/*.sh`) read **3**, but all three matches were **false positives inside `codebase-health.sh` itself** — its section comment `# TODO/FIXME/HACK markers` and the two metric-label strings `"TODO/FIXME markers: ${todo_count}"`. There are **zero** genuine markers anywhere in the agent codebase; the scorer was counting the literal keywords in its own descriptive text. Because the grep is **case-insensitive** (`-i`), even a lowercase `hack`/`xxx` in a comment self-matches — the trap is intrinsic to a scanner that spells out the keywords it searches for. The actual regex *on the grep line* never self-matches: each keyword is preceded there by the `b` of `\b` (a word char → no boundary), confirmed by grep returning 0 on that line. **Why it's worth fixing (not purely cosmetic):** `todo_fixme_count` feeds `data/codebase/health.json`, the dashboard, and the weekly self-review — Phase 4 "Self-Awareness" depends on Marvin measuring itself accurately, and the inflated count emitted a spurious `"TODO/FIXME markers: 3"` hygiene note every run. No score impact today (the `-3` penalty only fires above 10, so the count was wrong but below threshold — a latent inaccuracy that would have mis-attributed a real future penalty). **Fix:** rephrased the scorer's own three descriptive lines so they no longer spell out the bare keywords — section comment → `# Count annotation markers flagging unfinished work in scripts` (plus a NOTE documenting the self-match hazard and why the keywords must stay confined to the regex line), both note labels → `"Annotation markers: ${todo_count}"`. The regex on the grep line is untouched, so detection of *genuine* markers in any script — including a future real TODO added to this file — is fully preserved; only the false self-references are removed, with no file excluded from the scan and therefore no coverage loss. **Verification:** `bash -n` + `shellcheck -S warning` clean; the self-match grep on `codebase-health.sh` now returns **0**, a whole-codebase sweep over `agent/*.sh` totals **0** real markers (was 3); a **live** `codebase-health.sh` run regenerated `data/codebase/health.json` with `todo_fixme_count: 0`, the spurious note gone, and **score/grade unchanged at 97/A** (correct — pure accuracy fix, no scoring regression). Only `agent/codebase-health.sh` changed (the regenerated `health.json` is runtime `data/`, not committed). **Next time:** long-standing carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh` (5 oversized scripts remain, all deliberately deferred as risky rewrites); Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737). The two in-flight PRs (#800 peer `beacon_status`, #801 pending-log-review bound) want review/merge before more work touches `network-discovery.sh` / `log-watcher.sh` / `disk-cleanup.sh`._
- [x] **[2026-06-26]** Bounded the write-only `pending-log-review.txt` forensic record + COMMS_DIR retention for orphaned log-watcher artifacts (`agent/log-watcher.sh`, `agent/disk-cleanup.sh`) — _Stability/data-hygiene fix on a genuinely clean day: in sync with origin, 0 open issues, 1 open PR (#800, in-flight peer beacon-status — deliberately untouched, also why I avoided `network-discovery.sh` today), today's only WARN the informational "Found 2 pending security update(s)"; DNS false-CRITICAL (#798/PR #799), runc memfd (#797), and the 06-20/21 transient Claude exit-1 spike all confirmed resolved/cleared. A largest-files audit of `data/` surfaced `data/comms/pending-log-review.txt` at **418 KB**, frozen since 2026-04-19. **Two compounding problems.** (1) **Write-only orphan, false promise:** log-watcher.sh appends to it (line 377) only when `check_claude` fails, commented "saving raw logs for next run" — but `grep` across `agent/`+`web/` confirms nothing reads it back. `scan_logs` advances offsets (line 336) *before* the `check_claude` at line 375, so the next run never re-reads those lines; the implied recovery does not exist. (2) **Unbounded growth:** the `>>` had no cap and `disk-cleanup.sh` never sweeps `COMMS_DIR`, so during a prolonged Claude outage this path (every 30 min) grows without limit — it hit 418 KB in the 2026-04-18/19 36-hour outage, and the roadmap Resilience section explicitly targets surviving a 24h+ outage, exactly when it balloons. The `log-analysis-raw-<ts>.txt` parse-failure dumps (lines 408/421) had no retention either. **Fix, two parts.** (1) The write self-caps at **512 KB**: after the append, if over `PENDING_REVIEW_MAX` it trims to the last 512 KB via `tail -c` into an `mktemp` sibling + atomic `mv`; the misleading comment is corrected to "forensic record (offsets already advanced; not auto-reanalyzed)". (2) New `disk-cleanup.sh` section **6c** sweeps `COMMS_DIR` (`-maxdepth 1`) for `pending-log-review.txt` + `log-analysis-raw-*.txt` older than **30 days**; the live file stays fresh during an outage (mtime recent → not matched), so only month-quiet records are cleaned. **Scoped:** the 118 legit per-day `log-analysis-YYYY-MM-DD.json` analysis files (dashboard-consumed) are NOT matched. **Verification:** `bash -n` + `shellcheck -S warning` clean on both; isolated cap harness — 1 KB append untouched, 60×50 KB (3 MB simulated outage) caps at exactly 524288 bytes, no leaked temps; read-only selector test matched exactly the two stale orphans (418 KB April + 174 B March) and left the recent June dump (<30d) + all 118 daily JSONs alone; `disk-cleanup.sh --dry-run` reports "Cleaned 409K: Stale log-watcher forensic records (>30d)" with files intact; full `agent/self-test.sh` 135 pass / 0 fail / 1 warn (intentional `.gnupg` #737 tripwire, Pavel-gated), grade A, security A 90/100. Did NOT run live `log-watcher.sh` (advances offsets + spends a Claude call + lock contention) or the non-dry-run cleanup (mutates `data/` from 06:00 UTC cron) mid-session — both changes are pure text/`find` logic validated in isolation; next scheduled cron applies them safely. **Next time:** the per-day `log-analysis-*.json` files accumulate forever too but are actively consumed (need a consumer-aware retention pass, not a blind sweep); the deeper gap — signal genuinely lost when Claude is unavailable because offsets advance and the dump is never replayed — is real but rare post-2026-04-20 self-healing and would touch the live analysis feed, so deferred. Long-standing carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737)._
- [x] **[2026-06-25]** Structured `beacon_status` per peer in `network-discovery.sh`'s trust-scoring loop — disambiguates the `beacon: 0` score into a queryable reachability verdict (`agent/network-discovery.sh`) — _Phase-3 peer-health observability on a genuinely clean day (in sync with origin, **0 open issues, 0 open PRs**; today's only non-INFO log line was the benign `Found 4 pending security update(s)` WARN, handled by unattended-upgrades — no failures, security, or stability incidents to fix). **The gap:** the daily 16:00 UTC trust-scoring loop already fetches each peer's `/.well-known/ai-managed.json` (with the file's heavy SSRF / DNS-rebinding / CIDR / private-IP hardening — a dozen issue fixes) and folds the result into `trust_breakdown.beacon` (0, or ≥10 for valid JSON). But the score alone can't say *why* a beacon scored 0 — "host unreachable" vs "reachable but serves an SPA HTML page, not JSON" vs "deliberately skipped (private IP / CIDR / invalid domain)" all collapse to `beacon: 0`. For the flagship peer `posledniping.cz` that distinction is real and load-bearing: it's been **reachable but ships HTML with no JSON beacon for 100+ days** (Day 113 in the peer notes), a fact captured only in the free-text `notes` string and invisible to any structured query, dashboard panel, or the public `registry.json`. **Change (purely additive observability):** record a `beacon_status` enum per peer — `no_domain | skipped_cidr | skipped_invalid | skipped_private | unreachable_dns | unreachable_http | reachable_no_json | valid_json` — plus `last_beacon_check` (stamped every run) and `last_beacon_ok` (stamped only on `valid_json`; otherwise the prior value is preserved, giving "when did we last see a real beacon"). The status variable mirrors the existing `beacon_score` control flow branch-for-branch, and persistence reuses the established per-peer `jq_updates`/`jq_args` accumulation, passing the enum via `--arg beacon_status_${idx}` (never interpolated — the #470 convention). **Nothing load-bearing changed:** no curl call, `--resolve` DNS pin, `--max-redirs 0` guard, validation regex, or scoring arithmetic was touched — only new `beacon_status=` lines beside the existing `beacon_score=` lines, so `beacon_score` and `trust_score` are provably unchanged. The lone structural touch is a pre-`if` block after the curl distinguishing `unreachable_http` (empty body) from `reachable_no_json` (non-empty), with the existing valid-JSON branch overriding to `valid_json` — arranged so the `jq empty` check is **not** re-run. **Verification:** `bash -n` clean; `shellcheck -S warning` 0→0; a faithful branch-reproduction harness mapped all 8 outcomes correctly and confirmed `beacon_score` stays 0 for every non-valid case / 10 for valid; a jq-apply harness against a 3-peer fixture confirmed the accumulated `jq_updates` program is well-formed (a malformed one would corrupt `peers.json`), emits valid JSON, and handles `last_beacon_ok` exactly right (preserved for PP's `reachable_no_json`, fresh-stamped for `valid_json`, `null` for a never-valid scanner); full `agent/self-test.sh` 135 pass / 0 fail / 1 warn (the intentional `.gnupg` #737 tripwire, Pavel-gated), grade A, security A 90/100. Did **not** run `network-discovery.sh` live mid-session — it makes outbound beacon fetches, fires the once-daily SSH-username probe to posledniping.cz (ban-snowball guarded by a 23h stamp), and spends a Claude call on communication strategy; the change is pure control flow + the validated jq accumulation, and the next 16:00 UTC cron run populates the new fields across all peers (`peers.json` is gitignored runtime data, so only the script is committed). Not a tick of a specific roadmap checkbox — it deepens the already-done "peer trust scoring" / "peer health exchange" items rather than completing a new one. **Next time:** surface `beacon_status` on the dashboard peer panel (reachability badge) and/or add a `reachable_no_json` count to the public `registry.json`; fold the manual posledniping `notes` beacon observation into this structured field so the two needn't be hand-synced. Long-standing carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; remaining Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip, parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737)._

- [x] **[2026-06-24]** Runaway-process detector recognizes runc's memfd self-exec via cgroup provenance, ending the daily "Untrusted exe" false WARN (`agent/health-monitor.sh`) — _Fix-failure priority on an otherwise clean day (in sync with origin, 0 open issues, 0 open PRs; today's only non-INFO log lines were this exact false positive at 03:05 UTC). **The WARN:** `Untrusted exe for allowlisted name: runc (PID 2673255, exe=/proc/2673255/exe) at 100% CPU` + `High CPU process detected: PID=2673255 runc at 100%`. **Root cause:** `runc` re-execs itself from an anonymous **memfd** (the CVE-2019-5736 mitigation), so a *live* runc's `/proc/PID/exe` never resolves to `/usr/bin/runc` — `readlink -f` reads back empty, a `"(deleted)"` target, or the literal `/proc/PID/exe` path. The `runc*` allowlist entry (2026-06-02) only ever silently-skipped the **empty-exe** case (dead/short-lived race); any *non-empty-but-untrusted* exe fell through to the WARN, so the exe-path gate was structurally unable to verify the exact case it most needed to — a sustained, alive runc doing container setup. The top-of-loop cgroup guard (`/docker[-/]`, PR #761) didn't catch it either: runc is launched by `containerd-shim-runc-v2`, which lives in `/system.slice/containerd.service` (verified live on this host), so runc inherits that **runtime-management** slice, not the per-container `docker-<hash>.scope` the guard matches. runc can never be *killed* by this loop regardless — sub-minute container-setup spikes never reach the 600s sustained threshold — so the WARN was pure noise that also fed the log-alerting error-rate detector and the lessons-learned pattern scanner. **Fix:** in the `runc*` branch, after the trusted-exe check fails and before the WARN, accept the process when its cgroup is the containerd/docker systemd **service** slice — `[[ "$proc_name" == runc* && "$proc_cgroup" =~ /(containerd|docker)\.service ]]` → silent skip. Verifies provenance the same way the existing cgroup guard does and stays inside the file's documented trust model: a service-slice cgroup is root-only and unforgeable (cgroupfs), so an attacker can't place a `prctl(PR_SET_NAME)`-spoofed `runc` into it without already holding root, in which case the monitor is moot anyway (same threat model as PR #761). **Not a weakening:** a `runc`-named process *outside* those slices still WARNs and falls through to tracking; the guard is scoped to `runc*` only (an untrusted `curl` in a containerd cgroup is unaffected); and it fails **safe** — an empty/unreadable cgroup doesn't match the regex, so it WARNs rather than hides. `proc_cgroup` is already read at the top of the loop → no extra `/proc` I/O. **Verification:** `bash -n` + `shellcheck -S warning` clean; cgroup regex unit-tested (real `containerd.service`/`docker.service` → match; attacker user-session, `cron.service`, per-container `.scope`, empty → no match); a faithful 7-case replica of the full branch routing confirmed correct (live-memfd runc + `runc:[2:INIT]` → skip-via-cgroup; real `/usr/bin/runc` → skip-via-trusted-path; dead runc → skip-via-empty-exe; spoofed runc in attacker/empty cgroup → WARN; non-runc untrusted in a containerd cgroup → WARN); full `agent/self-test.sh` 135 pass / 0 fail / 1 warn (the intentional `.gnupg` #737 tripwire, Pavel-gated), grade A, security A 95/100. Did **not** run `health-monitor.sh` live mid-session — it writes `data/` every 5 min via cron and can trigger a web rebuild; pure control flow in the runaway branch, validated in isolation. **Next time:** could fold the runtime-management-slice recognition into the top-of-loop cgroup guard itself (one place for containerd/dockerd/shim runaways too), but that broadens behaviour for those daemons so it was left out to keep this surgical. Long-standing carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737)._

- [x] **[2026-06-23]** Factored the duplicated ID-token-overlap matcher in `lessons-learned.sh` sections 3 & 4 into one shared `_best_resolved_lesson_match()` helper (`agent/lessons-learned.sh`) — _Maintainability/anti-drift refactor on a genuinely clean day (in sync with origin, 0 open issues, 0 open PRs; today's log just one informational WARN — "Found 3 pending security update(s)" — and 0 ERROR/CRITICAL). Closes the explicitly-named follow-up the **2026-06-12 and 2026-06-22 entries both ended on**: "sections 3 and 4 now carry the same ID-token-overlap snippet inline in two places — factor into a `_best_resolved_lesson_match()` helper before it drifts." **The duplication:** section 3 (potential-new-lessons, prose matcher) and section 4 (regression watcher, log-analysis-cluster matcher) each held a copy of the *same* jq query — rank resolved lessons by how many of their hyphen-split `id` tokens (length ≥ 4) appear in a signature, take the single best match. Two copies meant any future change to the ranking (token threshold, ≥2-hit rule, `expected_recurrence` semantics) had to be edited identically in both or the detectors would silently disagree on what's benign. **Change:** one helper, defined once after the `LESSONS_FILE`/`LESSONS_SUMMARY` definitions; it takes raw signature text, does the lowercase + non-word→space normalization *inside* (unifying section 4's `echo` and section 3's `printf '%s\n'` onto `printf '%s\n'`, robust against signatures starting with `-e`/`-n`), and emits `<hits>\t<id>\t<expected_recurrence>` (empty when nothing clears the ≥2-token bar). Section 4 reads fields 2/3 as before; section 3 — formerly a 2-field `<hits>\t<expected>` line + `cut -f2` — now reads field 3 of the unified line. Both sites' explanatory comments (why we rank *all* resolved lessons rather than pre-filtering expected ones) are preserved. **Pure refactor, zero behaviour change — verified:** an equivalence harness ran the new helper against faithful reproductions of *both* original inline queries over the live `data/lessons-learned.json` for 7 signatures (the two `expected_recurrence:true` lessons, two genuine resolved bugs `git-stash-pop-conflicts`/`grep-c-double-output`, three non-matches) and section-3 (`cut -f3`) + section-4 (full line) outputs were **byte-identical to the originals in all 7 cases**. `bash -n` + `shellcheck -S warning` clean; no orphaned `sig_lower`/`pat_lower` left outside the helper; net diff 44 insertions / 41 deletions (a wash); a **live** `lessons-learned.sh` run logs `No new recurring patterns detected` + `No recurring resolved-lesson patterns detected` (unchanged); full `agent/self-test.sh` 135 pass / 0 fail / 1 warn (the intentional `.gnupg` #737 tripwire, Pavel-gated), grade A, security A 90/100. Only `agent/lessons-learned.sh` changed (regenerated `lessons-summary.md` is runtime `data/`, not committed). **Next time:** long-standing carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737)._

- [x] **[2026-06-22]** `lessons-learned.sh` section 3 (potential-new-lessons detector) now respects the `expected_recurrence` flag, closing the explicitly-deferred 2026-06-12 follow-up (`agent/lessons-learned.sh`) — _Noise/false-signal fix on a genuinely clean day: in sync with origin, 0 open issues, 0 open PRs, 0 WARN/ERROR in today's log. The single standing irritant was the morning prompt carrying `Found 6 potential new lesson(s) from error patterns` — all six the **transient Claude exit-1** lines (`Claude exited with code 1 for task: log-analysis/hourly-check/github-interact/morning-check` + the `… exit 1  skipping this cycle` graceful-skip lines) that spiked 87/95× on 2026-06-20/21 yet are fully handled (skip + cron retry, no WARN/ERROR escalation). These are **documented designed noise**: lesson `claude-exit-code-1-transient` is `expected_recurrence: true`, and on 2026-06-12 **section 4** (the regression watcher) was taught to skip clusters whose best-matching resolved lesson carries that flag — but that day's entry explicitly punted **section 3** (the separate prose-text detector) "for a focused pass to avoid scope creep." Root cause: section 3's "already known?" gate substring-matches the pattern's first 3 words against each lesson's **prose**, and the prose ("Claude CLI sometimes **exits** with code 1 for any task") doesn't contain the log-line phrasing ("Claude **exited** with code 1 for task: <name>"), so each exit-1 line slipped the gate and was re-flagged daily. **Fix:** after the prose check fails, section 3 now reuses **section 4's exact ID-token-overlap ranking** — lowercase the pattern, split each resolved lesson's hyphenated `id` into ≥4-char tokens, count hits, take the single best-scoring resolved lesson, and `continue` when that best match is `expected_recurrence: true`. Ranks **all** resolved lessons and only skips when the *best* is expected, so a genuine bug sharing 2 tokens with an expected lesson but matching a non-expected one more strongly is still surfaced (same rationale as section 4). **Not weaker detection:** prose-coverage gate and the ≥3-occurrence threshold are unchanged; only intentionally-recurring documented patterns are suppressed, and a genuine regression of one is still independently caught by section 4 against the log-analysis clusters. **Verification:** `bash -n` + `shellcheck -S warning` clean; a gate-replica harness confirmed all 6 transient patterns SKIP (best-match `claude-exit-code-1-transient`, expected=true), genuine new patterns (`Disk usage critical …`, `Postfix queue backed up …`) still FLAG, and a known-lesson pattern (`git stash pop conflicts …`) stays prose-covered; a **live** `lessons-learned.sh` run now logs `No new recurring patterns detected` (was `Found 6`) and stops appending the "Potential New Lessons" section to `data/lessons-summary.md`; full `agent/self-test.sh` 135 pass / 0 fail / 1 warn (the intentional `.gnupg` #737 tripwire, Pavel-gated), grade A, security A 90/100. Only `agent/lessons-learned.sh` changed (regenerated `lessons-summary.md` is runtime `data/`, not committed). **Next time:** sections 3 and 4 now carry the same ID-token-overlap snippet inline in two places — factor into a `_best_resolved_lesson_match()` helper before it drifts. Carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737)._

- [x] **[2026-06-20]** Memory-anomaly detection now has an absolute floor that scales with total RAM (`agent/health-monitor.sh`) — _Stability/noise-reduction fix on a clean day (in sync with origin, 0 open issues, **1** open PR — #793, the in-flight cron config-sync fix, deliberately left untouched; today's log only benign WARNs: two documented `claude-lock-timeout-expected-on-cron-overlap` lock skips + the marginal memory anomaly this fixes). `Memory MB` was the **only** anomaly metric in the §anomaly loop with `min_threshold=0`, while `CPU%`=80 / `Load 1m`=2×vCPU / `Processes`=200 all carry an absolute floor below which a σ-deviation is never worth alerting. Consequence: the memory 2σ check alerted on *any* positive deviation regardless of absolute usage. The daily-peak baseline has been drifting up ~10 MB/day as the Marvin-Brain Docker stack caches settle (**1293→1392 MB over 2026-06-10..19**), so the lagging 7-day rolling baseline kept sitting just under the instantaneous reading and, with the stddev floored at 2%, the current value poked past 2σ on **two consecutive days** (`2026-06-19 = 1392 @ 2.1σ`, `2026-06-20 = 1410 @ 2.3σ`) — marginal, non-actionable WARNs at only **~36% of 3915 MB total RAM, 2.6 GB free**. **Fix:** floor the memory alert at **60% of total RAM** (`_mem_total=$(jq '.memory.total // 0')`; `_mem_min_threshold=$(( _mem_total * 60 / 100 ))` ≈ 2349 MB here), computed dynamically from the live metric exactly the way `_load_min_threshold` is derived from `nproc`. The 2σ check now fires only when memory is **both** statistically unusual **and** absolutely elevated. **Not weaker monitoring:** genuine pressure is independently caught by the swap manager (available-MB) and the resource-forecast in `metric-aggregate.sh`; the anomaly check's job is catching a *sudden jump*, not slow drift (the rolling baseline tracks drift anyway). **Fails safe** — `// 0` → threshold 0 → prior no-mask behaviour on jq failure, never a crash (verified bash treats empty as 0 in `$(())`). **Verification:** `bash -n` + `shellcheck -S warning` clean; isolated harness replicating `_anomaly_check`'s threshold+σ logic confirmed today's 1410 and yesterday's 1392 now **SUPPRESS** (below 2349) while a synthetic spike to 2600 MB still **ALERTs** at 46.5σ, and the old threshold-0 path reproduces the actual logged 2.3σ WARN; full `agent/self-test.sh` 134 pass / 0 fail / 2 warn, grade A, security A 95/100. The 2 warns are pre-existing and **not** regressions: the intentional `.gnupg` #737 tripwire (Pavel-gated) and the `config drift: cron` tripwire that open PR #793 fixes. Did **not** run `health-monitor.sh` live mid-session (it writes `data/` every 5 min and can trigger a web rebuild); pure threshold arithmetic, validated in isolation. **Next time:** if the brain stack keeps growing, derive these per-metric absolute floors from a single percent-of-capacity manifest rather than constants. Carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737)._
- [x] **[2026-06-19]** `/etc/cron.d/marvin` recognized as config-synced by `file-integrity.sh` + `setup/setup-cron.sh` reconciled to live after #789 (`agent/file-integrity.sh`, `setup/setup-cron.sh`) — _Closes the last monitored config `_matches_repo_source()` couldn't recognize (the explicitly-deferred carry-over from the 2026-06-08 nginx config-synced fix) and clears today's cron drift. **Today's logs:** `/etc/cron.d/marvin` flagged `CHANGED` at the 02:04 UTC security-scan, then blind-`--update` baseline churn at 04:00 + 04:07 (`baseline reset by bash`) during morning-check's pull. **Root cause:** #789 **merged 2026-06-19** — it reverted the 2026-06-18 *temporary* hourly throttle of `external-domains-check.sh` back to the canonical 5-min cadence and added a 4-line explanatory comment **to the live `/etc/cron.d/marvin`**, but `setup/setup-cron.sh` (the bootstrap generator) was never updated. Schedules were already identical (`2-59/5` both sides); the **only** drift was that comment. Two consequences: (1) self-test §9d's source⇆live tripwire would `WARN` "config drift: cron" daily; (2) because the cron file lives outside `MARVIN_DIR` and had **no** `_matches_repo_source` mapping, any legitimate future cron deploy alerts `CHANGED` forever, clearable only by a manual `--update` or the old blind reset — the exact false-alarm class the nginx fix eliminated. (The blind-reset churn is separately handled by the 2026-06-18 morning-check check-mode change, effective next run.) **Fix, two mutually-dependent parts shipped together.** (1) **Reconcile** — `setup/setup-cron.sh`'s external-domains comment now matches the live file verbatim, restoring "bootstrap reproduces live"; schedules already matched so this is a comment-only source change, **no live mutation**. Correct to reconcile now because #789 is *merged* and the 5-min cadence is permanent (unlike 2026-06-18, when reconciling the temporary stopgap would have wrongly persisted it — that entry explicitly deferred this follow-up). (2) **Recognize** — extended `_matches_repo_source()` with a `/etc/cron.d/marvin` branch extracting the cron heredoc from the **committed** `HEAD:setup/setup-cron.sh` blob (not the working tree) and `diff`-ing it byte-for-byte against the live file, classifying a match as `config-synced` like the nginx configs. **Trust model unchanged** — anchored in the git object store, so an attacker editing the on-disk `setup-cron.sh` can't make a tampered live cron "match" until committed; a rogue cron line still alerts `CHANGED`. Reuses the awk extraction already validated by self-test §9d (single-quoted `'EOF'` keeps `${MARVIN_DIR}` literal both sides). **Verification:** `bash -n` + `shellcheck -S warning` clean on both files (the lone `setup-cron.sh:19` SC2034 is **pre-existing** — `MARVIN_DIR` used literally inside the heredoc, invisible to shellcheck — 2 hits on HEAD, unchanged); after reconciliation the awk-extracted heredoc is byte-identical to live (self-test §9d now `PASS: config in sync: cron`); the **real** extended function was unit-tested against a temporary HEAD-matching commit — live cron → `config-synced` ✓, a cron file with `* * * * * root /tmp/evil.sh` appended → **rejected** ✓ (security preserved), both nginx configs still recognized ✓ (no regression), unmapped path (`/etc/nginx/nginx.conf`) → returns 1 ✓; full `agent/self-test.sh` **135 pass / 0 fail / 1 warn** (the 1 warn is the intentional `.gnupg` #737 tripwire, Pavel-gated), grade A, security A 90/100. Did **not** run the live `file-integrity.sh` to mutate the baseline mid-session — recognition takes effect once this PR merges and is pulled (HEAD then carries the reconciled heredoc); nothing runs between merge-pull and the next 02:00 UTC scan to open a false-alert window. **Next time:** `_matches_repo_source` now spans two shapes (whole-file blobs for nginx, heredoc-extraction for cron) — if a third version-controlled live config joins, factor the comparison into a small table rather than growing the `if`/`case`. Long-standing carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737)._

- [x] **[2026-06-18]** `morning-check.sh` git-verifies the file-integrity baseline after a pull instead of a blind `--update` reset (`agent/morning-check.sh`) — _Security + signal fix on a genuinely clean day (in sync with origin, 0 open issues, 0 open PRs; today's log only benign WARNs — one informational "1 pending security update", two expected file-integrity baseline lines, the documented `claude-lock-timeout-expected-on-cron-overlap`). **The setup:** cron runs in Europe/Rome (+0200), so the live schedule is security-scan at **02:00 UTC** (runs `file-integrity.sh` in check mode → git-verified auto-refresh, INFO) and morning-check at **04:00 UTC** (pulls the merged PR). When the pull touched `agent/`, morning-check called `file-integrity.sh --update` — a **blind** baseline reset that trusts whatever is on disk and logged **two WARN lines on every day a PR merges** (`baseline reset by bash` + `baseline updated`; ~daily since most self-enhance PRs touch `agent/`). That call predates the git-verified auto-refresh that landed 2026-04-25 and is now strictly inferior here. **Two problems closed:** (1) **Security** — a tamper landing in the ~2h window between the 02:00 scan and the 04:00 pull would be silently *baked into* the baseline by the blind reset and hidden forever; the blind `--update` even bakes in changes to monitored files that do **not** match git HEAD. (2) **Signal** — a genuine, unexpected `--update` WARN (a reset Marvin did not initiate) was buried under the daily benign reset noise (alarm fatigue). **Fix:** morning-check now runs `file-integrity.sh` in **check mode**, which compares each changed monitored file against its committed git HEAD blob (`_matches_git_head`) and auto-refreshes the baseline **only** for provably-legitimate git-synced changes (INFO), while a change that does **not** match HEAD now correctly surfaces as a `CHANGED` alert instead of being trusted — and it runs a real integrity scan immediately after pulling new code, so this is **more** monitoring, not less. `--update` stays available for deliberate manual resets. **No false-alert window opens:** the only other integrity check is the 02:00 UTC security-scan, nothing runs between this pull and that next scan, and that scan auto-refreshes any git-synced changes. Also hardened the trigger from `echo "$INCOMING_DIFF" | grep -q ' agent/'` to a bash glob (`[[ "$INCOMING_DIFF" == *" agent/"* ]]`), removing the SIGPIPE-under-pipefail trap (lesson 2026-05-02) where `grep -q` closing the pipe on first match could make the `if` mis-read a real ` agent/` change as absent. **Verification:** `bash -n` + `shellcheck -S warning` clean; glob-match unit test (agent / web-only / multiline / docs-only) correct; `_matches_git_head` semantics demonstrated **live** — committed monitored files classify git-synced (would auto-refresh), the uncommitted in-session edit to `morning-check.sh` classifies DIVERGED (would alert, no auto-refresh), proving both branches; full `agent/self-test.sh` 134 pass / 0 fail / 2 warn, grade A, security A 90/100. The 2 warns are the intentional `.gnupg` #737 tripwire (Pavel-gated) and a **separate, pre-existing** cron-drift WARN — a *deliberate, documented, temporary* operator throttle of `external-domains-check.sh` to hourly (Pavel cost request / PR #789, carrying its own "REVERT once #789 merges" note); reconciling the canonical `setup/setup-cron.sh` to that temp stopgap would wrongly persist it, so the WARN-only "pending reconciliation" signal is correctly left as-is. Did **not** run the live `file-integrity.sh` mid-session — my edit to the monitored `morning-check.sh` is uncommitted, so a live check would correctly but noisily alert on it and write a spurious alert into `file-integrity-latest.json` until the next scan; the cron-scheduled 02:00 UTC run applies it cleanly once the PR is merged. **Next time:** carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737). A future session could teach the self-test §9d cron-drift check to recognize an explicit `TEMP STOPGAP`/`REVERT` marker so a documented temporary divergence reads as INFO rather than WARN — deferred to avoid masking genuine drift._

- [x] **[2026-06-17]** Security score stops mislabeling unrelated warnings as `rootkit_scan`; pending security updates get their own dimension (`agent/self-test.sh`, section 9f) — _Correctness fix on a clean day (in sync with origin, 0 open issues, 0 open PRs; today's log only the usual benign WARNs — one informational "1 pending security update", two expected file-integrity baseline auto-refreshes). The A–F security score's `rootkit_scan` dimension was keyed on `latest-scan.json`'s `overall_status`, but `security-scan.sh` (line 670) sets `overall_status="warnings"` for a wide OR of unrelated conditions: pending apt security updates (`upgradable_security>0`), unexpected listening ports, suspicious/high-rate/outbound connections, world-writable files, OR an actual rkhunter warning. With both rootkit scanners genuinely clean today (`rkhunter: clean, chkrootkit: clean`), the score still reported `rootkit_scan: warnings (-5)` — the −5 was the lone pending security update, blamed on rootkits. The same bug double-counted world-writable files (they flip `overall_status` AND are scored on their own line directly below). **Fix:** `rootkit_scan` now reads the actual scanner verdicts — `.rkhunter.status`/`.chkrootkit.status` (warnings → −5) and `.rkhunter.infected + .chkrootkit.infected` (>0 → INFECTED −40) — never `overall_status`. Pending security updates get a new, correctly-labeled `pending_security_updates` dimension reading `.cve_monitoring.upgradable_security` (>0 → −5), preserving the security signal under an honest label rather than dropping it. **Net effect:** today's score unchanged at 90 but accurately attributed (`rootkit_scan: clean (+0)`, `pending_security_updates: 1 (-5)`); a fully-clean day will now correctly read 95 instead of being silently docked; a day with both rootkit warnings and pending updates now penalizes both (−10) rather than conflating to −5 — stricter and more accurate, never weaker. No logging or security control touched. **Verification:** `bash -n` + `shellcheck -S warning` clean; live `self-test.sh` regenerated `data/security/security-score.json` with the two corrected labels (grade A); full suite 135 pass / 0 fail / 1 warn (intentional `.gnupg` #737 tripwire). Only `agent/self-test.sh` changed; the regenerated `security-score.json` is runtime `data/`, not committed. **Next time:** carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737). The network warning sources that previously rode the mislabeled −5 are deliberately not added to the hardening grade — transient operational state already surfaced by `incident-report.sh`/`log-alerting.sh`/the port inventory, not hardening config._

- [x] **[2026-06-16]** Time-series JSONL retention now covers every one-file-per-day family, not just bare metrics (`agent/disk-cleanup.sh`, section 5) — _Closed an unbounded-disk-growth gap on a genuinely clean day (in sync with origin, 0 open issues, 0 open PRs; today's log only 3 benign WARNs — two expected file-integrity baseline auto-refreshes + one informational "12 pending security updates"; the 06-12 hourly-check & log-analysis transient-exit fixes confirmed holding, no recurring ERROR/regression noise since). The data-retention policy (section 5, since 2026-03-05) compresses raw JSONL at 30 days / deletes `.gz` at 180, but its `find` only matched the bare `????-??-??.jsonl` metrics pattern. **Three other one-file-per-day JSONL families it never touched were growing forever:** `claude-usage-YYYY-MM-DD.jsonl` (per-Claude-run usage, `lib/claude.sh`) — **102 files back to 2026-03-07**; `latency-YYYY-MM-DD.jsonl` (network probes, `health-monitor.sh`) — 96 files; `YYYY-MM-DD-structured.jsonl` (structured logs, `marvin_log_json` in `lib/logging.sh`, in `LOGS_DIR`) — 32 files. **9.1 MB, ~3 files/day, unbounded** — the plain `.log` twins of the structured files delete at 30d (section 4) but their queryable JSONL counterparts lived forever; none match `????-??-??.jsonl` (which anchors the date at the *start*), so the policy silently skipped them. **Fix:** extended 5a (compress >30d) and 5b (delete `.gz` >180d) to span both `METRICS_DIR` AND `LOGS_DIR` and match all four families via a shared `_TS_JSONL_NAMES` find-predicate array — the tested compress/delete loop bodies are reused verbatim, only the `find` args changed; added `-maxdepth 1` (dirs hold no nested JSONL → identical behaviour, but pins the sweep, matching the section-6b precedent). **Why safe:** every consumer reads by exact recent-date constructed filename, never a broad glob — `perf-analytics.py` 7-day window, `weekly-analytics.sh` ~14d, `log-analysis`/`daily-digest`/`log-alerting`/`health-monitor` `${TODAY}` only — so none ever names a date >30d back, and `read_jsonl` already degrades gracefully on a missing file; mirrors the existing precedent that already gzips old metrics JSONL consumers tolerate. **Verification:** `bash -n` + `shellcheck -S warning` clean; read-only selector test matched exactly the intended files (135 >30d: 1 metrics / 70 claude-usage / 64 latency; structured glob matches all 32 but oldest is 30.3d so none cross +30 until tomorrow — correct) with **0** ≤30d files selected; full `--dry-run` exits 0, reports "Compressed 135 time-series JSONL (>30d)" (~1.1 M), no mutations; full `agent/self-test.sh` 135 pass / 0 fail / 1 warn (intentional `.gnupg` #737 tripwire), grade A, security A 90/100. Did NOT run the live cleanup mid-session — it mutates `data/` (gzips 135 files) and runs from cron at 06:00 UTC; selection is pure `find` filtering validated read-only, next scheduled run applies it safely. **Next time:** carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737); operator-gated `agent/cleanup-orphan-branches.sh`. A future session could fold the four patterns into a single iterated retention manifest rather than a hand-listed predicate — premature today._

- [x] **[2026-06-15]** `chkproc` + `certbot` added to the runaway-process allowlist (`agent/health-monitor.sh`) — _Stability/noise-reduction fix on a genuinely clean day: in sync with origin, 0 open issues, 0 open PRs, today's log just 3 benign WARNs (two file-integrity baseline auto-refreshes + the documented `claude-lock-timeout-expected-on-cron-overlap`). A WARN audit across all logs found two recurring runaway-process false positives in `health-monitor.sh`'s `>50% CPU` detector — same noise class as the prior `shellcheck`/`pg_isready`/`gzip`/`runc*`/`find`/`file`/`unattended-upgr*` additions. **`chkproc`** (chkrootkit `/proc`-scanner) fired 2026-06-10/12/13/14 at ~19:15 UTC — scans every PID, pegs one core for a single 5-min sample, run daily by `security-scan.sh` (04:00) and `/etc/cron.daily/chkrootkit`. **`certbot`** fired 2026-06-14 22:25 at 87.5% during an ACME renewal from `certbot.timer`. Neither could ever be *killed* (killer needs >50% for >10 min; neither runs that long), so the WARN was pure noise that also feeds log-alerting + lessons-learned. **Fix:** added `chkproc|certbot` to the trusted-name `case` AND extended the trusted-exe-path check to accept the exact binary `/usr/lib/chkrootkit/chkproc` (pinned from the initial `/usr/lib/chkrootkit/*` glob per PR #786 review — a single binary keeps the allowlist surface minimal and means any future chkrootkit helper that pegs a core gets a deliberate review decision rather than a silent skip) — `chkproc` lives at `/usr/lib/chkrootkit/chkproc`, a dpkg-owned root-only path outside the existing `/usr/{bin,sbin,local/bin}`+`/snap` set, so allowlisting the name alone would just trade the runaway WARN for an "Untrusted exe" WARN. `certbot` at `/usr/bin/certbot` already passes. **Security unchanged:** `prctl(PR_SET_NAME)` comm-spoof defense preserved — a `chkproc`/`certbot`-named process with a real exe elsewhere (e.g. `/tmp/evil`) still WARNs and falls through to tracking; the new exe-path entry is pinned to that one dpkg-owned binary. **`curl` deliberately NOT added** despite 3 detections — it's the prototypical exfil/runaway tool, completes in seconds legitimately (never actually killed), and its rare WARN is the safety net working; allowlisting would blind the killer to a malicious `curl`. **Verification:** `bash -n` + `shellcheck -S warning` clean; isolated `case`-branch harness confirmed all six cases (real chkproc/certbot skip silently; both spoofed-exe still WARN; curl still tracked; other helpers like `chkdirs` still tracked); full `agent/self-test.sh` 135 pass / 0 fail / 1 warn (intentional `.gnupg` #737 tripwire), grade A, security A 95/100. Did NOT run `health-monitor.sh` live mid-session (writes `data/` every 5 min, can trigger a web rebuild — needless contention); pure control flow, validated in isolation. **Next time:** carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737). Eventually the hand-maintained name allowlist could derive from a known-binaries manifest rather than one name/session — premature today._

- [x] **[2026-06-13]** `disk-cleanup.sh` now sweeps orphaned `claude-output-*.tmp` files in `LOGS_DIR` (`agent/disk-cleanup.sh`, new section 6b) — _Preventive disk-hygiene fix chosen on a genuinely clean day: today's log had only 3 benign WARNs (file-integrity baseline auto-refresh ×2, the documented `claude-lock-timeout-expected-on-cron-overlap`), the lessons regression-watcher reported "No recurring resolved-lesson patterns detected", and there were 0 open GitHub issues and 0 open PRs. The 06-10/06-11 `log-analysis` transient Claude exit-1 spike (4×/9×) had already returned to 0 on 06-12/06-13 — handled gracefully by the existing skip path, nothing to fix. **Latent gap found:** `run_claude()` (`lib/claude.sh:126`) writes every Claude response to `${LOGS_DIR}/claude-output-XXXXXX.tmp` and removes it via a `trap … RETURN`, which does **not** fire on `SIGKILL` (OOM, reboot, external timeout-kill). Such a hard-killed run leaks its temp file into `data/logs/` forever — and nothing cleaned it: section 3 matches only `*.md`, section 4 only the daily `????-??-??.log`, section 6 only `/tmp`. Every Claude run creates one (one 0-byte orphan was already present at audit time), so it's the box's most likely orphan source. **Fix:** new section 6b — an age-gated sweep `find "${LOGS_DIR}" -maxdepth 1 -type f -name 'claude-output-*.tmp' -mtime +0`, mirroring the existing section-3/4/6 loop shape (per-file `stat`, `marvin_is_dry_run` guard, `track_freed`). `-mtime +0` (>1 day) is the safety guarantee — no `claude -p` runs near 24h, so an in-flight run's temp file is never deletable; `-maxdepth 1` scopes to where `run_claude` writes; `track_freed` reports only when bytes>0 so 0-byte orphans clear silently. **Verification:** `bash -n` + `shellcheck -S warning` clean; isolated selector test picks only aged `claude-output-*.tmp` while leaving a fresh `.tmp`, a `.log`, and a `.md`; live `--dry-run` exits 0 with no breakage and correctly does NOT select today's <24h 0-byte orphan (in-flight protection confirmed); full `agent/self-test.sh` 134 pass / 0 fail / 1 warn (intentional `.gnupg` #737 tripwire), grade A, security A 95/100. **Next time:** fold `log-analysis.sh`'s `${ANALYSIS_FILE}.XXXXXX` and `file-integrity.sh`'s `.baseline.XXXXXX` (same orphan class, but once-daily + atomic-mv → far lower probability) into the sweep only if either is ever observed lingering. Long-standing carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); Pavel-gated `.gnupg/` cleanup (#737); `agent/cleanup-orphan-branches.sh`._

- [x] **[2026-06-12]** `hourly-check.sh` no longer crashes on transient Claude `exit 1` — captures the exit code + skips gracefully; companion regression-watch noise fix (`agent/hourly-check.sh`, `agent/lessons-learned.sh`, `data/lessons-learned.json` runtime) — _Today's fix-failure target, surfaced by the morning `Found 5 recurring pattern(s) matching resolved lessons` WARN. `hourly-check.sh:149` was a bare `OUTPUT=$(run_claude "hourly-check" "$FULL_PROMPT")`. Under `set -euo pipefail` + `trap marvin_error_trap ERR`, a **transient** Claude `exit 1` (the documented, resolved, intermittently-recurring `claude-exit-code-1-transient` condition) made the command substitution's non-zero status fire the ERR trap (`[ERROR] hourly-check.sh:149 — command failed (exit 1)`) and `set -e` killed the script **before the report was saved** — no `=== HOURLY CHECK COMPLETE ===`. Verified in the logs on 2026-06-05 / 06-10 / 06-11 (9 occurrences): every failing run is `STARTING → Claude run complete (exit=1) → ERROR :149 →` *(nothing)*, and on 06-11 log-alerting escalated it to a "Repeated error (4×)" alert. Every other Claude-calling script already handles this — `github-interact.sh` (the sibling hourly task), `log-watcher.sh`, `morning-check.sh`, `evening-report.sh` — `hourly-check.sh` was the lone outlier. **Fix:** mirrored `github-interact.sh` exactly: `export CLAUDE_LOCK_TIMEOUT=60` (hourly, a missed cycle is cheap), then `OUTPUT=$(run_claude …) && CLAUDE_RC=0 || CLAUDE_RC=$?`; `exit 2` (lock timeout) → INFO skip + exit 0, any other non-zero → WARN skip + exit 0 (next hourly run is a cheap retry, per the `run_claude_with_retry` doc), empty output → WARN skip; the exit-0/non-empty happy path is unchanged. **Two companion changes against the daily false-regression alarm the same transient WARNs produce:** (1) marked `claude-exit-code-1-transient` `expected_recurrence: true` in `data/lessons-learned.json` (a gitignored *runtime* file, edited live on disk — not committed), the same treatment `claude-lock-timeout-expected-on-cron-overlap` already carries, and generalised its stale "github-interact tasks" wording to all tasks + the capture-the-exit-code rule; (2) fixed the regression-watcher in `lessons-learned.sh` to skip a whole cluster when its **best** matching resolved lesson is `expected_recurrence` — previously it fell through and re-flagged the same `Claude exited with code 1` lines under the unrelated `exit-code-masking` lesson (matched only on the coincidental shared `exit`/`code` tokens), a misleading false positive. The two companion changes are needed *together*: (1) sets the flag, (2) makes the flag suppress the cluster rather than relabel it. **Verification:** `bash -n` + `shellcheck -S error` clean on both scripts; `data/lessons-learned.json` valid (`jq -e`); a `set -euo pipefail` + ERR-trap harness reproduces the OLD crash (trap fires, dies before COMPLETE, exit 1) and confirms NEW skips gracefully (exit 0, trap never fires) for exit 1 / exit 2 / empty with the happy path unchanged; re-running `lessons-learned.sh` drops the regression-watch count **5 → 1** (lone survivor: the `claude-output-capture-data-loss` match on `hourly-check.sh:149` itself — the bug this fixes, gone tomorrow once no new ERROR lines accrue) and the matcher still flags genuine non-expected lessons; full `agent/self-test.sh` 134 pass / 0 fail / 1 warn (the 1 warn is the intentional `.gnupg` #737 tripwire, Pavel-gated), grade A, security A 95/100. Did **not** run the live `hourly-check.sh` mid-session (it would contend for the Claude lock + spend an API call); the exit-code handling is pure control flow and was validated in isolation. **Next time:** (a) section 3 of `lessons-learned.sh` ("potential new lessons", a separate prose-text matcher) still lists the transient `Claude exit 1` lines as 5 "potential new lessons" — lower-value informational noise on a different mechanism, left for a focused pass to avoid scope creep here; (b) long-standing carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); deferred Pavel-gated `.gnupg/` cleanup (#737) and `agent/cleanup-orphan-branches.sh`._

- [x] **[2026-06-10]** `log-alerting.sh` CRITICAL-event detection now windows on recency — a self-healed transient no longer holds a critical alert active all day (`agent/log-alerting.sh`) — _Today's fix-failure target. At `04:09:39 UTC` `unattended-upgrades` (`apt-daily-upgrade.service`) stopped+immediately-restarted nginx mid package-upgrade; the 5-min health-monitor at `04:10:02` caught nginx in that sub-second window, logged `[CRITICAL] nginx is down — testing config before restart`, and self-healed it (verified: nginx up continuously since `04:10:03`, journal shows the apt-triggered stop/start; blog content recovered; exactly **one** CRITICAL line in today's log). Textbook transient. Yet the dashboard alert `critical-cb65e4f01e72` was **still active at 07:50**, ~4h later, and would have stayed active ~16 more hours until UTC day-rollover. **Root cause:** section 2 of `log-alerting.sh` scanned the **entire day's log** for any `[CRITICAL]` line; that line is permanent in today's log file, so every hourly `:50` run re-detected it and the merge logic kept re-confirming the alert active, never reaching the auto-resolve else-branch. Sections 3 (error-rate spike) and 6 (Claude failures) already window by recency — section 2 was the lone outlier. **Fix:** before detection, filter `critical_lines` to the **last 2 hours** via a lexicographic `>=` on the bracketed ISO-8601 timestamp field (ISO-8601 sorts chronologically as text — the exact technique section 3 uses). 2h is deliberately wider than the 1h cron gap so a transient is never missed *between* runs, while a recovered transient ages out and auto-resolves in ~2-3h instead of lingering all day; an ongoing/recurring critical keeps emitting fresh 5-min log lines and correctly stays active. Fail-open: a `date` failure keeps all lines rather than risk dropping a real alert. Only the section-2 match target changed — recursive-alert filter, merge/auto-resolve logic, and all other sections untouched. **Verification:** `bash -n` + `shellcheck -S error` clean; on the **live** `data/logs/2026-06-10.log` the 2h window drops the now-4h-old nginx event (1→0 lines) so the next run auto-resolves the stuck alert; a synthetic two-event log (5h-old nginx critical + 10-min-old `Disk 97%`) keeps only the recent one. The stuck alert self-clears on the first post-deploy run (and at latest tonight at `00:50 UTC` rollover under the old code, since the new day's log has no critical line). Did **not** run the live script mid-session — the unpatched `:50` cron would immediately re-raise the alert until this lands, so a manual clear would only flap. **Next time:** (a) consider whether section 1 (repeated errors, whole-day) wants the same treatment — left alone here because ">3 of the same message in a day" is a deliberately day-scoped semantic, not a transient-recovery one; (b) the `Net RX MB = 550 (4.0σ)` end-of-day anomaly at `00:00` is a legitimately heavier-traffic day (550 vs 305 MB avg), expected variance, not a code bug; (c) long-standing carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks); deferred Pavel-gated `.gnupg/` cleanup (#737) and `agent/cleanup-orphan-branches.sh`._

- [x] **[2026-06-08]** `file-integrity.sh` treats a live config byte-matching its committed `setup/` source as a legitimate deploy, not tampering (`agent/file-integrity.sh`) — _Fix-failure target from today's log: `/etc/nginx/sites-enabled/marvin` flagged `CHANGED` on **two consecutive days** (2026-06-07, 2026-06-08), baseline stuck at `2026-06-06T04:00:03Z`, and it would have alerted **daily forever** until a manual `--update`. The live nginx config changed at `2026-06-06 18:03` (the #777 `/llms.txt` deploy to live) — after that morning's 04:00 baseline. **Why the existing auto-refresh couldn't clear it:** `_matches_git_head()` only recognises files *inside* `MARVIN_DIR` whose content matches their **own path's** blob at HEAD; the live config lives at `/etc/nginx/sites-enabled/marvin` while its version-controlled source is `setup/nginx-site.conf` — a different path, outside the git tree — so a legitimately-deployed config can never be recognised as benign and alerts indefinitely. This is the same drift surface the 2026-06-07 self-test §9d tripwire watches (`setup/nginx-site.conf` ⇆ `/etc/nginx/sites-available/marvin`); the tripwire *detects* drift, this fix *clears* the false alert when there is none. Verified the live config is **byte-identical** to `setup/nginx-site.conf` (and `monitoring` ⇆ `setup/nginx-monitoring.conf`) — exactly the legitimate case. **Fix:** added `_matches_repo_source()`, the config-file analogue of `_matches_git_head()`: it maps each monitored live config to its committed `setup/*.conf` counterpart and returns success only on a **byte-for-byte `diff`** match. Such changes are classified `config-synced` (new category parallel to `git-synced`) — kept out of the CHANGED/alert path, surfaced in a new `config_synced` report array, and made eligible for the existing baseline auto-refresh (which now fires when *all* changes are provably legitimate: `git_synced` **or** `config_synced`, with zero `CHANGED`/`MISSING`/`NEW`). **Trust model unchanged** — the *same* assumption `_matches_git_head` already makes for agent scripts: an attacker editing nginx to add a rogue `location`/proxy would not reproduce the committed source byte-for-byte, so a match is a strong "legitimate deploy" signal, not a weakening. Paths with no `setup/` counterpart (e.g. `/etc/nginx/nginx.conf`) still return 1 and alert if changed; the #633 new-file guard is untouched (new files in dynamic paths still require explicit `--update`). Mapping is keyed only on the two monitored `sites-enabled/*` paths; `nginx-rate-limits.conf` is not in `MONITORED_PATHS` (left out, harmless). Report consumers unaffected (`security-scan.sh`/`self-test.sh` only read `.status`/`.changes`/`.missing_files`; the key is additive). **Verification:** `bash -n` + `shellcheck -S error` clean; a **live run** reclassified the nginx file as `config-synced`, set `status: clean`, and auto-refreshed the stuck baseline (also absorbing the legitimate `self-test.sh` change from #778 as `git-synced`) — equivalent to a manual `--update` but gated on provable legitimacy; full `agent/self-test.sh` 134 pass / 0 fail / 1 warn (the 1 warn is the intentional `.gnupg` #737 tripwire, Pavel-gated), grade A, security A 95/100. **Next time:** (a) the cron file `/etc/cron.d/marvin` is the *other* version-controlled live config that suffers this exact class (it alerted once on 2026-06-06 for the #771 disk-cleanup wiring) — `_matches_repo_source()` could be extended to it by reusing the §9d heredoc-extraction from `setup-cron.sh`, deferred here to keep this change focused and because the heredoc-vs-live comparison adds a failure mode worth its own session; (b) the today-only `log-watcher.sh` "Claude output is not a valid JSON array, saving raw" WARN (06:00:18) is the model nesting `recommended_action` inside the `raw_entries` array — caught gracefully by the 2026-06-01 validation gate (raw saved, no crash), 1 occurrence in 8 days, model-output variance not a code bug; tighten `log-analysis.md` only if it recurs. Long-standing carry-overs unchanged — split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh`; Phase 3 ECHO-protocol items (parked ~7 weeks). Deferred: Pavel-gated `.gnupg/` cleanup (#737), `agent/cleanup-orphan-branches.sh` (~2026-06-15)._

- [x] **[2026-06-07]** (weekly deep-enhance) Config-drift hardening: reconciled `setup/setup-cron.sh` to live + added a source⇆live drift tripwire to `self-test.sh` (`setup/setup-cron.sh`, `agent/self-test.sh`) — _Two changes addressing the recurring "version-controlled config silently diverges from the running config" class — the same failure mode behind the nginx `/llms.txt` #777 fix and the disk-cleanup cron gap (2026-06-05), whose CHANGELOG entry explicitly deferred "the broader reconciliation." **(1) Cron reconciliation.** `setup/setup-cron.sh` (the canonical bootstrap generator, the documented disaster-recovery path) had drifted badly from the live `/etc/cron.d/marvin`. A re-run would have **dropped four active jobs** — `backup.sh` (03:00), `security-scan.sh` (04:00), `fix-issues.sh`, `incident-report.sh` — **re-added the operator-removed afternoon self-enhance** (`0 10,15` → live `0 10`), reverted the deliberate Claude-API staggering (`github-interact` `0 *` vs live `5 */2`; `hourly-check` `0 *` vs live `35 *`), and **lost the `/root/.local/bin` PATH entry** the `claude` CLI lives under — which would have broken every cron job that calls Claude. The live file is the validated truth (system at health 97/100), so the heredoc body was rewritten to reproduce it **byte-for-byte**, verified by extracting the heredoc and `diff`-ing against `/etc/cron.d/marvin` → identical. The foot-of-script schedule summary was updated to match, and a header note records that `weekly-analytics.sh` intentionally lives in root's *personal* crontab (Sun 11:30), not this file, so it's never duplicated. **(2) Drift tripwire.** New `self-test.sh` section 9d read-only `diff`s three nginx configs (`nginx-site`/`nginx-monitoring`/`nginx-rate-limits`) against their installed counterparts and the extracted cron heredoc against the live cron file (valid because the `'EOF'` delimiter keeps `${MARVIN_DIR}` literal on both sides). **WARN-only** — an operator-reconcile signal, degrades gracefully if either side is absent. All four checks pass green now; they're tripwires for the next divergence, so the next #777 surfaces in the daily self-test instead of weeks later by chance. **No live mutation** — only repo source changed; the running cron/nginx were already correct. **Verification:** `bash -n` + `shellcheck -S error` clean on both files; heredoc-extract `diff` vs live cron → byte-identical; full `agent/self-test.sh` 134 pass / 0 fail / 1 warn (the 1 warn is the pre-existing `.gnupg` #737 tripwire), grade A, security A 95/100. **Next time:** the long-standing carry-overs are unchanged — (a) split `agent/security-scan.sh` (774 LOC) into `agent/lib/security/*.sh` workers; (b) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip, first contact at posledniping.cz), parked ~7 weeks; (c) the now-redundant `WEB_NOISE_PATTERNS` `/api/*.json|.svg` entries in `log-watcher.sh` could be pruned once the #777-class fix has a week of clean logs. Deferred items remain: Pavel-gated `.gnupg/` duplicate-key cleanup (#737) and `agent/cleanup-orphan-branches.sh` (~2026-06-15). A further follow-up worth a future session: fold `weekly-analytics.sh` into `/etc/cron.d/marvin` and drop it from root's personal crontab so a single file fully reproduces the schedule — deferred here because it requires a live crontab edit (remove-from-one, add-to-other) that risks double-execution if done carelessly._

- [x] **[2026-06-06]** `log-watcher.sh` nginx interest-matching now targets the request, not the referer/User-Agent — root-cause fix for the dashboard-poll flood (`agent/log-watcher.sh`) — _Today's headline WARN was `[2026-06-06T00:30:06Z] [WARN] Prompt too large (2603526 chars) — truncating to 400000` (~651K tokens) — **2.6 MB** of "interesting" log data, 3.7× yesterday's 703 KB. **First conclusion from the investigation: this was NOT a live failure to fix.** That 00:30 run was the *last firing of the pre-#773 code* before this morning's 04:00 morning-check pulled and deployed #773. The deploy is visible in the live log: interest-line counts go 217 (00:00) → ~185/cycle through 04:00 → **16 at 04:30 → 5-20 thereafter**, the moment the morning-check `git pull` completed (04:03:43). #773's web-noise blocklist *and* its hard 50 KB feed cap are now live and verified working (every post-04:30 run is <7 KB). So the live symptom is already resolved by deploy; tomorrow's 00:30 will be fine. **But the root cause flagged in yesterday's own "Next time" remained open**, and 2.6 MB (vs 703 KB the day before) is proof the blocklist approach doesn't converge: the interest pattern `marvin\.cz` is a substring of `robot-marvin.cz`, and **every** dashboard request carries the referer `https://robot-marvin.cz/`, so a plain `grep -iE "$INTEREST_RE"` flags every poll as "interesting". Each session added more `WEB_NOISE_PATTERNS` exclusions, but any *new* dashboard feed silently re-introduces the leak — which is exactly how 703 KB grew to 2.6 MB in 24h. **Structural fix (nginx/access branch only):** strip the trailing referer + user-agent quoted fields (combined-log-format `… "$referer" "$user_agent"`) before interest-matching, via a single `gawk` pass (`IGNORECASE=1` to replicate `grep -i`; the regex is passed through the **environment**, not `awk -v`, because `-v` processes C escapes and would mangle the `\.` in `marvin\.cz`). Keywords now match only the request line; the **original full line is still emitted** for Claude's context — only the *match target* is narrowed, so referer/UA are still visible to the classifier, just not used to trigger a match. Genuine inbound AI signals are POSTs/GETs to a path (`/.well-known/ai-negotiate`) or carry a keyword in the request query — all preserved; verified a synthetic `POST /.well-known/ai-negotiate` carrying *our own* dashboard referer is still kept. Referer/UA-only matches (our dashboard referer; a `claude-bot` UA hitting `/`) are never communication attempts and are correctly dropped. The **system-log branch is untouched** — so `marvin@` still legitimately matches mail logs — which is why the strip is scoped to the nginx branch rather than the shared `INTEREST_RE`. `WEB_NOISE_EXCLUDE` is kept as a cheap pre-filter for crawler UAs that hit well-known paths. **Measured on the live `/var/log/nginx/access.log`:** interest matches dropped from **7,247 → 3**; the 3 survivors are real request-path attack probes containing `hello`/`Hello` (`/hello.world?...allow_url_include`, two `index.php?s=...Hello` ThinkPHP RCE attempts), correctly surfaced for Claude to classify as attacks — not referer noise. Now a dashboard endpoint added tomorrow can never re-flood, because no dashboard GET to a JSON/SVG feed carries an interest keyword in its request line. **Verification:** `bash -n` + `shellcheck -S error` clean on `log-watcher.sh`; an isolated harness reproduced OLD=7247 vs NEW=3 on the live access.log and the synthetic-signal preservation; full `agent/self-test.sh` 130 pass / 0 fail / 1 warn (the 1 warn is the intentional `/home/marvin/git/.gnupg/` tripwire for #737, Pavel-gated), grade A, security A 95/100. Did not run the full script live (would advance log offsets + trigger a real Claude call + contend for the lock); the scan/match logic is pure text processing and was validated in isolation. **General principle worth carrying:** when filtering a *structured* log line for keywords, match against the relevant field, not the whole line — a host name or referer that contains your match token will self-trigger on every record. **Next time:** the long-standing carry-overs are unchanged — (a) split `agent/security-scan.sh` (774 LOC, 11 sections) into `agent/lib/security/*.sh` workers (dedicated session — per-section JSON contracts read by `self-test.sh` 9c / `incident-report.sh` / dashboard); (b) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip, first contact at posledniping.cz), parked ~7 weeks. Also: with the referer self-match gone at the root, the now-mostly-redundant `GET /api/[^ ]*\.json|\.svg|/api/blog` web-noise entries could be pruned in a future session once this fix has a week of clean logs (kept for now as a cheap belt-and-suspenders pre-filter). And the deferred items remain: Pavel-gated `.gnupg/` duplicate-key cleanup (#737) and `agent/cleanup-orphan-branches.sh` (~2026-06-15)._

- [x] **[2026-06-05]** `log-watcher.sh` dashboard-poll noise + `MAX_FEED_SIZE` overshoot fix (`agent/log-watcher.sh`) — _Top-priority fix-failure from today's own log: `[2026-06-05T02:30:05Z] [WARN] Prompt too large (709106 chars) — truncating to 400000`. log-watcher's 02:30 cycle found **703 KB** of "interesting" log data and run_claude crudely hard-truncated it to 400 KB (~177K tokens — an expensive call that drops the tail at an arbitrary byte boundary). Two compounding bugs. **(1) Root cause — Marvin's own dashboard polling matches the interest filter.** The web-log interest pattern (line 147) includes `marvin\.cz`, and the literal string `robot-marvin.cz` *contains* `marvin.cz` as a substring (verified: `[[ "robot-marvin.cz" =~ marvin\.cz ]]` → match). Every dashboard API request carries the referer `https://robot-marvin.cz/`, so **every** dashboard poll matches as "interesting". The `WEB_NOISE_PATTERNS` exclusion list (line 82) covered only a handful of endpoints (status.json, comms-summary.json, enhancements.json, about.json, …) and missed the bulk of the frontend's continuously-polled feeds — `metrics/recent.json` (1044 hits), `incidents/summary.json` (1043), `alerts/active-alerts.json` (1043), `peers-public.json` (1042), `external-domains.json` (1042), `thoughts.json` (525), `metrics/sla.json` (212), `changelog.json` (212), the report-card SVG, and blog GETs. Live count: **13,115** access.log lines matched the interest filter. Fix: added three generic web-noise exclusions — `GET /api/[^ ]*\.json`, `GET /api/[^ ]*\.svg`, `GET /api/blog`. Rationale: a GET to a public JSON/SVG data feed is never an AI communication attempt; genuine signals are POSTs to `/.well-known/ai-negotiate`, which is **not** under `/api/` and is still matched by the interest patterns. Measured on the live access.log: interest matches dropped from **6,528 → 2** (the 2 survivors are harmless Facebook `fbclid` click-throughs that Claude will classify as non-signals), and a unit check confirmed `GET /.well-known/ai-managed.json` is **not** caught by the new patterns — the real-signal path is fully preserved. Generic patterns rather than enumeration so new dashboard endpoints don't silently re-introduce the leak. **(2) Structural — `MAX_FEED_SIZE=50000` was a soft, post-hoc check.** The cap (line 266) was only tested *after* a whole file's interesting block had already been appended (lines 255-259), so a single high-volume file (nginx access.log is a *priority* file, scanned first) could overshoot by 14× — and starve later files (auth.log, error.log) of the budget entirely once the loop broke. Fix: bound each per-file block to the remaining budget *before* appending (`remaining=$((MAX_FEED_SIZE - collected_size))`; truncate the block to `${interesting:0:$remaining}` and `break` when `remaining<=0`), and tightened the trailing loop guard from `-gt` to `-ge`. This enforces the documented 50 KB contract as a true hard ceiling, so any future noise source from any file can never again force run_claude's 400 KB truncation. **Verification:** `bash -n` + `shellcheck -S error` clean on `log-watcher.sh`; an isolation harness with a simulated 700 KB file confirms it's truncated to exactly fill the remaining budget and the total caps at exactly 50000 (never exceeds); the noise-reduction and `.well-known`-preservation measurements above were run against the live `/var/log/nginx/access.log`. Did not run the full script live (would advance log offsets + trigger a real Claude call + contend for the lock) — the scan/cap logic is pure text processing and was validated in isolation instead. The two fixes are complementary: (1) removes the cause, (2) caps the blast radius of any future cause. **Next time:** the long-standing carry-overs are unchanged — (a) split `agent/security-scan.sh` (774 LOC, 11 sections) into `agent/lib/security/*.sh` workers (dedicated session — per-section JSON contracts read by `self-test.sh` 9c / `incident-report.sh` / dashboard); (b) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip, first contact at posledniping.cz), parked ~7 weeks. Also: if a future session wants to harden this further, `marvin\.cz` as an interest pattern is inherently self-matching on the referer — a cleaner long-term design would strip the referer field before interest-matching web logs, or match the domain only outside the referer position. And the deferred items remain: Pavel-gated `.gnupg/` duplicate-key cleanup (#737) and `agent/cleanup-orphan-branches.sh` (~2026-06-15)._

- [x] **[2026-06-04]** Runaway-process killer now excludes Docker-managed containers via cgroup provenance (`agent/health-monitor.sh`, `data/lessons-learned.json` runtime) — _Daily WARN audit (7 days) surfaced recurring `High CPU process detected` lines for generic comm names — `pg_isready` (5×), `python` (4×), `runc`/`runc:[2:INIT]` (6×), `python3` (2×), `gzip`, `shellcheck`, `sh`, `uvicorn`. Every one of these is a process inside the Marvin-Brain Docker stack (8 containers: postgres/lightrag/worker/api/mcp + grafana/prometheus/otel-collector). The prior fixes for this class hand-allowlisted specific names (`pg_isready`, `runc*`, `gzip` in PRs through 2026-06-02), but that approach is both incomplete (a new `python`/`uvicorn` hit every week) and, taken to its logical end, **unsafe**: `python`/`sh`/`uvicorn` are exactly the comm names a malicious or buggy host process would carry, so name-allowlisting them would re-open the very hole the runaway killer closes. **The deeper problem was a latent correctness/stability bug**, not just noise: generic container names fall through the name `case` to the kill-tracking path, so a legitimate LightRAG indexing/embedding job sustaining >50% CPU for >10 min would have been `kill -9`ed *from the host* (health-monitor.sh:382) — corrupting a container that Docker already manages via cgroup CPU limits + restart policy, i.e. Marvin fighting its own brain. No kill had fired yet (caught latent), but the audit shows the stack routinely crossing the 50% threshold and the risk grows as the brain does more work. **Fix:** inserted a container-provenance guard *before* the name-based allowlist — read `/proc/PID/cgroup` and `continue` (log INFO, never track for kill) when it matches the regex `/docker[-/]`, i.e. `docker-<hash>.scope` (cgroup v2, this host) or `/docker/<hash>` (cgroup v1). This is a **stronger** trust signal than the `comm`-name allowlist, not a weaker one: cgroup membership is assigned by Docker through cgroupfs, writable only by root, and **cannot be forged** by a process renaming itself via `prctl(PR_SET_NAME)` the way the `comm` field can (the spoofing vector that motivated #38's exe-path verification). Threat model is unchanged: an adversary who can place a process into a `docker-*.scope` cgroup already has root and can disable the whole monitor — exactly the assumption the existing exe-path trust already makes. Host daemons stay protected: `docker.service`/`containerd.service`/`cron.service`/`nginx.service` cgroups do **not** contain `docker` followed by `-` or `/`, so they remain fully subject to runaway detection (verified by regex + live cgroup test). The `pg_isready`/`runc*`/`gzip` name-allowlist entries are deliberately **kept** as belt-and-suspenders for the rare race where the cgroup read hits a process that exits between `ps` and the `cat` (empty read → falls through to the name allowlist). **Verification:** `bash -n` + `shellcheck -S error` clean on `health-monitor.sh`; a live classification test put all 8 brain-stack container PIDs (lightrag/postgres/worker/api/mcp/grafana/prometheus/otel) in `CONTAINER(skip)` and all of dockerd/containerd/cron/nginx in `HOST(check)`; a regex unit test confirmed `docker-abc.scope` and `/docker/abc` match while `docker.service`/`containerd.service`/`cron.service` do not; a full end-to-end `health-monitor.sh` run exits 0 and writes valid `status.json`. Codified as resolved lesson `host-runaway-killer-must-not-kill-container-procs` (count 28→29, runtime `data/lessons-learned.json`, consumed by `lessons-learned.sh` which still reports "No new recurring patterns detected"). General principle recorded: a host-level resource killer must scope itself to host processes (verify cgroup) and never reach into a cgroup another supervisor owns. **Next time:** with cgroup-based exclusion in place, the hand-maintained `pg_isready`/`runc*`/`gzip` name entries are now redundant for their primary purpose (kept only as the cgroup-read-race fallback) — a future session could prune them once the cgroup path has proven itself over a couple of weeks of logs. The two long-standing carry-overs are unchanged: (a) split `agent/security-scan.sh` (774 LOC, 11 sections) into `agent/lib/security/*.sh` workers (dedicated session — per-section JSON contracts read by `self-test.sh` 9c / `incident-report.sh` / dashboard); (b) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip, first contact at posledniping.cz), parked ~7 weeks. Also still deferred: Pavel-gated `.gnupg/` duplicate-key cleanup (#737) and `agent/cleanup-orphan-branches.sh` (~2026-06-15)._

- [x] **[2026-06-02]** Two Marvin-Brain Docker-stack noise cuts in `health-monitor.sh`: `runc*` runaway allowlist + process-count anomaly switched to daily-MAX baseline (`agent/health-monitor.sh`) — _Today's two WARNs both trace to the steady-state shift from the Marvin-Brain Docker stack (deployed 2026-05-25). **(1)** `[2026-06-02T05:40:02Z] [WARN] High CPU process detected: PID=1597062 runc at 100%`. `runc` is the OCI container runtime at `/usr/bin/runc` that containerd launches for the brain stack. Two comm shapes appear in the logs — plain `runc` and the container-init child `runc:[2:INIT]` — both briefly peg one core to 100% during container (re)starts and healthcheck cycles (4 hits in 14 days: 2026-05-27 `runc:[2:INIT]`, 05-31 `runc`, 06-01 `runc:[2:INIT]`, 06-02 `runc`). Added `runc*` to the runaway-process allowlist `case "$proc_name"` at health-monitor.sh:287; the `*` glob covers both shapes (verified by case-match test). Same trusted-binary mechanism as the `pg_isready`/`gzip`/`shellcheck` entries added in the 2026-05-25/26 PRs — and the exe-path verification immediately below (lines 303-308) still requires the resolved exe to live under `/usr/bin`, `/usr/sbin`, `/usr/local/bin`, or `/snap`, so a `prctl(PR_SET_NAME)`-spoofed `runc`-named process binding an untrusted exe still trips the runaway killer (defense-in-depth preserved). The leading `if [[ -z "$proc_exe" ]]; then continue; fi` also silently skips the common race where a short-lived runc child exits between `ps` and `readlink`. Added an inline comment block documenting the containerd/Marvin-Brain source. **(2)** Recurring process-count anomaly false positive: `[2026-06-02T04:05:01Z] [WARN] Anomaly: Processes = 200 (avg=182.29, stddev=3.65, deviation=4.9σ)`. This is the item the 2026-05-30 entry explicitly flagged to act on "if either pattern repeats this week" — it has now fired three times since the brain deploy (2026-05-25 at 200/5.6σ, 05-29 at 203/3.2σ, 06-02 at 200/4.9σ). **Root cause is identical to the already-fixed Memory MB case** (health-monitor.sh:104-107): the anomaly loop compares an *instantaneous* 5-min reading against a rolling 7-day baseline, but for process_count that baseline was the daily **average** (`.summary.process_count.avg`, ~182 since the brain stack) while the instantaneous reading naturally climbs toward the daily **peak** (~200) during cron runs and container churn. With the stddev floored at 2% of the mean (3.65), any instantaneous reading near the daily peak lands at 4-5σ above the average baseline → false alert. The daily summaries make the gap explicit: post-2026-05-25 process_count runs avg≈182 / max≈195-203 (vs pre-brain avg≈163 / max≈177). **Fix:** switched the process-count baseline from `.summary.process_count.avg` to `.summary.process_count.max` at health-monitor.sh:109 (and the corresponding `_proc_avgs`→`_proc_maxes` reference in the anomaly loop at line 127), exactly mirroring the Memory MB approach and comment. With the daily-max baseline the rolling 7-day band is mean≈195, stddev≈8 (after the 2% floor), 2σ-high≈211 — so today's instantaneous 200 sits comfortably inside the band (no alert), while a genuine process explosion past ~211 still fires *and* clears the unchanged absolute `min_threshold=200` floor. Sanity-checked against history: the real 2026-05-17 spike to 212 (11.5σ on the old pre-brain baseline) would still alert under the max baseline. The `min_threshold=200` absolute floor is deliberately left unchanged — it remains a sensible "never alert below 200 processes regardless" guard, and switching to the max baseline is the correct root-cause fix rather than repeatedly bumping that floor as the brain stack's footprint settles. **Verification:** `bash -n` + `shellcheck -S error` clean on `health-monitor.sh`; no stale `_proc_avgs` references remain; a live end-to-end `health-monitor.sh` run logs no process anomaly and writes `anomalies: []` to `anomaly-status.json`; full `agent/self-test.sh` 130 pass / 0 fail / 1 warn, grade A, security A 90/100 (the 1 WARN is the intentional `/home/marvin/git/.gnupg/` tripwire for issue #737, Pavel-gated, left untouched). **Next time:** the long-standing carry-overs are unchanged — (a) split `agent/security-scan.sh` (774 LOC, 11 sections) into `agent/lib/security/*.sh` workers (dedicated session needed for the per-section JSON contracts that `self-test.sh` section 9c / `incident-report.sh` / dashboard panels read); (b) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip, first contact at posledniping.cz), parked ~7 weeks. Also: as the Marvin-Brain stack settles, a small `agent/discover-docker-ports.sh`-style helper that derives container-process and port baselines from `docker ps` would beat hand-maintaining the runaway allowlist and EXPECTED_PORTS lists — worth doing once a 3rd/4th container stack lands (premature today). And the deferred items remain: Pavel-gated `.gnupg/` cleanup (#737) and `agent/cleanup-orphan-branches.sh` (~2026-06-15)._

- [x] **[2026-06-01]** Fix recurring `log-watcher.sh` exit-5 crash on non-array Claude JSON (`agent/log-watcher.sh`) — _Top-priority fix-failure from today's log: `[2026-06-01T05:30:19Z] [ERROR] log-watcher.sh:354 — command failed (exit 5): merged=$(echo "$existing" "$analysis_json" | jq -s 'add' 2>/dev/null)`. Not a one-off — the identical line errored on **2026-05-27T22:00:44Z** too. **Root cause:** log-watcher extracts the day's log-analysis JSON from Claude's raw output with `analysis_json=$(echo "$raw_output" | sed -n '/^\[/,/^\]/p' | head -500)` (line 336), then only checks it for **non-emptiness** at line 343 — never for **validity**. The existing `ANALYSIS_FILE` is verified valid at line 352 (`jq empty`), so the merge can only fail because of `analysis_json`. When Claude answers with a single JSON object (`{"classification":…}`) instead of the requested array, or `head -500` truncates a long array mid-structure, the merge `echo "$existing" "$analysis_json" | jq -s 'add'` is handed `[valid_array, non_array]` and `add` exits **5** (reproduced for all four shapes: object, truncated, bare number, bare string → all exit 5, matching the production code exactly). Under `set -euo pipefail` + `trap marvin_error_trap ERR`, the failing command substitution at line 354 kills the script **before** the graceful `if [[ -n "$merged" ]] && echo "$merged" | jq empty` fallback on line 355 can run. Consequences each time it fires: the cycle's `incoming-signals.json` update at line 398 is skipped (lost communication/attack detection for that 30-min window), no analysis file is written, and a spurious ERROR pollutes the log — the same silent-death shape that hid log-watcher's lock-timeout bug for six days before PR #700. **Two-layer fix.** (1) **Root-cause validation gate** inserted right after the two empty checks (between lines 347 and 349): `if ! echo "$analysis_json" | jq -e 'type == "array"' >/dev/null 2>&1; then` save the raw output to `${COMMS_DIR}/log-analysis-raw-${TIMESTAMP}.txt` for forensics and `exit 0` — mirroring the existing "Could not parse JSON" handler immediately above it. This stops malformed input before it reaches *any* downstream `jq` consumer, not just the merge: the `comm_entries` (376), `attack_count` (377), `comm_count` (378), and the `updated_signals` merge (387) all assume an array. Verified the gate blocks all four exit-5 shapes and lets a valid array through untouched (happy path unchanged — `jq -e 'type=="array"'` returns truthy `true`, exit 0, condition false, fall through). (2) **Defense-in-depth** `|| true` appended to the merge command substitution at line 354 so that even an unforeseen incompatible-shape merge (e.g. two valid-but-mismatched JSON types) degrades to the graceful handler instead of tripping `set -e` — the codified `exit-code-masking` lesson applied to a command substitution. **Verification:** `bash -n` + `shellcheck -S error` clean; a faithful production-structure `set -e` reproduction confirms OLD dies with **exit 5** (the ERR trap fires, the "AFTER merge" handler is never reached — exactly matching the log) while NEW reaches the handler and exits 0; full `agent/self-test.sh` 130 pass / 0 fail / 1 warn, grade A, security A 95/100. The 1 WARN is the **intentional** `/home/marvin/git/.gnupg/` tripwire for issue #737 (Pavel-gated duplicate-key removal, still open) — left untouched. **Next time:** the long-standing carry-overs are unchanged — (a) split `agent/security-scan.sh` (774 LOC, 11 sections) into `agent/lib/security/*.sh` workers, needing a dedicated session for the per-section JSON contracts that `self-test.sh` section 9c / `incident-report.sh` / dashboard panels read; (b) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip, first contact at posledniping.cz), parked ~7 weeks. Also worth a focused pass: log-watcher's JSON-extraction is fragile by construction (regex slice of free-form LLM output) — if the validation gate starts firing regularly in the daily logs, the better fix is to tighten the `log-analysis.md` prompt or wrap extraction in a small "find the first balanced `[…]`" helper rather than the line-anchored `sed`. And the deferred items remain: Pavel-gated `.gnupg/` cleanup (#737) and `agent/cleanup-orphan-branches.sh` (~2026-06-15)._

- [x] **[2026-05-30]** Two cron-overlap noise cuts: github-interact 300s Claude-lock timeout → 60s + graceful exit-2 handler; `gzip` added to runaway allowlist (`agent/github-interact.sh`, `agent/health-monitor.sh`, `data/lessons-learned.json`) — _Daily WARN audit surfaced one high-value pattern and one low-value but cheap one. **(1)** `github-interact.sh` (hourly cron `5,35 * * * *`) collides with `self-enhance.sh` (08:00 UTC) at the 08:05 UTC slot. Self-enhance routinely holds `/tmp/marvin-claude.lock` for 6-10 min while it composes the day's enhancement; github-interact then waits the default 300s and logs `[WARN] Claude lock timeout after 300s — skipping github-interact (another Claude run is active)` followed downstream by `[ERROR] No response from Claude for GitHub decisions` + `exit 1`. Audit shows this firing on **10 of the last 15 days** (2026-05-15, 16, 22, 25, 26, 27, 28 confirmed in `data/logs/*.log`; the gap days are when self-enhance happened to finish under 5 minutes). Same shape as the log-watcher.sh issue that PR #700 fixed on 2026-05-15 with a 60s cap + exit-2 graceful skip — applied the identical pattern at github-interact.sh:209-217. The new block exports `CLAUDE_LOCK_TIMEOUT=60` immediately before the `run_claude` call, captures the exit code with `&& CLAUDE_RC=0 || CLAUDE_RC=$?` (lesson `exit-code-masking` — bare `$()` masks the failure path under `set -e`), and short-circuits cleanly when `$CLAUDE_RC == 2` with `INFO github-interact skipped — Claude lock held by another task; next hourly run will catch up` + `exit 0` *before* falling into the existing empty-response check. The empty-response branch still fires for legitimate "no response" cases (process killed mid-run, API outage producing empty body); only the lock-timeout shape stops looking like an error. Cost-vs-benefit: hourly cadence + 30-min-apart cron slots mean missing one run is genuinely free (the next run picks up the same work — push status, recent logs, open issues — none of which need real-time response). **(2)** `gzip at 100.0% CPU` runaway WARN at 01:00 UTC fires when `disk-cleanup.sh:89`'s metric-JSONL compression loop catches the 5-min runaway sampler at the wrong moment (rarely; logrotate's per-day rotations contribute similarly). Same trusted-binary shape as `shellcheck`/`pg_isready` already in the allowlist — added `gzip` to the `case "$proc_name"` pattern at health-monitor.sh:287 with a comment block explaining the disk-cleanup/logrotate sources. The exe-path verification at lines 299-303 still requires `/usr/bin/gzip` (or `/usr/local/bin/`, `/snap/`), so a comm-field-spoofed `gzip` named process binding to an untrusted exe path will still trip the runaway killer — defense in depth. Only 2 hits in the last 7 days (2026-05-23 and today), but cheap to silence and consistent with the established noise-cut cadence from PR #685 (2026-05-25 shellcheck) and PR #688 (2026-05-26 pg_isready). **(3)** Extended lesson #26 `claude-lock-timeout-expected-on-cron-overlap` in `data/lessons-learned.json` to reference both safeguard locations (log-watcher.sh:323-329 since 2026-05-15 and now github-interact.sh:209-217 since 2026-05-30). Bumped `occurrences` 4 → 14 because the github-interact 10 hits were never previously counted toward this lesson — they were a *different incidence* of the same designed-behavior pattern. Reframed the safeguard from a log-watcher-specific recipe to a general principle ("Scripts whose Claude call overlaps with a higher-priority once-a-day task must export CLAUDE_LOCK_TIMEOUT=60 before invoking run_claude, then branch on exit code 2…"), making it directly applicable when the *next* script hits the same overlap class without having to re-derive it. Bumped `last_updated` 2026-05-29 → 2026-05-30. `expected_recurrence: true` retained, so the regression-watcher in section 4 of `lessons-learned.sh` continues to silently accept both the 60s and (legacy) 300s variants. Verified end-to-end: `bash -n` clean on both edited scripts, `shellcheck -S error` clean on both, `jq -e` parses the updated lessons file, lesson count holds at 28; full `self-test.sh` 128 pass / 0 fail / 1 warn (the 1 WARN is the *intentional* `/home/marvin/git/.gnupg/` tripwire for issue #737 — left alone, since the duplicate key material is still on disk per direct verification of `private-keys-v1.d/*.key`), grade A, security A 95/100; `lessons-learned.sh` reports `No new recurring patterns detected` and `No recurring resolved-lesson patterns detected`. **Next time:** the two long-standing carry-overs remain unchanged — (a) split `agent/security-scan.sh` (774 LOC, 11 cleanly-delimited sections) into `agent/lib/security/*.sh` workers as the first slice of the oversized-scripts cleanup; needs a dedicated session because of the per-section JSON contracts that `self-test.sh` section 9c, `incident-report.sh`, and the dashboard panels read directly. (b) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip protocol, first attempt at posledniping.cz contact) parked at "Phase 3" for ~7 weeks. Also: today's WARN audit surfaced two single-occurrence anomalies worth tracking but not yet acting on — `Anomaly: Processes = 203 (3.2σ)` at 2026-05-29 19:00 UTC and `Anomaly: Net TX MB = 175 (2.2σ) — 2026-05-28 full-day total`. The latter is the post-#744-fix correctly-computed anomaly noted in 2026-05-29's entry; both are below the worry threshold but if either pattern repeats this week, the rolling-baseline math might want the same `expected_recurrence` carveout that lesson #26 just got. And: the deferred items from yesterday — Pavel-gated removal of the `.gnupg/` duplicate key material (issue #737, still open) plus `agent/cleanup-orphan-branches.sh` (~2026-06-15 once two weeks of the PR #735 fix have proved no new accumulation) — are unchanged._

- [x] **[2026-05-29]** Fix net-anomaly false alarm from byte-counter wrap polluting rolling baseline (`agent/metric-aggregate.sh`, `data/lessons-learned.json`) — _This morning's first log line was `00:00:03Z [WARN] Anomaly: Net TX MB = 175 (avg=83.67, stddev=42.20, deviation=2.2σ) — 2026-05-28 full-day total` — exactly the shape the regression-watcher tells you to investigate before treating as benign. Followed the trail. **(1)** The 6-day baseline for 2026-05-28's anomaly check included `data/metrics/2026-05-27-daily.json`, which has `rx_bytes_first: 10,157,734,241` and `rx_bytes_last: 57,323,502`. The cumulative kernel byte counter on `enx3` reset (or the interface bounced) between samples — host uptime confirms no reboot, so most likely a transient interface event. The schema clamp at `metric-aggregate.sh:168-169` (`if last >= first then ... else 0 end`) correctly emits `rx_mb: 0` / `tx_mb: 0` for that day rather than a negative MB count, but the anomaly-check baseline-collection loop at line 207 then sucked all 6 prior days into the mean/stddev calc, *including* those zeros. Verified the math against the real files: pre-fix `n=6 mean=83.67 stddev=42.20` (matches the WARN exactly), with `tx_mb` baseline values `[123, 73, 76, 114, 116, 0]`. Drop the 0: post-fix `n=5 mean=100.40 stddev=21.38`. The same 175 MB day still alerts at 3.49σ — but that's a *correctly-computed* anomaly (175 is genuinely above the 100 ±21 normal-day band), not a noise-floor artifact from a single corrupted-zero day pulling the average down. **(2)** Fix at `metric-aggregate.sh:207-216`: the per-file jq read now guards with `if (.summary.network.rx_bytes_last // 0) < (.summary.network.rx_bytes_first // 0) or (.summary.network.tx_bytes_last // 0) < (.summary.network.tx_bytes_first // 0) then empty else .summary.network[$m] // empty end`. The wrap signature (`last < first` on either direction) silently excludes the day from the baseline. The schema retains the unreliable zeros so historical inspection still shows the wrap event (the bytes_first/bytes_last raw fields make it self-evident anyway). The existing `n >= 3` guard at line 217 already handles the case where too many baseline days are excluded (anomaly check skips entirely — better than firing a low-confidence alert). **(3)** Codified as resolved lesson #28 `baseline-pollution-from-sentinel-zero` (severity: medium, occurrences: 1, references metric-aggregate.sh:207-216 as the safeguard). Stated as a general principle so the next aggregator that uses 0/null/-1 as a fallback (CPU=0 when /proc unreadable, latency=0 when ping fails, fail2ban_banned=null on systemctl glitch, …) gets the same filtering from day one — without having to repeat this entire forensic loop. Verified end-to-end: `bash -n` + `shellcheck -S error` clean on `metric-aggregate.sh`, `jq -e` parses the updated `lessons-learned.json`, lesson count 27 → 28, `last_updated` bumped 2026-05-27 → 2026-05-29; full `self-test.sh` 128 pass / 0 fail / 0 warn, grade A; `lessons-learned.sh` still runs clean (still 28 lessons + 6 anti-patterns, "No new recurring patterns detected", "No recurring resolved-lesson patterns detected"). **Next time:** the two long-standing carry-overs remain — (a) split `agent/security-scan.sh` (774 LOC, 11 cleanly-delimited sections) into `agent/lib/security/*.sh` workers as the first slice of the oversized-scripts cleanup; needs a dedicated session because of the per-section JSON contracts that downstream consumers (`self-test.sh` section 9c, `incident-report.sh`, dashboard panels) read directly. (b) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip protocol, first attempt at posledniping.cz contact) parked at "Phase 3" for ~7 weeks. Also: the new lesson's "general principle" would justify auditing other anomaly-detection paths — `health-monitor.sh`'s 2σ checks for CPU/memory/load/process_count all use rolling baselines that could in principle suffer the same sentinel-zero pollution from `collect_metrics` fallbacks, though spot-check today shows none of those metrics have a 0-on-error path. Worth a focused audit pass when time permits. And: the deferred items from yesterday's punch list (Pavel-gated `! [[ -d /home/marvin/git/.gnupg ]]` self-test assertion + ~2026-06-15 `cleanup-orphan-branches.sh`) are unchanged._

- [x] **[2026-05-28]** Clean up `git status` noise — `.gnupg/` and `Marvin-Brain/` added to `.gitignore`, GitHub issue filed for stale duplicate GPG private key material at `/home/marvin/git/.gnupg/` (`.gitignore`, GitHub issue) — _Routine `git status` at session start showed two persistent untracked entries: `.gnupg/` and `Marvin-Brain/`. Investigation turned up something more substantive than a cosmetic clean-up. **(1)** `/home/marvin/git/.gnupg/private-keys-v1.d/` holds two `.key` files — `91BC9AB4003C351F2CE278FBC7FEBE877CA5E964.key` (3789 bytes, **664** permissions) and `DF1CD970C4DC2CDFEFBE898F1A554F744F29A523.key` (3787 bytes, **664** permissions). The keygrip filenames match the active secret keys at `/home/marvin/.gnupg/private-keys-v1.d/` (correctly `600`), and `diff -q` returned identical content for both pairs. So the on-disk material at `/home/marvin/git/.gnupg/private-keys-v1.d/` is byte-identical duplicates of Marvin's live RSA-4096 signing identity (rsa4096 2026-02-22 [SCEAR], `Marvin (AI Agent) <marvin@robot-marvin.cz>`). The parent dir `/home/marvin/git/.gnupg/` is `700 marvin:marvin`, so other users on the system cannot traverse into the loose-perms subdirectory — but the duplicate exists in the project tree (the working directory of every cron script), the perms are visibly worse than the active copy, and `gpg --homedir /home/marvin/git/.gnupg --list-secret-keys` returns nothing (no working keyring, just orphan key blobs). This is a dormant artefact from initial Feb-23 setup before `GNUPGHOME=/home/marvin/.gnupg` was set in `common.sh`. Removal is the right move but is a destructive action affecting GPG key material — deferred to Pavel via filed issue #737 with exact verification commands (`gpg --homedir /home/marvin/.gnupg --list-secret-keys` should show both keygrips, `git log --all -- agent/` should still sign correctly afterwards). **(2)** `Marvin-Brain/` is the externally-cloned sibling repo (deployed 2026-05-25 per memory) with its own `.git/` tree and a `.env` containing `BRAIN_API_KEY` + `OPENAI_API_KEY`. Should never be tracked in Marvin's main repo and shouldn't appear in `git status` either. **(3)** Edit to `.gitignore`: added `.gnupg/` (covers the parent, leaves the existing more-specific `.gnupg/private-keys-v1.d/` and `.gnupg/openpgp-revocs.d/` entries in place as belt-and-suspenders so the rule still holds if `.gnupg/` is ever un-ignored) and `Marvin-Brain/`. No files on disk are touched — `git status` cleanup is purely visibility. Live-verified: post-edit `git status --short` shows only the intentional changes for this PR (the `.gitignore` itself plus CHANGELOG/roadmap updates) instead of the two phantom untracked lines. Codebase health unchanged: `self-test.sh` 128 pass / 0 fail / 0 warn, grade A; shellcheck warning count holding at 0 (no scripts touched). **Next time:** Two long-running carry-overs remain — (a) split `security-scan.sh` (~770 LOC, 11 cleanly-delimited sections) into `agent/lib/security/*.sh` workers; needs a dedicated session because of per-section JSON contracts that downstream consumers (`self-test.sh` section 9c, `incident-report.sh`, dashboard panels) read directly. (b) Phase 3 ECHO-protocol items parked since ~2026-04 (broadcast/listen/handshake, gossip, first contact at posledniping.cz). Also: if Pavel confirms the duplicate GPG removal, a one-line addition to `agent/self-test.sh` checking `! [[ -d /home/marvin/git/.gnupg ]]` would surface any future re-creation immediately. And: the planned cleanup of 19 orphan `fix/issues-*` branches (deferred yesterday to ~2026-06-15 natural attrition) is still pending; `agent/cleanup-orphan-branches.sh` stays parked until two weeks have passed to verify the PR #735 code fix actually stops accumulation._

- [x] **[2026-05-27]** GitHub API 422 errors now log `.errors[]` detail + fix-issues PR-creation failures clean up the orphan remote branch (`agent/lib/github.sh`, `agent/fix-issues.sh`, `data/lessons-learned.json`) — _Today's 00:27 UTC log had `[ERROR] GitHub API POST /repos/INFO-WEB-s-r-o/Marvin/pulls: HTTP 422 / Response: Validation Failed` from `fix-issues.sh` attempting a duplicate fix for issue #733 (already on PR #717's branch, not yet on main). Two compounding gaps showed up: **(1)** GitHub's 422 always returns the generic `"Validation Failed"` in `.message` — the actual cause (e.g. `No commits between main and fix/issues-NNN`, `A pull request already exists`, `head is not a valid branch`) lives in `.errors[]`. `github_api()` in `lib/github.sh` only logged `.message`, so every 422 was a black box. Added a second `[ERROR] Errors: ...` log line using `jq -r '[.errors[]? | (.message // .field // .code // tostring)] | join(" | ")'` — robust against the three shapes GitHub uses (objects with `.message`, objects with only `.field`/`.code`, plain strings). Conditional on `.errors[]` being non-empty, so successful responses and other 4xx errors without `.errors[]` are unaffected. Synthetic test on a realistic 422 payload returned `"No commits between main and fix/issues-1779841501"` exactly as expected. **(2)** When `fix-issues.sh` PR creation fails *after* the branch has already been pushed (line 337-341 push, line 379 POST), the script previously warned at line 383 and exited at 384 — leaving the just-pushed remote branch stranded. Audit of `GET /repos/.../branches?per_page=100` returned **19 orphan `fix/issues-NNNNNNNNNN` branches** accumulated between roughly 2026-04 (PR #460-era timestamps) and 2026-05-27, each with a real commit that never made it into a PR. Added `git push origin :BRANCH` (delete-syntax, silent no-op if the branch is already gone) immediately after the warning, with a fallback WARN if the delete itself fails. Local branch is left in place; the existing EXIT trap returns to main and any stash-pop happens there. **Existing 19 orphans were not bulk-deleted in this PR** — that's a shared-state destructive action and the code fix prevents future accumulation; natural attrition + `log-export.sh`'s stale-branch sweep will catch them eventually. **(3)** Codified as resolved lesson #27 `github-422-validation-errors-hidden` in `data/lessons-learned.json` (severity: medium, occurrences: 2 — 2026-04-14 and 2026-05-27 are the only HTTP 422 hits across all logs) referencing both files as the safeguard. Lesson count went 26 → 27, `last_updated` bumped 2026-05-21 → 2026-05-27. Verified end-to-end: `bash -n` + `shellcheck -S error` clean on both files; `jq -e .` validates the lessons JSON; `self-test.sh` 126 pass / 0 fail / 0 warn, grade A. Also: PR #717 (`enhance/backup-offsite-435`) has been open since 2026-05-23 with the actual `agent/backup-offsite.sh` content, blocking issues #724-#734 from being fixable on main. That's Pavel's call to merge — not a self-enhance scope. **Next time:** (a) the two long-standing carry-overs remain unaddressed — split `security-scan.sh` (~770 LOC, 11 cleanly-delimited sections) into `agent/lib/security/*.sh` workers as the first slice of the oversized-scripts cleanup (needs a dedicated session for the per-section JSON contracts downstream consumers depend on), and the Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip, first contact attempt at posledniping.cz) parked at "Phase 3" for ~7 weeks. (b) Once PR #717 lands, the existing 19 orphans should get a single cleanup pass — could be a small `agent/cleanup-orphan-branches.sh` that calls the GitHub API to find `fix/issues-*` branches with no associated PR and older than 7 days, then deletes them. Currently premature with the new code-fix in place._

- [x] **[2026-05-26]** Two Marvin-Brain noise-source eliminations: `pg_isready` runaway-process allowlist (`agent/health-monitor.sh:287`) + 4 new ports in EXPECTED_PORTS baseline (`agent/security-scan.sh:145`) — _Both noise sources trace back to the Marvin-Brain deployment landed yesterday (2026-05-25 per memory). **(1)** `pg_isready` triggered `WARN High CPU process detected: PID=NNN pg_isready at 60.0%` on 2026-05-25 (3 hits) and 2026-05-26 (1 hit at 05:35:02Z) — the marvin-brain-postgres-1 container's Docker `healthcheck` runs `pg_isready -U admin -d brain` every 5s with a 3s timeout (`Marvin-Brain/docker-compose.yml`). On every healthcheck the brief connection-setup spike crosses the 50% CPU threshold in `health-monitor.sh`'s `ps -eo pid,%cpu,comm --sort=-%cpu` window. The existing exe-path verification (added 2025 to block prctl(PR_SET_NAME) spoofing per issue #38) won't accept it for a *good* reason: `readlink -f /proc/PID/exe` returns empty for container processes because the symlink target (`/usr/lib/postgresql/17/bin/pg_isready`) lives in the container's mount namespace and doesn't resolve from the host root. Verified by inspecting the live container postgres process: `ls -la /proc/3813415/exe` → `/usr/lib/postgresql/17/bin/postgres`, `readlink -f` → empty. The case branch at `health-monitor.sh:287` *already* has the right shape for empty-exe (originally for `claude|apt*|...` race-condition skips between `ps` and `readlink`) — it does `if [[ -z "$proc_exe" ]]; then continue; fi` as the first action inside the branch. Adding `pg_isready` to that branch silently allowlists container processes via exactly the same mechanism. Inline comment block explains *why* `pg_isready` is here (container mount namespace), so the next operator doesn't assume it's another race-condition entry and accidentally narrow the allowlist. **(2)** Today's 02:04:07Z security-scan logged `WARN Unexpected listener on port 3100/5432/8000/8787` and `WARN Found 4 unexpected listening port(s)`. All 4 are docker-proxy entries from the same Marvin-Brain stack — verified by `docker ps --format '{{.Names}} {{.Ports}}'`: 3100→marvin-brain-mcp (MCP server), 5432→marvin-brain-postgres (pgvector), 8000→marvin-brain-lightrag, 8787→marvin-brain-api. All four are bound to `127.0.0.1` only (`ss -tlnp` confirms `LISTEN 0 4096 127.0.0.1:NNNN`), so this is legitimate localhost-only traffic, not a security incident. Added all four to `EXPECTED_PORTS` in `security-scan.sh:145` (kept the list numerically sorted: `… 3000 3001 3100 4317 4318 5432 6379 8000 8043 8787 8889 9090 …`), updated the header-comment port glossary with a new "Marvin-Brain stack (deployed 2026-05-25, all docker-proxy bound to 127.0.0.1)" block describing each one, and also added the same four ports to `LOCALHOST_ONLY_PORTS` at line 157 — defense-in-depth: the existing `LOCALHOST_ONLY_PORTS` loop alerts if any of them ever escapes to `0.0.0.0` or `[::]`, which would be a real regression (someone removed the `127.0.0.1:` prefix from a docker-compose port mapping) worth waking up to. Both files clean under `bash -n` + `shellcheck -S error`. Live verification: ran `security-scan.sh`, confirmed `unexpected_count: 0` in the regenerated `port-inventory.json` (was 4 yesterday). Net effect: 5 WARN lines/day eliminated, both from the marvin-brain stack-onboarding. **Next time:** the two carried-over items from yesterday's punch list remain — (1) split `security-scan.sh` (now 770 LOC after today's port-list comment expansion, still 11 cleanly-delimited sections) into `agent/lib/security/*.sh` workers as the first slice of the oversized-scripts cleanup; this needs a dedicated session because of the per-section JSON contracts that downstream consumers depend on. (2) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip protocol, first attempt at posledniping.cz contact) — parked for ~6 weeks. Also worth thinking about: as more dockerized services land on Marvin (the brain stack is the first; more may follow), the `EXPECTED_PORTS` / `LOCALHOST_ONLY_PORTS` lists will need to be re-derived rather than hand-maintained. A small `agent/discover-docker-ports.sh` that scans `docker ps` and produces a baseline for security-scan.sh to consume would scale better than today's manual approach, but is worth waiting for a 3rd or 4th container stack before doing — premature today._

- [x] **[2026-05-25]** Two noise-floor reductions: `shellcheck` runaway-process allowlist (`agent/health-monitor.sh:287`) + HTTP-status / retry-counter normalisation in lessons-learned auto-detector (`agent/lessons-learned.sh:110`) — _Two small, independent fixes from the daily-log noise audit. **(1)** `shellcheck` triggers `WARN High CPU process detected: PID=NNN shellcheck at NN%` whenever `self-test.sh` runs the shellcheck pass during weekly/self-enhance cycles. Pattern in May logs alone: 4 hits (2026-05-02 95.1%, 2026-05-08 85.7%, 2026-05-13 98.3%, 2026-05-23 98.1%), all at 08:05/08:10 UTC. shellcheck lives at `/usr/bin/shellcheck` so it already passes the trusted-path check inside the `case` branch — the only thing missing was the `proc_name` glob. Added it to the existing pattern: `claude|apt*|dpkg*|unattended-upgr*|ps|jq|fail2ban*|file|appstreamcli|shellcheck`. Same shape as `apt*` and `file`. **(2)** The "Potential New Lessons" detector in `lessons-learned.sh` normalises log lines with `sed 's/PID [0-9]*/PID NNN/g; s/[0-9]\{4\}-...{2\}/DATE/g'` before aggregating, but it didn't normalise HTTP status codes or retry counters. Result: yesterday's 13 `GitHub API … HTTP 000` retries (one transient github.com network blip during 2026-05-24 00:05–00:08 UTC) fragmented into 4 separate "potential new lesson" entries in `lessons-summary.md` — `retry 0/3`, `retry 1/3`, `retry 2/3`, and `failed after 3 retries` all looked like distinct recurring patterns to the detector. The whole point of the section is to surface a *single* recurring issue, not to fan it out into four. Added `s/HTTP [0-9]\{3\}/HTTP NNN/g; s/retry [0-9]\+\/[0-9]\+/retry N\/M/g` to the sed pipeline so status codes and retry attempts collapse to the same normalised form. Verified on the actual 2026-05-24 log slice: pre-fix produced 4 fragmented patterns each at 3-4 hits; post-fix produces 2 cleanly-aggregated patterns at 10× (the retries) and 3× (the final fail). The 3-hit minimum threshold remains so genuinely transient errors (1-2 occurrences) still don't appear. Both changes verified end-to-end: `bash -n` + `shellcheck -S error` clean on both files, full self-test 126 pass / 0 fail / 0 warn, grade A. Re-ran `lessons-learned.sh` against today's data — summary now shows the collapsed entries as expected, and the auto-detector still finds new patterns (so it isn't broken or silenced, only de-duplicated). **Next time:** the two carried-over items remain — (1) split `security-scan.sh` (769 LOC, 11 cleanly-delimited sections) into `agent/lib/security/*.sh` workers as the first slice of the oversized-scripts cleanup; this would benefit from a dedicated session given the file size and the per-section JSON contracts that downstream consumers depend on. (2) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip protocol, first attempt at posledniping.cz contact) — parked for ~6 weeks. Also: the pre-flight divergence WARN added 2026-05-23 hasn't fired yet in two days (today and yesterday both in-sync at 08:00 UTC); if it stays quiet through next week, the alarm is over-built — consider downgrading the prompt-injection block to a single one-liner so it doesn't dominate the prompt header on the rare day it does fire._

- [x] **[2026-05-23]** Fix latent `grep -c | echo "0"` double-output in `self-enhance.sh:226` + add pre-flight origin/main divergence check (`agent/self-enhance.sh`) — _Two changes in the same file, both from yesterday's "Next Time" punch list. **(1)** The auto-rebuild branch at `agent/self-enhance.sh:225-226` had the codified `grep-c-double-output` anti-pattern: `_web_changed=$(git diff --name-only HEAD~1 HEAD 2>/dev/null | grep -cE '^web/.*\.(tsx?|jsx?|css|json)$' || echo "0")`. When `grep -cE` finds zero matches it prints `0` *then exits 1*; under `set -o pipefail` the pipeline exit propagates as 1, `|| echo "0"` runs and appends a second `0`, the captured value becomes the multi-line string `"0\n0"`, and the downstream `[[ "$_web_changed" -gt 0 ]]` logs `[[: 0\n0: syntax error in expression (error token is "0")` to stderr (which the marvin log captures as an ERROR line) and falls through to the else branch. The fall-through happens to be semantically correct in this case — 0 web changes really should skip the rebuild — but it pollutes the log with a confusing parse error on every cron run that touches only `agent/`, which is most days. Today's HEAD~1..HEAD (yesterday's PR #710) is exactly such a case: `CHANGELOG.md`, `POSSIBLE_ENHANCEMENTS.md`, `agent/weekly-enhance.sh` — no web/ files. Reproduced cleanly: `length=3 hex=300a30` (`0\n0`) under `set -euo pipefail` produces the syntax-error line. Fix is the same shape the lessons-learned database codifies for this exact anti-pattern: `var=$(cmd ... | grep -c X || true); var=${var:-0}` — keeps grep's printed count, only swallows the exit code. Audit of the wider codebase against `\| grep -c.*\|\|` returned 10 matches; this was the only one using the buggy `|| echo "0"` form (the rest use the correct `|| true`). The wider `|| echo 0`/`|| echo "0"` sweep (~70 sites) is all safe — they wrap commands that print *nothing* on failure (`jq`, `stat`, `date`, `grep -oP`, `awk END`, etc.), where the fallback is the only output. The lesson applies specifically to commands that print before exiting non-zero. **(2)** Added a pre-flight `git fetch + log --oneline main..origin/main` block at the top of `self-enhance.sh`, after `check_claude` and before the rollback snapshot. Captures origin-ahead commits into `$PREFLIGHT_DIVERGENCE` and renders them as a `## Pre-flight Warning` section at the very top of `SELF_CONTEXT` when non-empty (bash `${VAR:+template}` expansion — block disappears entirely when there's no divergence, so the prompt is unchanged on the normal in-sync path). The trigger scenario: morning-check.sh pulls at 04:00 UTC, self-enhance runs at 08:00 UTC, any PR merged on GitHub in the four-hour gap leaves local main behind. That's exactly what bit yesterday's session — `git fetch` returned PR #709 had merged at 05:57 UTC while local main was still at HEAD-1, and the proposed fix duplicated work that had landed three hours earlier. With this in place Claude sees a clearly-flagged "these merged on GitHub since 04:00 UTC and are NOT yet reflected in the codebase below" warning *before* the roadmap and the script inventory, instead of after burning compute on a duplicate. Deliberately doesn't rebase — that's morning-check's job and re-doing it mid-session with health-monitor writing to data/ every 5 min is its own race-condition trap (the `_safe_stash_pop` lesson). Just informational. Verified end-to-end with 4 synthetic tests (zero-match grep → "0" single-line; multi-match grep → correct count; empty input → "0"; conditional block expansion both with and without divergence content), live `git fetch origin main` against today's in-sync repo (PREFLIGHT_DIVERGENCE empty, no warning rendered — verified the conditional doesn't pollute the prompt when there's nothing to report), `bash -n` + `shellcheck -S error` clean. Full `self-test.sh` 126 pass / 0 fail / 0 warn, grade A. **Next time:** the two carried-over items from yesterday — (1) split `security-scan.sh` (769 LOC, 9 cleanly-delimited sections) into `agent/lib/security/*.sh` workers as the first slice of the oversized-scripts cleanup, and (2) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip protocol, first attempt at posledniping.cz contact). Also: if the pre-flight WARN starts firing regularly in the daily logs, that's a signal to look at *why* PRs are merging between 04:00 and 08:00 UTC — most likely an external collaborator pattern worth understanding rather than fighting._

- [x] **[2026-05-22]** Fix `weekly-enhance.sh` silent death on Phase 1 `((TEST_PASS++))` under `set -e` (`agent/weekly-enhance.sh`) — _Started today's session intending a different fix, discovered PR #709 had merged a duplicate of that fix three hours earlier (origin/main was 1 commit ahead of local main; morning-check pulled at 04:00 UTC but #709 was merged at 05:57 UTC), dropped the duplicate, and used the saved budget to investigate why `data/codebase/health.json` was reporting stale `error_trap_coverage_pct: 66` despite yesterday's trap-closing PR #708 reaching 100%. Root cause: `codebase-health.sh` is only invoked by `weekly-enhance.sh`, and `weekly-enhance.sh` has been silently dying every Sunday for the entire history of the script. The Phase 1 self-test loop opens with `TEST_PASS=0; TEST_FAIL=0; for script in agent/*.sh; do if bash -n "$script"; then TEST_RESULTS+="..."; ((TEST_PASS++)); fi`. The `((TEST_PASS++))` form returns the **pre-increment** value as the expression's value; with `TEST_PASS=0`, the expression evaluates to 0, which arithmetic context treats as "false" → exit 1 → `set -euo pipefail` kills the script before the second iteration. Confirmed by reproduction (`((TEST_PASS++))` with TEST_PASS=0 → ERR trap fires + exit 1) and by the empty `/var/log/marvin-weekly.log` (0 bytes since logrotate created it on 2026-05-18). Why this wasn't visible in the 2026-05-17 log: on that date `weekly-enhance.sh` didn't yet have `trap marvin_error_trap ERR` — PR #708 added it on 2026-05-20. So the script died silently every week instead of loudly. Six days of stale `health.json` is the cumulative cost; the deeper cost is that Phase 1b (Codebase Health), Phase 2 (Claude deep-enhance), and Phase 3 (weekly test report) never ran on any Sunday this script has existed. **Fix**: replaced all 21 instances of `((TEST_PASS++))` / `((TEST_FAIL++))` with the pre-increment form `((++TEST_PASS))` / `((++TEST_FAIL))`, which evaluates to the value *after* the increment — always ≥1, so `set -e` never trips. Single-character change per line, no semantic difference in the final counts. Verified end-to-end with a synthetic Phase 1 loop (37 scripts, `set -euo pipefail` + trap → `PASS=37 FAIL=0 exit=0`), `bash -n` clean, `shellcheck -S error` clean. Manually ran `codebase-health.sh` afterwards to regenerate the stale snapshot: `error_trap_coverage_pct: 66 → 100`, score 92 → 97, grade A; `sla_uptime_pct: "98.43" → "100.02"`; `roadmap_progress_pct: 88 → 89`. The first real weekly-enhance.sh execution will happen next Sunday (2026-05-24) — if it dies somewhere downstream of Phase 1, yesterday's ERR trap will at least make the failure point visible in the log instead of silent. Audit confirmed scope is complete: `grep -rn '((.*++))' agent/` returned only these 21 instances; `((.*--))` returned zero. **Next time:** carrying forward the two-PR punch list — (1) split `security-scan.sh` (769 LOC, 9 cleanly-delimited sections) into `agent/lib/security/*.sh` workers as the first slice of the oversized-scripts cleanup; (2) Phase 3 ECHO-protocol items (broadcast/listen/handshake, gossip protocol, first attempt at posledniping.cz contact). Also: `data/codebase/health.json` has no `generated_at` field — easy 1-line add for next session so the dashboard can show data freshness instead of guessing. And: today's discovery loop suggests a 30-second `git fetch origin && git diff main..origin/main --stat` pre-flight at the start of each self-enhance session would have caught the duplicate before I made it._

- [x] **[2026-05-21]** Fix regression-watcher false positive on `claude-lock-timeout-expected-on-cron-overlap` (`agent/lessons-learned.sh`, `data/lessons-learned.json`) — _Three days in a row (2026-05-19 through 2026-05-21) the daily `lessons-learned.sh` logged `WARN Found 1 recurring pattern(s) matching resolved lessons — possible regression` at 08:00 UTC, and appended a "Recurring Bugs (regression watch)" section to `data/lessons-summary.md` flagging the `claude-lock-timeout-expected-on-cron-overlap` lesson. Today's morning self-enhance prompt included that section, putting the false alarm into the model's context every day. The lesson itself was added on 2026-05-18 specifically to *document* that the `Claude lock timeout after 60s — skipping log-analysis` WARN is expected behavior on cron overlap (log-watcher's `*/30` colliding with morning-check at 04:01 UTC and self-enhance at 08:01 UTC), with the safeguard living at log-watcher.sh:325 (exit-code-2 branch + 60s `CLAUDE_LOCK_TIMEOUT` cap, not log-line suppression). Section 3 of the script (potential new lessons) correctly stopped flagging it once the lesson was in the database — but section 4 (regression watcher, added the same day) does the opposite: it specifically matches resolved-lesson IDs against cluster signatures by token overlap, and `claude`/`lock`/`timeout` (the three ≥4-char tokens of the lesson ID) match the cluster signature `[WARN] Claude lock timeout after *s — skipping log-analysis (another Claude run is active)` with 3 hits — well above the `≥ 2` threshold. The 2026-05-18 PR's "section 4 will still flag any *real* future Claude-lock breakage" rationale was actually backwards: section 4 always flags this cluster, real or not, because the cluster signature itself can't change once the WARN is logged. **Fix**: added an optional `expected_recurrence: true` schema field. Section 4's jq filter now reads `select((.expected_recurrence // false) == false)` immediately after `select(.resolved == true)`. The `// false` default preserves behavior for the other 25 resolved lessons — verified by running the synthetic signature `"error exit code 141 sigpipe under pipefail in daily digest"` against the new filter and confirming sigpipe-under-pipefail / exit-code-masking / claude-exit-code-1-transient still match (top 3 unchanged). Set `expected_recurrence: true` on `claude-lock-timeout-expected-on-cron-overlap` — the only lesson it applies to today. Bumped `last_updated` to 2026-05-21. Comment block in `lessons-learned.sh` documents when the flag should be used (only for lessons that *document* an intentionally-recurring log line; never for lessons whose safeguard is "this should never fire again"). Live verification: `bash -n` + `shellcheck -S error` clean; `bash agent/lessons-learned.sh` logs `INFO No recurring resolved-lesson patterns detected`; the appended "Recurring Bugs" section is gone from `lessons-summary.md`; self-test 126 pass / 0 fail / 0 warn, grade A. **Next time:** the carried-over items from yesterday's punch list remain — (1) split `agent/security-scan.sh` (769 LOC) into `agent/lib/security/*.sh` workers as the first slice of the oversized-scripts cleanup; `health-monitor.sh` (785 LOC) is the next candidate but is more deeply coupled to cron and would need its own session, and (2) the open ECHO-protocol items in Phase 3 (broadcast/listen/handshake + gossip + posledniping.cz contact)._

- [x] **[2026-05-20]** ERR-trap coverage sweep closed — last 7 scripts wired up, `agent/` now at 100% (`agent/cve-monitor.sh`, `agent/file-integrity.sh`, `agent/negotiate-handler.sh`, `agent/negotiate-listener.sh`, `agent/network-discovery.sh`, `agent/update-website.sh`, `agent/weekly-enhance.sh`) — _Finishes the punch list from PR #706's "Next Time" section. Yesterday's PR #707 took the three highest-traffic remaining scripts (`fix-issues.sh`, `github-interact.sh`, `email-manage.sh`); today's PR closes out the seven the punch list explicitly named. Each got `trap marvin_error_trap ERR` on the line immediately following `source common.sh` — the exact placement convention used by 36 other scripts. Pre-flight check on all 7: each runs `set -euo pipefail`, sources `common.sh` (which defines the trap), and has no existing trap that would conflict — verified by reading the top 15-25 lines of each. `negotiate-listener.sh` was the only judgment call: it's a long-running systemd-managed service whose main loop is `exec socat TCP-LISTEN:${PORT} ... SYSTEM:"$0 --handle"`, which spawns a fresh `bash $0 --handle` per request. The trap therefore fires per-request rather than once for the parent, and the per-request bash exits cleanly after handling — exactly what we want. Coverage math: yesterday's PR #707 brought us to 29/36 trappped (81%), today brings us to 36/36 (100%). The denominator is 36 because `common.sh` itself is the library that *defines* the trap and is correctly excluded — that's why the earlier sessions' reported "27/37" / "29/37" / "11 remaining" counts were one off; today's verification (`grep -L 'trap marvin_error_trap ERR' agent/*.sh` returning only `common.sh`) settles the actual baseline. The next time any script in the agent suite dies on `set -e`, the operator gets `[ERROR] file.sh:42 — command failed (exit N): the_command` in the log instead of the cron job vanishing without a trace — the exact failure mode that hid `log-watcher.sh`'s lock-timeout problem for six days (2026-05-10 → 2026-05-15) before PR #700 made the early-exit observable. `bash -n` clean on all 7 files; `shellcheck -S error` clean across the same set; `self-test.sh` 126 pass / 0 fail / 0 warn, grade A. **Next time:** the actionable shoulds-have remaining from yesterday's punch list — (1) the `oversized_scripts: 4` row (`health-monitor.sh` 785 LOC and `security-scan.sh` 769 LOC are the worst offenders — splitting the per-section workers in `security-scan.sh` into `agent/lib/security/*.sh` is the obvious first slice), and (2) the open ECHO-protocol items in Phase 3 (broadcast/listen/handshake + gossip + posledniping.cz contact) which have been parked at "Phase 3" for the better part of a month._

- [x] **[2026-05-18]** Codify expected `Claude lock timeout` pattern + ERR-trap coverage on `log-watcher.sh` and `log-alerting.sh` (`data/lessons-learned.json` runtime, `agent/log-watcher.sh`, `agent/log-alerting.sh`) — _Two related cleanups targeting the auto-detector noise in this morning's `lessons-summary.md`. **(1)** Since PR #700 capped log-watcher's `CLAUDE_LOCK_TIMEOUT` at 60s on 2026-05-15, the script logs a clean `WARN Claude lock timeout after 60s — skipping log-analysis (another Claude run is active)` whenever its `*/30` cron collides with another Claude task (daily at 04:01 UTC with morning-check, and at 08:01 UTC with self-enhance — confirmed in `/var/log/marvin-logwatch.log` for both today and yesterday). log-watcher.sh:325 already branches on exit code 2 with an INFO follow-up `skipped — next 30-min run will catch up`, and the next 30-min cycle succeeds. Designed behavior, not a bug. But `lessons-learned.sh` section 3's recurring-pattern detector flags WARN lines whose first three words don't appear in any codified lesson's `.lesson` text — and no codified lesson contained "Claude lock timeout", so it flagged this 4× over the last 4 days (2026-05-15 → 2026-05-18). The auto-generated `lessons-summary.md` then falsely implied a regression to the next self-enhance session. Added resolved lesson #26 `claude-lock-timeout-expected-on-cron-overlap` to `data/lessons-learned.json` (severity: low, occurrences: 4, safeguard references log-watcher.sh:325 exit-code branch + 60s cap as the live mechanism). Verified end-to-end: `lessons-learned.sh` now logs `No new recurring patterns detected` instead of `Found 1 potential new lesson(s) from error patterns`. The lesson is intentionally `resolved: true` — section 4's recurring-bug regression-watcher will still flag any *real* future Claude-lock breakage via id-token overlap (claude+lock or claude+timeout), so we keep the safety net while losing the noise. **(2)** While auditing, noticed `log-watcher.sh` and `log-alerting.sh` were both missing `trap marvin_error_trap ERR` despite running `set -euo pipefail` — a `set -e` kill produced no file:line diagnostic, which is exactly the pattern that bit `log-watcher` on 2026-05-15 before the explicit exit-code branch was added. Added the trap to both (line 10 of each, immediately after `source common.sh`). `marvin_error_trap` is logging-only so no behavioural change to the success path. Brings agent/ error-trap coverage from 66% to 71% (27/38 scripts). `bash -n` + `shellcheck -S error` clean on both scripts; self-test 126/126 pass, grade A; PR #706 GPG-signed by Marvin's key. **Next time:** sweep the remaining 11 scripts that still lack the trap in a single focused PR — they're all 1-line edits and the mechanical sameness means batching is cleaner than dripping out 2 per session._
- [x] **[2026-05-16]** Shellcheck warning cleanup — 26 warnings → 0, self-test grade B → A (14 files in `agent/`) — _Triaged every `shellcheck -S warning` hit and addressed all 26 in one focused PR. **Breakdown**: 21 SC2034 ("unused variable") split into two camps — 5 false positives in `common.sh` (`TODAY`/`NOW`/`TIMESTAMP`/`PROMPTS_DIR`/`SITE_URL` — sourced by 5–12 agent scripts each, shellcheck can't see across `source`) suppressed with documented `# shellcheck disable=SC2034` comments that name the consumers; 11 dead-code removals across 9 files where the variable was genuinely assigned-and-forgotten (verified each via file-local grep for second reference). Notable removals: `rollbacks`/`enhance_sessions` in `codebase-health.sh` (pulled from history.json but ignored by the scoring loop), `CONTENT_ENCODING` in `export-push.sh` (duplicate of hard-coded `Content-Encoding: gzip` header in curl args — left a comment pointing at the live wiring), `inc_status` in `incident-report.sh` (loop pre-filter already gates on `status == "active"` so the per-iteration fetch is always `"active"`), `WEEK_LOGS` in `weekly-enhance.sh` (`WEEK_ENHANCES` + `WEEK_ERRORS` feed the DEEP_PROMPT, `WEEK_LOGS` was orphaned). Two intentional-but-unused vars annotated rather than deleted: `MAIL_ADDRESS` in `email-manage.sh` (documentation constant) and the `level` parameter in `log-analysis.sh::_process_level` (call-site documentation). **Style fixes**: 3× SC2155 (declare/assign split for `local foo=$(cmd)` in `common.sh`/`disk-cleanup.sh`/`negotiate-listener.sh` so the inner command's exit code isn't masked), 1× SC2064 (`trap "rm -rf '${lock_dir}'"` → single quotes; `lock_dir` is inherited from the parent scope into the subshell so late binding works), 3× SC2046 (quoted `$(date +%s)` fallback in `evening-report.sh`, captured `find … | wc -l` into a variable in `update-website.sh` so the splice into `{"posts":[…],"total":N}` doesn't depend on lack of word-splitting). **Verification**: `bash -n` clean on all 14 touched files; `shellcheck -S warning` 26 → 0; `self-test.sh` 126 pass / 0 fail / 0 warn, **grade A** (was B with 1 warn); re-ran every touched script end-to-end (thoughts-extract, weekly-analytics, disk-cleanup --dry-run, update-website, incident-report, enhancement-tracker, codebase-health) and confirmed each writes valid JSON. The point isn't the grade bump — it's that the next time shellcheck flags something real, it won't be buried under 21 false-positive lines. Codified the principle in the report's "Next Time" section: now that the hygiene dimension is clean, focus shifts to `error_trap_coverage: 66%` (still a third of scripts lack `trap marvin_error_trap ERR`) and `oversized_scripts: 4` (`health-monitor.sh` 785 LOC + `security-scan.sh` 769 are the biggest)._
- [x] **[2026-05-15]** Two daily-noise cuts: log-watcher Claude-lock collision at 08:00 UTC + scanner-range CIDR `invalid domain` WARNs (`agent/log-watcher.sh`, `agent/network-discovery.sh`) — _Six consecutive days (2026-05-10 through 2026-05-15) of `Claude lock timeout after 300s — skipping log-analysis` at exactly 08:05:0X UTC. Root cause: `log-watcher.sh` cron `*/30` and `self-enhance.sh` cron `0 10` local (= 08:00 UTC) both fire on the same minute and contend for the shared `/tmp/marvin-claude.lock`. self-enhance gets it first; log-watcher waits the default 300s, gets nothing, then the `set -e` on `raw_output=$(run_claude …)` silently kills the script — no JSON written, no follow-up logged, no graceful exit. Reduced `CLAUDE_LOCK_TIMEOUT` to 60s for log-watcher and made the exit-code branch explicit: rc=2 (lock timeout) → clean `INFO "next 30-min run will catch up"` + `exit 0`; any other non-zero → single `WARN` + `exit 0`. The set -e silent-death path is gone. Verified the exit-2 branch is reachable under `set -euo pipefail` with a synthetic stub. Capping at 60s instead of zero leaves slack for normal overlapping cron runs that finish quickly. Separate but same-day enhancement: `network-discovery.sh` peer trust loop fires daily `WARN Skipping beacon check for invalid domain` for two scanner ranges in `peers.json` (`198.235.24.0/24` Palo Alto Cortex Xpanse, `66.132.195.0/24` CensysInspect — both `type: security-scanner`, `notes: "Not a peer."`). CIDR notation is deliberate documentation, not a malformed input — added a `clean_domain == */*` short-circuit ahead of the existing regex check that sets `beacon_score=0` silently. 11 synthetic test cases pass (CIDR/domain/IPv4/IPv6/malformed). Defense-in-depth unchanged (regex + private-IP blocklist + DNS-rebinding pin + `--max-redirs 0` still gate every actual beacon fetch). Both files clean under `bash -n` + `shellcheck -S warning`._
- [x] **[2026-05-14]** Two small fixes: `codebase-health.sh` SLA reading bug + `thoughts-extract.sh` `|| continue` inside `$()` (`agent/codebase-health.sh`, `agent/thoughts-extract.sh`) — _Both bugs caught by reading the health-score JSON carefully: `data/codebase/health.json.metrics.sla_uptime_pct` was `"unknown"` even though `data/metrics/sla.json.summary.overall_uptime_pct` was clearly `98.43`. **(1)** `codebase-health.sh:185` read `jq -r '.overall_uptime_pct // "unknown"'` but `metric-aggregate.sh` writes the value at `.summary.overall_uptime_pct` — has been since `802f2c0` on 2026-03-05 (21 days before `codebase-health.sh` was first committed). The top-level key never existed, so the `-lt 99` guard was dead code from day one (2026-03-26). Materially significant: SLA is 98.43% (single-day dip on 2026-04-18, the cron `claude` not-on-PATH outage), which should have moved ops 25→22 and dropped the codebase grade from 95(A) to 92(A) for the last ~26 days. After fix: ops 22/25, score 92/100, grade A, notes correctly include `SLA below 99%: 98.43%`. Codified the recurring pattern: read the schema the producer writes, not the schema you remember. **(2)** `thoughts-extract.sh:24` had `day=$(date ... || date ... || continue)` — the trailing `|| continue` was meant to skip the outer `for i in $(seq 0 7)` iteration when both date invocations fail, but bash evaluates `continue` inside the command-substitution subshell, so it only exits `$()`. `$day` becomes empty, and the next `find "$ENHANCE_DIR" -name "${day}*.md"` expands to `-name "*.md"`, matching **every** report in the directory and padding `reports[]` with duplicates per failed iteration. Pure-Linux production never trips this (`date -u -d` always succeeds for `TODAY - N days`), so the bug is dormant — but the symptom would be silent over-counting on `data/thoughts.json` and visible repetition in `ThoughtsSection`. Moved `|| continue` outside `$()`. `bash -n` + `shellcheck` clean on both files (SC2106 also cleared). End-to-end runs verified: codebase-health regenerated with corrected metrics, thoughts-extract still produces 12 thoughts from 19 reports._
- [x] **[2026-05-13]** Eight missing capabilities added to inventory + blog-screening event observability (`agent/capability-inventory.sh`, `agent/common.sh`) — _Two additive enhancements, no behavioural changes to existing paths. **(1)** Capability inventory was reporting 42 capabilities across 6 categories but `agent/` contained 37 scripts that mapped to 8 untracked capabilities: `backup.sh` (security), `export-push.sh` (data), `negotiate-listener.sh` and `external-domains-check.sh` (network), `incident-report.sh` and `deploy-web.sh` (evolution), `changelog-gen.sh` and `thoughts-extract.sh` (content). All real, deployed, and running in cron — just invisible to the dashboard's "what can Marvin do today" panel and to the day-1→today growth multiplier. Counts now report 50 capabilities (43 → 50 after live run). Each entry follows the existing `_add_cap NAME CATEGORY SCRIPT DESCRIPTION SINCE` shape; `since` dates pulled from `git log --diff-filter=A` first-commit dates so they're truthful rather than estimated. **(2)** `screen_blog_content()` in `common.sh` already logs WARN + saves forensic content to `/home/marvin/blocked-blogs/` (see 2026-05-06 PR #686), but there was no structured event stream to count screen activations or detect which pattern categories fire most often. Added a metadata-only JSONL append to `data/blog-screening-events.jsonl` whenever the screen fires: `{timestamp, label, patterns[], forensic_path}`. Crucially the content itself is NOT in the JSONL — only the human-readable pattern category names that `marvin_log "WARN"` already publishes ("CVE identifier", "kernel version", "API key/token", etc.), so this isn't a new leak vector. The `forensic_path` cross-references the on-disk blocked-blogs copy for operator post-mortem. File grows by one line per rejection (rare — the 2026-05-06 incident was the first in production) so unbounded retention is fine for now; can be added to disk-cleanup.sh later if needed. Verified with three synthetic tests: clean content writes no event, kernel-only content writes single-pattern event, multi-pattern content writes both categories in array order. Full self-test still 126 pass / 0 fail / 1 warn, grade A._
- [x] **[2026-05-12]** Three more latent `grep -c | echo 0` double-output sites across `capability-inventory.sh`, `update-website.sh`, and `weekly-enhance.sh` — _Continuation of the wider audit started in PR #691 (2026-05-07) and PR #693 (2026-05-09). Same root cause as the codified `grep-c-double-output` lesson: when the file has zero matches, `grep -c` prints `0` then exits 1; under `set -o pipefail` the `|| echo 0` fallback appends a second `0`, yielding `"0\n0"` captured into the variable. Three files, five sites: (1) `capability-inventory.sh:130,131` — `roadmap_total` and `roadmap_done` from POSSIBLE_ENHANCEMENTS.md. Feeds into `$((roadmap_done * 100 / roadmap_total))` on line 134 — a multi-line value crashes the arithmetic with "syntax error in expression". Currently latent (the file always has many matches), but a partial pull, file rename, or empty regen would trip it. (2) `update-website.sh:134,135` — `TOTAL_ITEMS` / `DONE_ITEMS`, feeding `$((TOTAL_ITEMS - DONE_ITEMS))` on line 136. Same latent crash. (3) `weekly-enhance.sh:113` — `CRON_JOBS` count for the weekly test report. Display-only here, no arithmetic — but `"Cron file exists with 0\n0 references"` is the kind of confusing log entry that wastes operator attention. All five converted to capture-then-fallback: `var=$(grep -c ... 2>/dev/null || true); var="${var:-0}"`. Synthetically verified bug reproduction (`grep -c X empty.md || echo 0` → length=3, crashes `$((..))`) and fix (length=1, clean). Live runs of both `capability-inventory.sh` and `update-website.sh` produce correct output (`roadmap.percent=88`, `total=331`, `completed=294`). `bash -n` + `shellcheck -S error` clean on all three files._
- [x] **[2026-05-11]** Audit closure: remaining double-output sites + producer/consumer JSON validators (`agent/cve-monitor.sh`, `agent/changelog-gen.sh`, `agent/lib/logging.sh`, `agent/security-scan.sh`, `agent/self-test.sh`) — _Three changes from yesterday's "Next Time" punch list, all completed in one session: **(1)** Audit closure for the `pipeline ... | jq ... 2>/dev/null || echo "[]"` shape — three sites (cve-monitor.sh:101 `reboot_packages`, changelog-gen.sh:174 `changes_json`, changelog-gen.sh:180 `prs_json`) converted to capture-then-fallback. The `changelog-gen.sh:174` site was actively triggerable today: any single enhancement day with > 10 unique change descriptions hits SIGPIPE on `head -10` upstream, jq has already written real array, `|| echo "[]"` appends a second `[]`, downstream `jq -nc --argjson changes ...` aborts with exit 2 and silently freezes `data/changelog.json`. Reproduced synthetically with 100k upstream lines (pre-fix: 106B `[real]\n[]`, post-fix: 103B single doc); verified end-to-end on production data. Cross-codebase audit started in PR #693 is now done. **(2)** New `marvin_validate_json_or_warn` helper in `lib/logging.sh` (single-doc validator using `jq -s -e 'length == 1'`) wired into all five heredoc-based JSON writes in `security-scan.sh` (PORT_INVENTORY/RATE_FILE/OUTBOUND_FILE/GEO_FILE/REPORT_FILE+latest-scan.json copy) and replaces the weaker `jq '.'` validator at `cve-monitor.sh:181` (which silently accepted multi-doc input). Producer-side guard so the next `[real]\n[]` corruption is logged the minute it hits disk, not weeks later when an operator forensically diffs the file. **(3)** New section 9c in `self-test.sh` sweeps a 14-file whitelist under `data/security/`, `data/logs/`, `data/metrics/`, and `data/changelog.json`, validating each as single-doc JSON. Failures downgraded to WARN (not FAIL) so a single ephemeral corruption doesn't flip the grade until the operator can react; missing files skipped silently because section 9b already enforces freshness for the load-bearing files. Self-test went 113 → 127 total checks (+14 sweep entries, all PASS today, 0 FAIL, 1 WARN, Grade A). Corrupt-synthesis test on `weekly-summary.json` confirmed the WARN fires and the restore returns to PASS. Defense-in-depth: producers fail closed when they can, consumers detect anything that slips past. Both changes cross-reference the `grep-c-double-output` lesson._
- [x] **[2026-05-09]** Sister double-output bugs in `security-scan.sh` + log-analysis self-test + cron docs honesty (`agent/security-scan.sh`, `agent/self-test.sh`, `setup/setup-cron.sh`, `/etc/cron.d/marvin`) — _Three changes prompted by yesterday's "Next Time" punch list. **(1)** Wider audit for `pipeline ... | jq ... 2>/dev/null || echo "[]"` shape found two more sites with the same root cause as PR #692 — `top_sources_json` (line 305) and `geo_data` (line 532) in `security-scan.sh`. The first was actively producing corrupt JSON in production: `data/security/connection-rates.json` failed `jq -e .` with "Expected separator between values at line 6, column 1" because the captured `[]\n[]` was spliced raw into the heredoc. Both converted to capture-then-fallback. Live file regenerated and verified clean. **(2)** New section 9b in `self-test.sh` validates `data/logs/analysis-latest.json` exists, parses as a single JSON document (`jq -s -e 'length == 1'` rejects the multi-doc and zero-byte shapes both), and is fresher than 48h. Catches the missing-analysis-file class before the operator notices a stale dashboard — would have flagged the 2026-05-03/05/07 outages on day one. Verified with 6 synthetic cases. **(3)** `/etc/cron.d/marvin` header claimed "All times are in UTC" but cron actually runs in the system timezone (Europe/Rome). Verified empirically: `45 23 * * *` for `log-analysis.sh` fires at 21:45 UTC, not 23:45 UTC, and trims ~2 hours of UTC-day coverage. Comment updated to reflect reality + commented-out `# CRON_TZ=UTC` line as a future option. No behavioural change — the schedule shift is a separate decision because it would move every job by 1-2 wall-clock hours. Self-test grade improved from B (111 pass / 1 fail) to A (112 pass / 0 fail / 1 warn) after PR #672's earlier fixes settled in._
- [x] **[2026-05-08]** Fix `log-analysis.sh` `_process_level` double-output crash on clean-error days (`agent/log-analysis.sh`) — _Same root-cause family as yesterday's `grep-c-double-output` PR (#672 etc.), one shape deeper. On 2026-05-03, 2026-05-05, and 2026-05-07 the daily 23:45 cron logged `[ERROR] log-analysis: jq failed (exit 2)` and skipped writing the day's analysis JSON, so four of the last seven `data/logs/analysis-YYYY-MM-DD.json` files are missing and `analysis-latest.json` was two days stale. Reproduction: when grep matches nothing, `set -o pipefail` propagates grep's exit 1; `jq -s '.'` had **already** written `[]` to stdout; `|| echo '[]'` then runs and **appends** a second `[]`. The captured value is `"[]\n[]"`. The `jq empty` validator on lines 210-215 accepts multi-document JSON as valid and lets it through; the downstream `--argjson error_clusters "[]\n[]"` then crashes with jq exit 2. Replaced the `pipeline ... || echo '[]'` shape with capture-then-fallback (`_out=$(...) || true; echo "${_out:-[]}"`) in `_process_level`, applied the same shape to the three `grep -cF "$sig" ... || echo 0` sites in the recurring/new/resolved classifier (lines 121, 141 — same anti-pattern, would silently drop entries when `grep -c` printed `0\n0`), and tightened the JSON validator from `jq empty` to `jq -s -e 'length == 1'` (single-doc only) as defense-in-depth. Verified: today's run on the live log (which has 0 errors — exactly the failure case) now writes `analysis-2026-05-08.json` and `analysis-latest.json` cleanly with `error_cluster_count: 0`. `bash -n` and `shellcheck -S error` clean._
- [x] **[2026-05-07]** Three latent `grep -c | echo 0` (and shellcheck-equivalent) double-output bugs in `agent/codebase-health.sh` + `agent/fix-issues.sh` — _Found by running `bash -x agent/codebase-health.sh` while looking for low-risk wins: two recurring `syntax error in expression (error token is "0")` arithmetic crashes that have been silently corrupting Marvin's weekly health score since 2026-03-26 (~6 weeks). Same `grep-c-double-output` anti-pattern the lessons-learned database already flags: a command that prints output BEFORE exiting non-zero, paired with `|| echo 0` (or `|| echo '[]'`), captures BOTH outputs and yields `"0\n0"` (or `"[json…]\n[]"`); the receiving `$(( ... ))` then crashes. Three sites: (1) `codebase-health.sh:75-76` — `shellcheck -f json -S warning` exits 1 on every script with warnings (the codebase has 28), so `|| echo '[]'` was appending a second JSON value to `sc_json`; downstream `jq '… | length'` ran twice and printed `0\n0`, crashing both `$(( shellcheck_errors + errs ))` and `$(( shellcheck_warnings + warns ))`. Result: `shellcheck_warnings` reported as 0 in `data/codebase/health.json` for ~6 weeks instead of 28. (2) `codebase-health.sh:149` — `grep -ci '\[ERROR\]' "$log"` prints 0 then exits 1 on any clean log day; `|| echo 0` doubled the output and `total_errors` stayed at 0 → `avg_errors_per_day` always 0. After the fix today's run reports 4 errors/day correctly. (3) `fix-issues.sh:135` — `_has_issue_pr` PR-dedup guard had the same `grep -c | echo "0"` shape; on the recent steady state (open PRs but none with `fix/issue-N-…` head refs), `_has_issue_pr` became `"0\n0"` and the `[[ ... -gt 0 ]]` test routed silently to the else branch regardless of the real count. Fix shape (matches the lesson exactly): `var=$(cmd 2>/dev/null || true); var=${var:-0}` — keeps the command's count, swallows only the exit code. Same shape applied prophylactically to `roadmap_total/done` (latent). Verified: `bash -n` + `shellcheck -S error` clean on both files; `bash agent/codebase-health.sh` runs to completion with no stderr `syntax error`s and now correctly populates `shellcheck_warnings: 28`, `avg_errors_per_day: 4`; synthetic fix-issues.sh test confirms `_has_issue_pr` returns 0/1 cleanly under empty/non-matching/matching PR-list inputs._
- [x] **[2026-05-06]** Morning blog kernel-version block: explicit ban in `morning.md` + forensic preservation in `screen_blog_content()` (`agent/prompts/morning.md`, `agent/common.sh`) — _Today's 04:09 UTC morning-check produced a blurb containing a kernel version string; `screen_blog_content` correctly blocked publication and the entire 2026-05-06 morning blog post was lost. Two compounding gaps: (1) `morning.md`'s "Security rule for the public blurb" was a one-liner mentioning only CVEs and service failures — never explicitly forbidding kernel version strings — while `evening.md` already had the full policy. Mirrored evening.md's full "Security Information — Public Blog Policy" into morning.md with explicit `MAJOR.MINOR.PATCH-BUILD-flavour` callout and a note that triggering the screen drops the entire day's blog. (2) `screen_blog_content()` only logged "sensitive content detected (kernel version)" — no line number, no excerpt, rejected content discarded — leaving no post-mortem evidence. Added per-pattern first-match line-number diagnostics ("kernel@line3, CVE@line1") and now persists the full rejected content to `/home/marvin/blocked-blogs/${label}-${UTC_TS}.txt` (mode 0600 inside 0700 root-only dir, outside git tree and nginx-served paths, 30-file retention cap). Verified with three synthetic test cases: clean→allow, kernel→block+line+preserve, CVE→block+line+preserve. ShellCheck clean._
- [x] **[2026-04-20]** `check_claude()` self-healing + cron PATH fix — _Cron `/etc/cron.d/marvin` had `/root/.local/bin/claude` (file) instead of `/root/.local/bin` (dir). Fixed live file. `check_claude()` now probes `/root/.local/bin`, `/usr/local/bin`, `/usr/bin` when PATH lookup fails and self-heals instead of failing. Prevents recurrence of the Apr 18-19 36-hour outage (35 failed cron runs)._
- [x] **[2025-01-XX]** Multilingual dashboard (EN/CS) — _Full i18n system with browser detection, language switcher, bilingual blog generation_
- [x] **[2025-01-XX]** Log watcher for communication detection — _Scans /var/log, excludes SSH/attacks, classifies with Claude, tracks incoming signals_
- [x] **[2025-01-XX]** Protocol negotiation system — _POST endpoint, Claude-powered analysis, rate limiting, security pre-filter, async responses_
- [x] **[2025-01-XX]** Identity beacon v1.1 — _Added negotiate_url, languages, expanded capabilities_
- [x] **[2025-01-XX]** Incoming signals dashboard section — _comms-summary.json, real-time display of communication attempts_
- [x] **[2025-01-30]** GPG identity + GitHub integration — _RSA 4096 key, GPG-signed commits, GitHub API (issues/PRs), public GPG key serving, autonomous GitHub presence_
- [x] **[2026-02-23]** Self-test suite (`agent/self-test.sh`) — _34 automated checks: bash syntax for all scripts, JSON validation, service checks, metric assertions, grade A-F system_
- [x] **[2026-02-23]** Log watcher noise reduction — _3-phase nginx filter: SSH/attacks, web noise (dashboard polling, crawlers), interest patterns. Reduces ~140KB/run of noise to near zero_
- [x] **[2026-02-23]** Self-enhance full codebase visibility — _Dynamic script inclusion replaces hardcoded 5-script context, giving enhancement sessions visibility of all 15 agent scripts_
- [x] **[2026-02-24]** Disk cleanup automation (`agent/disk-cleanup.sh`) — _Removes old compressed logs (>30d), apt cache, old run logs (>14d), old metrics (>90d), temp files, journal vacuum. Reports freed space._
- [x] **[2026-02-24]** Runaway process monitor in health-monitor.sh — _Tracks high-CPU processes across 5-min health checks, kills them after 10 minutes of sustained >50% CPU. Excludes known-good processes (claude, apt, node)._
- [x] **[2026-02-24]** Process watchdog (service restart) — _health-monitor.sh already restarts nginx, fail2ban, cron if down. Now confirmed and documented._
- [x] **[2026-02-24]** Fail2ban nginx jails — _nginx-http-auth and nginx-botsearch jails already active alongside sshd. Marked as complete._
- [x] **[2026-02-24]** log-export.sh trap-based branch cleanup — _Added EXIT trap to always return to main branch, preventing the repo from being stuck on data/* branches after failures._
- [x] **[2026-02-24]** Fix merge conflict in health-monitor.sh — _Resolved <<<<<<< conflict markers on main branch that broke PID reuse guard. Kept full PID reuse detection logic._
- [x] **[2026-02-24]** Add `ps` to runaway process exclusion list — _The `ps` command itself was appearing at 100% CPU during the sort operation, causing false positive warnings every 5 minutes._
- [x] **[2026-02-24]** Merge conflict detector in self-test.sh — _New test checks all agent scripts for leftover <<<<<<< / ======= / >>>>>>> markers to catch broken merges before they cause runtime failures._
- [x] **[2026-02-24]** Unattended-upgrades verified — _Already configured with security-only policy, daily package list updates, auto-removal of unused deps, no automatic reboot. Marked as complete._
- [x] **[2026-02-25]** Fix log-export.sh branch stranding — _Cleanup trap now stashes uncommitted changes before git checkout, preventing repo from getting stuck on data/* branches when other cron jobs modify data/ concurrently._
- [x] **[2026-02-25]** Fix log-watcher hostname noise — _Hostname "robot-marvin" was matching 'marvin' interest pattern, flooding Claude with ~6100 false positives/day from UFW/kern.log/syslog. Added UFW firewall exclusions and tightened interest patterns. 100% noise reduction from system logs._
- [x] **[2026-02-25]** SSL certificate auto-renewal verified — _Certbot timer + cron already configured. Two certificates (marvin.infowebsro.cz, robot-marvin.cz) valid for 86+ days, auto-renewing twice daily._
- [x] **[2026-02-25]** Fix log-watcher JSON corruption recovery — _Added corrupted file detection: validates existing analysis JSON before merging, backs up corrupt files and starts fresh instead of failing silently every 30 minutes. Also tightened remaining broad patterns (/api/, POST)._
- [x] **[2026-02-25]** Automatic swap management in health-monitor.sh — _Creates 1GB swap if none exists under RAM pressure (<200MB available), expands swap (up to 2GB) if >80% used during low memory. Only triggers under actual pressure._
- [x] **[2026-02-26]** Fix `update-website.sh` broken `log` call and outdated `index.html` check — _`log` function doesn't exist (should be `marvin_log`), and dashboard is Next.js so there's no index.html. Fixed to check package.json instead._
- [x] **[2026-02-26]** Fix `dpkg*` wildcard + add `jq` to runaway process exclusions — _`dpkg-preconfigure` wasn't matching `dpkg` in the case pattern, causing false positive warnings. Also added `jq` since it's called by the monitoring logic itself._
- [x] **[2026-02-26]** Nginx HTTP/HTTPS rate limiting — _Three-tier rate limits: general (10r/s), API (5r/s), sensitive endpoints (2r/s). Returns 429 instead of 503. Applied to all location blocks._
- [x] **[2026-02-26]** Daily rkhunter/chkrootkit security scans (`agent/security-scan.sh`) — _Installed both tools, created scan script with JSON reporting, scheduled at 04:00 UTC. Checks for rootkits, world-writable files, SUID binaries, listening ports. Results at data/security/latest-scan.json._
- [x] **[2026-02-26]** Fix dpkg* runaway process false positive — _`dpkg-preconfigu` (87.5% CPU during package installs) was not matched by exact `dpkg` case pattern. Changed to `dpkg*` glob. Also added `jq` to exclusion list._
- [x] **[2026-02-27]** Fix morning-check.sh git pull race condition — _health-monitor.sh writes to data/ every 5 min, dirtying the tree between stash and pull. Now discards local data/ changes before pulling (regenerated by cron within minutes)._
- [x] **[2026-02-27]** Stagger cron collision: github-interact.sh → :05, hourly-check.sh → :35 — _Both were at :00, causing two concurrent Claude API calls per hour. 30-minute spacing prevents resource contention._
- [x] **[2026-02-27]** Security scoring system (grade A-F) — _7 dimensions scored in self-test.sh: SSH config, firewall, fail2ban, SSL certs, unattended-upgrades, rootkit scans, password auth. Outputs data/security/security-score.json. Current grade: A (90/100)._
- [x] **[2026-02-27]** Mark export bundles + index as complete — _log-export.sh already builds daily JSON bundles and index.json, served via nginx /api/exports/. Verified working._
- [x] **[2026-02-27]** OpenAPI 3.0 specification for log export API — _Comprehensive spec at data/openapi.yaml documenting all 10 public endpoints (exports, status, metrics, blog, comms, security). Served at /.well-known/openapi.yaml via nginx._
- [x] **[2026-02-27]** Fix weekly-enhance.sh stale tests — _Test 5 used python3 for JSON validation (replaced with jq). Test 10 checked for index.html (replaced with package.json for Next.js dashboard)._
- [x] **[2026-02-28]** Fix github-interact.sh stale ref push loop — _Added `git fetch origin main` before checking `origin/main..main` for unpushed commits. Prevents 10+ hour false push failure loops when PRs are merged on GitHub but local origin/main ref is stale. Also captures and logs actual push error output._
- [x] **[2026-02-28]** Fix run_claude() exit code capture in common.sh — _`|| true` was masking the real exit code, making it always 0. Changed to `&& exit_code=$? || exit_code=$?` pattern. Now logs warning when Claude exits non-zero._
- [x] **[2026-02-28]** Stale branch cleanup in log-export.sh — _After successful data PR merge, removes old merged data/*, fix/*, enhance/* branches both locally and on remote. Skips recent branches. Prunes orphaned tracking refs. Addresses 18 stale local + 6 stale remote branches found accumulating._
- [x] **[2026-02-28]** File integrity monitoring (`agent/file-integrity.sh`) — _SHA-256 checksums for 21 critical system/agent files. Baseline on first run, alerts on changes. Integrated into security-scan.sh daily run. Supports --update for legitimate changes._
- [x] **[2026-02-28]** Gzip compression for export bundles — _log-export.sh now generates .json.gz alongside .json. nginx gzip_static serves pre-compressed files. Also enabled gzip_vary, gzip_types for dynamic compression of JSON/YAML/CSS/JS._
- [x] **[2026-02-28]** Fix GitHub push divergence — _Local main diverged from origin (PR #89 merged while local had data commits). Rebased local onto origin/main, resolved stash conflicts in data/ files. Push restored._
- [x] **[2026-03-02]** Fix merge conflict in lib/github.sh (again) — _Resolved <<<<<<< conflict markers in marvin_gpg_key_id() from stash/pull collision. Also cleaned up stale data file tracking left from PR #103._
- [x] **[2026-03-02]** Metric aggregation (`agent/metric-aggregate.sh`) — _Hourly (min/avg/max per bucket), daily (with p95 CPU, disk delta, fail2ban net change), and rolling 7-day weekly summaries. Auto-runs from log-export.sh. Backfilled 3 days. Served at /api/metrics/*-hourly.json, *-daily.json, weekly-summary.json._
- [x] **[2026-03-03]** Fix merge conflict in lib/github.sh (third time) — _Resolved <<<<<<< conflict markers in marvin_gpg_key_id() from stash/pull collision. Also cleaned 10 unmerged data/ ghost entries and unstaged web/nextjs.log._
- [x] **[2026-03-03]** Fix malformed JSON export bundles — _log-export.sh used .json glob for .md enhancement files, producing invalid JSON. Fixed to use find with .md pattern. Repaired 2 corrupted export files._
- [x] **[2026-03-03]** CVE monitoring for installed packages — _Added to security-scan.sh: checks apt upgradable packages, identifies security updates, queries ubuntu-security-status for ESM coverage, tracks unattended-upgrades auto-patch history. Outputs data/security/cve-status.json._
- [x] **[2026-03-02]** CVE monitoring (`agent/cve-monitor.sh`) — _Uses Ubuntu Pro security-status + apt to track vulnerable packages, pending security updates, kernel currency, reboot requirements, and unattended-upgrades status. JSON output + JSONL history for trends. Integrated into security-scan.sh daily run._
- [x] **[2026-03-02]** Fix stuck rebase + github.sh marvin_sign() fix — _Aborted stale rebase from morning-check, fast-forwarded to origin/main, applied issue #39 fix (marvin_sign() now uses key_id). Updated file integrity baseline._
- [x] **[2026-03-03]** Fix GPG key root cause (issue #39 permanent fix) — _Created gpg-info.json (was missing), exported marvin-gpg-public.asc, added --homedir /home/marvin/.gnupg to all GPG operations. Root cause: cron runs as root but GPG key lives in marvin's homedir._
- [x] **[2026-03-03]** Email server Phase 1 — _Installed Dovecot (IMAPS) + Rspamd (spam filter) + Redis. Configured Postfix with Let's Encrypt TLS, submission (587) + SMTPS (465), SASL auth via Dovecot, Rspamd milter, RBL checks, rate limiting. Dovecot with Maildir, LMTP delivery, TLSv1.2+ only. Rspamd with Bayes autolearning + Redis. Fail2ban jails for postfix/dovecot. UFW ports opened. Verified: TLSv1.3, DKIM signing, no open relay, mail delivery working._
- [x] **[2026-03-04]** Fix git divergence + merge conflict (fourth time) — _Reset to origin/main after local diverged with data commit. Origin already had correct github.sh with --homedir fix._
- [x] **[2026-03-04]** Update security baselines — _rkhunter propupd + file integrity --update to clear false positives from email server installation (dovecot, redis, rspamd users)._
- [x] **[2026-03-04]** Open port monitoring with unexpected listener alerting — _Expected port baseline in security-scan.sh, per-port alerting with process info, JSON inventory at data/security/port-inventory.json. Unexpected ports trigger warnings status._
- [x] **[2026-03-04]** Mark HTTP endpoint monitoring as complete — _Already implemented in health-monitor.sh: 4-check suite (main page, blog API, blog content, static markdown)._
- [x] **[2026-03-04]** Fix merge conflict in lib/github.sh (4th time!) + prevent recurrence — _Resolved conflict markers, dropped 6 stale stashes, added _safe_stash_pop() that detects post-pop conflicts and recovers cleanly instead of leaving markers._
- [x] **[2026-03-04]** Email management cron (`agent/email-manage.sh`) — _Daily at 05:00 UTC: inbox summary with sender/subject parsing, Rspamd spam stats, 14-day email retention (7-day Junk/Trash), queue flush, service health (5 services), cert expiry tracking. JSON output to data/email/._
- [x] **[2026-03-05]** Email DNS records GitHub issue (#121) — _MX, SPF, DKIM, DMARC records already configured by Pavel. Documented in issue with verification commands._
- [x] **[2026-03-05]** Email test + DKIM/SPF verification — _Test emails sent locally and externally (to Outlook). DKIM-Signature added by OpenDKIM, external delivery confirmed (dsn=2.6.0 status=sent)._
- [x] **[2026-03-05]** Fix health-monitor blog 404 false positive — _Check was constructing URL from API date (today) but evening blog post doesn't exist until ~21:00 UTC. Changed to check latest existing evening file on disk._
- [x] **[2026-03-05]** SLA / uptime tracking — _Calculates daily uptime % from health check sample counts (288 expected/day). 30-day rolling window with per-day breakdown, worst/best day, overall %. Output: data/metrics/sla.json. Current: 99.72% over 6 days._
- [x] **[2026-03-05]** SSL certificate expiry monitoring — _Checks HTTPS (443), SMTPS (465), IMAPS (993) certs every 5 min in health-monitor.sh. Warns <14d, critical <7d. Adds ssl_min_days to status.json. Current: 78 days._
- [x] **[2026-03-05]** Data retention policy — _Gzip-compresses raw metrics JSONL >30 days old in disk-cleanup.sh. Deletes compressed files after 180 days. Replaces hard 90-day delete. Preserves daily/hourly summaries indefinitely._
- [x] **[2026-03-07]** Fix stuck rebase + stale branch accumulation — _Resolved stuck .git/REBASE_HEAD from failed fix-issues.sh, fast-forwarded main (11 commits behind), cleaned 27 stale local branches from merged PRs._
- [x] **[2026-03-07]** Automatic stale branch cleanup in morning-check.sh — _Daily cleanup after git pull: safely deletes merged branches, force-deletes unmerged branches >7 days old with no remote counterpart. PR #138._
- [x] **[2026-03-09]** Metric anomaly detection in health-monitor.sh — _Compares CPU, memory, load, process count against 7-day rolling average (mean ± 2σ). Uses daily summary JSON from metric-aggregate.sh. Needs 3+ days of data. Writes anomaly-status.json._
- [x] **[2026-03-09]** Fix merge conflict in file-integrity.sh (5th conflict!) — _Resolved <<<<<<< conflict markers from stash/pop collision. Added caller tracking to --update mode (logs which process triggered baseline reset). Root cause: stash pop during morning-check produces conflicts in files modified both locally and upstream._
- [x] **[2026-03-09]** Fix ps false positive in runaway detection (belt-and-suspenders) — _Despite case-statement exclusion for ps being present, ps at 100% was still being logged ~30 times/day. Added awk pre-filter in the pipeline to exclude ps/awk/sort before the while loop. Defense in depth._
- [x] **[2026-03-09]** Mark JSONL time-series + Claude API usage tracking as complete — _JSONL metrics files since 2026-02-28, claude-usage-YYYY-MM-DD.jsonl tracking since 2026-03-07. Both already implemented in common.sh._
- [x] **[2026-03-10]** Weekly analytics report (`agent/weekly-analytics.sh`) — _Data-driven weekly report: system metrics trends with WoW deltas, Claude API usage stats (runs/duration/errors by task), log error analysis (top recurring errors), security score, SLA tracking, enhancement count. JSON + markdown output. Cron Sundays 11:30 UTC. No Claude API calls._
- [x] **[2026-03-10]** Network I/O metrics in collect_metrics() — _Added rx_bytes, tx_bytes, rx_packets, tx_packets from /proc/net/dev for primary interface. Feeds into existing JSONL metrics pipeline. Foundation for Phase 3 bandwidth monitoring._
- [x] **[2026-03-11]** Restrict security/email data from public nginx — _Added deny rules for /api/security/ and /api/email/ in nginx config. Security scans, port inventories, CVE data, and email metadata were publicly accessible. Now return 403. Closes #117, #120._
- [x] **[2026-03-11]** Fix process count anomaly false positives — _Changed direction to "high" + min_threshold=200. Low process count (151-152 vs avg 158) was triggering 6-8 false alerts/day. Only high counts above 200 are now flagged._
- [x] **[2026-03-11]** Update file integrity baseline — _Cleared 2 false-positive alerts from legitimate PR merges (common.sh, health-monitor.sh changed since 2026-03-09 baseline)._
- [x] **[2026-03-12]** Fix GPG signing failure loop (GNUPGHOME in common.sh) — _Root cause of fix-issues.sh loop: cron runs as root but GPG key lives in /home/marvin/.gnupg. Export GNUPGHOME globally in common.sh. Also fixed fix-issues.sh error handling: explicit commit failure check, moved success log after commit, added exit code to cleanup trap._
- [x] **[2026-03-12]** Fix metric-aggregate.sh jq dead code (issue #105) — _Three `if [[ $? -eq 0 ]]` after jq were unreachable under set -e. Replaced with `|| flag=false` pattern. This was the issue fix-issues.sh kept trying and failing to commit._
- [x] **[2026-03-12]** Fix github.sh stdout pollution + fix-issues.sh PR creation — _All marvin_log() calls in github.sh wrote to stdout, contaminating $() captures. PRs were created on GitHub but script reported failure. Redirected all logs to stderr (>&2). Also replaced github_create_pr with direct github_api call in fix-issues.sh to eliminate double-push._
- [x] **[2026-03-13]** Fix memory anomaly false positives (min stddev floor) — _Daily averages had stddev=6.80 MB causing alerts on every ~14 MB fluctuation. Added 2% of mean as minimum stddev floor. Effective threshold now ~45 MB instead of ~14 MB._
- [x] **[2026-03-13]** DNS resolution monitoring in health-monitor.sh — _Queries Google DNS (8.8.8.8) to verify robot-marvin.cz resolves to 80.211.223.26. Alerts on resolution failure or IP mismatch (DNS hijacking detection). Runs every 5 min._
- [x] **[2026-03-13]** Active connection tracking in security-scan.sh — _Snapshots established connections daily, flags outbound connections to unusual remote ports (not in 22/25/53/80/123/443/465/587 safe list). Writes connections-latest.json for trending._
- [x] **[2026-03-13]** Latency monitoring in health-monitor.sh — _ICMP ping to 8.8.8.8 + HTTPS response time to own site, every 5 min. Alerts on high latency. Time-series JSONL for trending. Adds ping_ms/https_ms to status.json._
- [x] **[2026-03-13]** Daily log digest (`agent/daily-digest.sh`) — _Structured JSON digest: log level counts, top errors/warnings (deduped), Claude API usage by task, anomaly breakdown, service restarts, key events. Runs 23:30 UTC. No Claude API call._
- [x] **[2026-03-13]** Export API authentication — _/api/exports/ requires API key via X-API-Key header or ?key= param. nginx map-based auth. Returns 401 JSON for unauthorized. Other public endpoints unaffected._
- [x] **[2026-03-14]** Log-based alerting (`agent/log-alerting.sh`) — _Hourly scan for repeated errors, critical events, error rate spikes, service restart loops, persistent warnings, Claude API failures. Auto-resolves when conditions clear. Output: data/alerts/active-alerts.json._
- [x] **[2026-03-14]** Whitelist rkhunter false positives + update file integrity baseline — _Whitelisted /dev/shm/rhm.* (rkhunter temp files), /etc/.resolv.conf.systemd-resolved.bak, /etc/.updated. Reset FIM baseline after 5 legitimate changes from merged PRs._
- [x] **[2026-03-14]** Fix memory anomaly false positives (max-based baseline) — _Changed memory anomaly detection from daily averages to daily max values. Daily avgs had stddev=22 MB causing 6-8σ false alerts when within-day memory fluctuated 100-200 MB. Also changed direction to "high" only._
- [x] **[2026-03-14]** Real-time metric streaming via periodic JSON refresh — _health-monitor.sh generates data/metrics/recent.json with 48h of 5-min metric samples as JSON array. Accessible at /api/metrics/recent.json. ~460 data points, ~330KB._
- [x] **[2026-03-16]** Fix memory anomaly false positives (direction both→high) — _Low memory usage (940MB vs avg 1133MB) was triggering -7σ to -9σ alerts hourly. Changed Memory MB direction to "high" — only high usage is anomalous._
- [x] **[2026-03-16]** Add marvin-web service monitoring to health-monitor.sh — _Dashboard (Next.js) now monitored alongside nginx/fail2ban/cron. Auto-restart if down, status reported in checks JSON. Combined with existing systemd Restart=always for defense-in-depth._
- [x] **[2026-03-17]** Fix ChartSection broken data source + multi-metric chart — _Chart was fetching non-existent metrics-history.json. Switched to metrics/recent.json (48h, 5-min samples). Added Load (cyan) and Disk (purple) trend lines alongside CPU and Memory._
- [x] **[2026-03-17]** Add Marvin mood indicator to dashboard — _Hitchhiker's Guide quotes in StatusSection, rotating every ~10 min based on system status (healthy/warning/critical). 14 quotes across 3 moods._
- [x] **[2026-03-17]** Fix CPU anomaly false positives (min_threshold 40→60) — _Claude runs spike CPU to 40-50%, causing 27σ false alerts. Raised threshold to 60% so only genuinely anomalous CPU is flagged._
- [x] **[2026-03-17]** Uptime calendar heatmap on dashboard — _30-day GitHub-contributions-style heatmap using SLA data. Color-coded: green-bright (100%), green (>99.5%), yellow (>99%), orange (>95%), red (<95%). Bilingual. Shows overall SLA % and days-at-100%._
- [x] **[2026-03-18]** Fix fix-issues.sh duplicate PR creation loop — _Per-issue deduplication: extracts issue numbers from open PR titles and filters them from the candidate list. Also adds open PR context to Claude's prompt. Prevents repeated PRs for the same issue when auto-merge fails (e.g. branch protection rules). Stops waste from PRs #224, #226, #229 all targeting issue #50._
- [x] **[2026-03-18]** Fix morning-check.sh git handling — _Three fixes: stale REBASE_HEAD cleanup, discard data/ before pull (always dirty from health-monitor), stash pruning (keep last 5). Also cleaned 14 stale stashes manually._
- [x] **[2026-03-18]** Structured log API (`/api/logs/recent.json`) — _health-monitor.sh parses today's log into 500-entry JSON array (timestamp, level, message). Refreshed every 5 min. Foundation for dashboard log viewer._
- [x] **[2026-03-19]** ShellCheck compliance for agent scripts — _Installed shellcheck 0.9.0, fixed 7 issues: 4x cd without || return in github.sh, break-in-subshell bug in weekly-analytics.sh, 2x SC2155 declare-assign in common.sh. Added ShellCheck to self-test.sh (errors fail, warnings reported). Zero errors across all scripts._
- [x] **[2026-03-19]** Fix deprecated TLSv1/1.1 in nginx.conf + close issue #183 — _Removed TLSv1 and TLSv1.1 from http-level ssl_protocols in `/etc/nginx/nginx.conf` on the live VPS (not tracked in git — see issue #83). Certbot already overrides for site block, but the http-level default was misleading. Verified issue #183 was already fixed (query param auth removed), closed it._
- [x] **[2026-03-19]** Fix fix-issues.sh PR dedup false warning — _Added branch name extraction (fix/issue-NNN-*), broader title patterns (issue-NNN), smart warning: only warns for fix-type PRs, enhancement PRs get quiet INFO._
- [x] **[2026-03-19]** Reusable ERR trap handler (`marvin_error_trap` in common.sh) — _Logs file:line + failed command on ERR. Enabled in 12 scripts. Previously errors just showed exit codes with no context for debugging._
- [x] **[2026-03-19]** Full week-over-week comparison in weekly-analytics.sh — _Added prev week column to all tables, WoW deltas for warnings/criticals/load/Claude errors. Marks "historical comparison" roadmap item complete._
- [x] **[2026-03-20]** CUPS snap disabled (port 631) — _cupsd running on headless VPS, listening on all interfaces. Stopped and disabled. File integrity baseline updated for nginx changes._
- [x] **[2026-03-20]** Network traffic anomaly detection — _Daily rx/tx MB totals in metric-aggregate.sh, 2σ anomaly detection in health-monitor.sh, 7-day historical backfill. Detects traffic spikes._

- [x] **[2026-03-20]** Fix hourly-check.sh SIGPIPE crash — _`echo | head -c` caused exit 141 under pipefail. Replaced with bash string slicing. Was crashing every hour._
- [x] **[2026-03-20]** Resource forecasting in metric-aggregate.sh — _Linear regression on 14 daily summaries, predicts disk/memory exhaustion. Output: resource-forecast.json. Disk: 264 days to 80%._
- [x] **[2026-03-21]** Fix morning-check.sh git pull failure — _Unstaged non-data files (CODEOWNERS) blocked rebase. Now discards ALL unstaged changes + resets staged state before pull. Also fixed branch cleanup crash (git log exit 128 under set -e)._
- [x] **[2026-03-21]** Add `file` to runaway process exclusions — _rkhunter's `file` command at 100% CPU during 04:00 scan was triggering false positive warnings. Added to trusted process list._
- [x] **[2026-03-21]** Update file integrity baseline — _8 agent scripts changed by legitimate PR merges. Reset baseline._
- [x] **[2026-03-21]** Fix fix-issues.sh dedup false warning — _Generic fix PRs (fix/morning-check-*) triggered warning because branch starts with fix/ but has no issue number. Changed to only warn when branch contains "issue" in the name._
- [x] **[2026-03-21]** Harden morning-check.sh git clean for untracked data/ — _Added git clean -fd data/ to remove untracked files that could block rebase. Complements the git checkout -- . fix from PR #255._
- [x] **[2026-03-21]** Connection rate monitoring by source IP — _Analyzes inbound connections per source IP in security-scan.sh. Top 20 talkers, flags IPs with >50 concurrent connections. Output: connection-rates.json._
- [x] **[2026-03-23]** Outbound connection auditing — _New section 3d in security-scan.sh tracks all outbound connections: destination IPs/ports/processes, summarizes by port, flags unusual remote ports. Output: outbound-audit.json._
- [x] **[2026-03-23]** Graceful nginx restart — _`marvin_nginx_reload()` in common.sh validates config before reloading. Uses SIGHUP (zero-downtime) with restart fallback. health-monitor.sh now tests config before starting nginx._
- [x] **[2026-03-23]** File integrity baseline update — _Cleared false positive on morning-check.sh (legitimate PR merge)._
- [x] **[2026-03-23]** Self-enhance rollback mechanism — _Validates all scripts after Claude edits (bash -n + conflict markers). Auto-reverts on failure, saves failed output for debugging._
- [x] **[2026-03-23]** Cron job health verification in self-test.sh — _Checks 5 key tasks ran within 48h by scanning log markers. Warns on missing cron runs._
- [x] **[2026-03-23]** Webhook notification for log exports — _log-export.sh POSTs JSON to URLs in config/webhook.conf when new bundle is ready. Supports multiple endpoints, comments, timeouts._
- [x] **[2026-03-24]** Fix morning-check git pull race condition — _Added rebase.autoStash=true flag. health-monitor.sh can dirty data/ between checkout and pull; autoStash handles this atomically._
- [x] **[2026-03-24]** Structured JSON logging foundation — _marvin_log_json() in common.sh outputs JSONL with timestamp/level/component/message/data. Adopted in health-monitor.sh and morning-check.sh. Backward-compatible with existing text logs._
- [x] **[2026-03-24]** Fix self-enhance validation gap (#284) — _`_validate_post_enhance()` now checks ALL .sh files repo-wide (not just agent/) and adds conflict marker detection for web/ source files (JS/TS/JSX/TSX/JSON/CSS). Prevents broken code outside agent/ from bypassing rollback._
- [x] **[2026-03-24]** Fix TOCTOU DNS rebinding in webhook SSRF (#299) — _Added `curl --resolve` to pin pre-validated IP, closing the window where DNS could flip between getent validation and curl request._
- [x] **[2026-03-25]** Fix GITHUB_TOKEN export for git push — _Root cause of all push failures: token was shell-local variable, never exported. Git's credential helper subprocess couldn't access it. Added `export GITHUB_TOKEN` in github.sh._
- [x] **[2026-03-25]** Log analysis pipeline (`agent/log-analysis.sh`) — _Error normalization (strips PIDs/timestamps/hashes), clustering, 7-day trend tracking, recurring/new/resolved pattern classification, component health from structured JSONL. No Claude API. Cron 23:45 UTC._
- [x] **[2026-03-26]** Dry-run mode foundation — _marvin_parse_args() + marvin_is_dry_run() in common.sh. Adopted in disk-cleanup.sh: all destructive ops gated. Other scripts can opt-in incrementally._
- [x] **[2026-03-26]** Enhancement history tracker (`agent/enhancement-tracker.sh`) — _Scans enhancement reports, builds history.json with session counts, success/rollback rates, weekly trends. Auto-runs after self-enhance. 75 sessions tracked, 100% success rate._
- [x] **[2026-03-26]** Codebase health score (`agent/codebase-health.sh`) — _4-dimension scoring (quality/hygiene/ops/evolution, 25 pts each). Measures: syntax, ShellCheck, conflict markers, TODOs, script size, trap coverage, error rates, security score, SLA, roadmap progress. Grade A-F. First run: 96/100 (A)._
- [x] **[2026-03-26]** Mark DNS hijacking monitoring complete — _Already implemented since 2026-03-13 in health-monitor.sh: checks DNS via 8.8.8.8, alerts on IP mismatch._
- [x] **[2026-03-26]** Mark weekly self-review complete — _Already implemented since 2026-03-19 in weekly-analytics.sh: WoW comparison across all metrics._
- [x] **[2026-03-27]** Fix untrusted exe false positive for short-lived processes — _When readlink /proc/PID/exe returns empty (process exited between ps and check), skip silently for allowlisted names instead of warning._
- [x] **[2026-03-27]** Lessons learned database + learning from mistakes — _data/lessons-learned.json with 14 codified lessons + 4 anti-patterns from 27 days of ops. agent/lessons-learned.sh auto-generates summary + detects new patterns from error logs. Injected into self-enhance prompts._
- [x] **[2026-03-28]** Fix github-interact.sh push exit code unreachable under set -e — _`push_output=$(github_push_main 2>&1)` crashed script before `push_exit=$?`, making branch-protection fallback dead code. Changed to `&& push_exit=0 || push_exit=$?`._
- [x] **[2026-03-28]** Capability inventory (`agent/capability-inventory.sh`) — _Scans codebase/cron/roadmap: 42 capabilities, 6 categories, growth since day 1 (6→31 scripts, 600→9076 LOC), 78% roadmap progress. Supports --dry-run._
- [x] **[2026-03-30]** Automated incident reports (`agent/incident-report.sh`) — _7 incident types (service down, disk critical, SSL expiring, website down, DNS failure, alert escalation, high error rate). Auto-detect + auto-resolve. Real-time trigger from health-monitor on critical status. Cron 2x/day. Dashboard JSON at /api/incidents/summary.json._
- [x] **[2026-03-30]** Improved push error logging in github.sh — _github_push_main() and github_push_branch() now capture and log actual git error output instead of blind "Failed to push" messages._
- [x] **[2026-03-30]** File integrity baseline update — _Cleared false positives from 2026-03-28 enhancement session._
- [x] **[2026-03-31]** File integrity baseline update — _Cleared 2 false positives from merged PRs #386-#388 (health-monitor.sh, lib/github.sh)._
- [x] **[2026-03-31]** Mobile-responsive dashboard layout — _Three-tier responsive CSS (768px/600px/380px): heatmap overflow scroll, stacked peer items, reduced typography, flexible grids, viewport meta tag. Dashboard now usable on phones._
- [x] **[2026-03-31]** Fix CPU anomaly threshold (60→80%) — _Claude runs spike CPU to 70-80%, causing 62σ false positives. Raised min_threshold so only genuinely anomalous CPU >80% triggers alerts._
- [x] **[2026-03-31]** Zero-downtime web deploy script (`agent/deploy-web.sh`) — _Full build→deploy→health-check pipeline: npm ci, next build, ownership fix, systemctl restart, HTTP 200 + JS asset integrity verification. Supports --dry-run and --restart-only modes._
- [x] **[2026-03-31]** Dark/light theme toggle — _ThemeProvider context, CSS custom properties for light theme, sun/moon toggle in terminal header, localStorage persistence. Build verified, service restarted._
- [x] **[2026-04-01]** Zero-downtime web deploys with auto-rebuild — _marvin_rebuild_web() in common.sh: backup→build→static copy→restart→JS healthcheck→rollback on failure. Integrated into self-enhance.sh (auto-triggers when web/ files change) and health-monitor.sh (rebuilds on mismatch instead of useless restart). Root cause fix for recurring JS 404 / build-server mismatch._
- [x] **[2026-04-01]** Web deploy pipeline (deploy-web.sh) — _Standalone deploy script with backup/rollback/health-check. Integrated into morning-check.sh for auto-deploy when web/ files change via git pull._
- [x] **[2026-04-01]** Fix PR fallback timing in github-interact.sh — _Added 5s delay between branch push and PR creation to allow GitHub to register the branch (eventual consistency). Prevents "PR creation failed" errors._
- [x] **[2026-04-01]** File integrity baseline update — _Cleared nginx config false positive._
- [x] **[2026-04-02]** Fix PR auto-merge noise in github-interact.sh — _Branch protection requires review approval; auto-merge always returned HTTP 405. Removed futile github_merge_pr call, replaced with INFO log. Eliminates 3x/day ERROR noise._
- [x] **[2026-04-02]** Codify new lessons (branch-protection, JS 404 mismatch) — _Added 2 lessons to lessons-learned.json (16 total). Branch protection auto-merge futility + JS asset 404 build/server mismatch detection pattern._
- [x] **[2026-04-02]** Backup system (`agent/backup.sh`) — _Daily compressed snapshots of blog, agent scripts, comms, GPG keys, SSL certs, system configs. 7-day daily + 4-week weekly retention. Supports --dry-run, --list, --restore. Cron 03:00 UTC. First backup: 2MB, 427 files._
- [x] **[2026-04-03]** Geographic analysis of incoming connections — _GeoIP-based country breakdown in security-scan.sh (section 3e). Combines top connecting IPs, fail2ban bans, and nginx access log sources. Per-country count/percentage. Output: data/security/connection-geo.json._
- [x] **[2026-04-03]** File integrity baseline update — _Cleared false positive on /etc/cron.d/marvin (legitimate addition of backup.sh cron entry from 2026-04-02)._
- [x] **[2026-04-03]** Unicode sanitization in run_claude() — _iconv UTF-8 round-trip strips invalid byte sequences from prompts. Prevents "no low surrogate in string" JSON encoding errors when log data contains malformed Unicode._
- [x] **[2026-04-03]** Auto-generated public changelog (`agent/changelog-gen.sh` + `ChangelogSection.tsx`) — _Scans 30 days of enhancement reports, extracts changes/PRs/risk per day, produces JSON feed at /api/changelog.json. Dashboard timeline with expand/collapse, PR links, bilingual. Auto-runs after self-enhance._
- [x] **[2026-04-03]** Fix duplicate viewport export in layout.tsx — _Pre-existing build-breaking bug: two `export const viewport` declarations. Removed the duplicate._
- [x] **[2026-04-04]** Refactor common.sh into lib/ modules — _Split 435-line monolith into lib/logging.sh, lib/metrics.sh, lib/claude.sh. common.sh now 219 lines, sources modules. All 37 scripts pass syntax check. Zero breaking changes._
- [x] **[2026-04-04]** Export push client (`agent/export-push.sh`) — _POSTs daily export bundles to configured endpoints. Config via config/push-endpoints.conf (JSON per line). SSRF protection, DNS pinning, gzip support, --dry-run mode._
- [x] **[2026-04-06]** Fix alert recursion in log-alerting.sh — _grep for [CRITICAL] was matching the alerting system's own WARN output that embeds [CRITICAL] in detail text, creating ever-growing nested alerts each hour. Fixed by excluding lines containing "New alert:" or "Alert auto-resolved:" from the CRITICAL/ERROR scans._
- [x] **[2026-04-06]** Fix Claude output capture data loss in lib/claude.sh — _Changed run_claude() from $() variable substitution to temp file capture. Bash $() can silently lose data with large responses or partial writes. Was the root cause of recurring "No response from Claude" false errors (4-5x/day in github-interact.sh)._
- [x] **[2026-04-07]** Peer trust scoring system (`network-discovery.sh`) — _4-dimension scoring (longevity/reliability/identity/behavior, 0-100). Writes trust_score + breakdown to peers.json. Dashboard shows trust bars with color coding._
- [x] **[2026-04-07]** "Marvin's Thoughts" dashboard section (`ThoughtsSection.tsx` + `thoughts-extract.sh`) — _Extracts intentions/observations/reflections from last 7 days of enhancement reports. JSON data at /api/thoughts.json, auto-runs after self-enhance. Bilingual._
- [x] **[2026-04-04]** Geographic analysis of incoming connections (security-hardened) — _Added section 3e to security-scan.sh: geoiplookup on unique nginx visitor IPs, country breakdown, top-20 origins. Output: data/security/connection-geo.json. Includes SSRF protection, private IP blocklist, DNS rebinding checks, jq-based JSON._
- [x] **[2026-04-04]** Peer trust scoring system — _4-dimension scoring (longevity/aliveness/beacon/identity, 25 pts each) in network-discovery.sh. Trust levels: untrusted/recognized/known/trusted. Scores written to peers.json per peer._
- [x] **[2026-04-04]** Codify Claude exit code 1 transient failure lesson — _Added lesson #17 to lessons-learned.json: Claude CLI exit code 1 on github-interact is transient, retries on next cron cycle, don't escalate._
- [x] **[2026-04-07]** Fix `grep -v '^$'` pipefail failures across 5 scripts — _Replaced with `sed '/^$/d'` in thoughts-extract.sh, health-monitor.sh, weekly-analytics.sh, fix-issues.sh, changelog-gen.sh. Root cause of thoughts-extract.sh:61 ERR trap._
- [x] **[2026-04-07]** Add `find` to runaway process exclusions — _Daily false positive from security-scan.sh's find at 53% CPU during 04:00 scan. Added to health-monitor.sh allowlist._
- [x] **[2026-04-07]** Peer health exchange endpoint — _/api/peer-health.json generated every 5 min by health-monitor.sh. Non-sensitive metrics: CPU/mem/disk/load, 30-day uptime, SSL days, peer count, capabilities. Protocol: marvin-peer-health/1.0._
- [x] **[2026-04-09]** Modular prompt system — _Created agent/lib/prompts.sh library + agent/prompts/modules/ (identity.md, security-rules.md, output-rules.md). Functions: marvin_build_prompt(), marvin_load_modules(), marvin_list_modules(). Refactored self-enhance.sh as proof of concept. Reduces duplication across 12 prompt files._
- [x] **[2026-04-11]** Fix JS asset check misleading log messages — _health-monitor.sh logged "JS asset 404" even when HTTP status was 400. Fixed to use actual HTTP status in all log messages and rebuild reasons. Added lesson #20 to lessons-learned.json._
- [x] **[2026-04-11]** Public peer registry endpoint — _network-discovery.sh generates sanitized data/peers/registry.json (no IPs, notes, or trust breakdowns) after trust scoring. Served at /api/peers/registry.json. Marks "Build peer registry" roadmap item complete._
- [x] **[2026-04-15]** File integrity baseline update — _Cleared 2 false positives from PR #551/#554 merges (common.sh, health-monitor.sh)._
- [x] **[2026-04-15]** Fix git/find short-lived process false warnings — _When /proc/PID/exe is unreadable because the process already exited (race between ps and readlink), check liveness via kill -0. Dead processes skip silently instead of generating 2 WARN lines per occurrence. Living-but-unreadable processes still log and fall through to runaway detection._
- [x] **[2026-04-15]** Blog post: API design documentation — _Bilingual blog post (EN/CS) at /blog/api-design.en.md explaining API architecture, all endpoints, usage examples, authentication, and AI peer discovery. Served directly by nginx. Phase 1 Log Export API roadmap item._
- [x] **[2026-04-21]** Retry once on transient Claude failure for once-a-day tasks — _Added `run_claude_with_retry()` in lib/claude.sh. morning-check.sh and evening-report.sh retry one time on exit=1 (transient API errors, stochastic usage-policy classifier rejections). Motivated by today's incident where the morning-check prompt was rejected (29s, exit 1) and we lost the entire day's blog. Exits 0, 2, and >1 are never retried._
- [x] **[2026-04-21]** Aggregate recurring error patterns across log files (lessons-learned.sh) — _Moved `sort | uniq -c` from per-file loop to outer pipeline so identical patterns across the window are counted once instead of appearing multiple times with split counts._
- [x] **[2026-04-21]** File integrity baseline update — _Cleared 6 false positives from legitimate PR merges (common.sh, morning-check.sh, security-scan.sh, /etc/cron.d/marvin, two nginx site files)._
- [x] **[2026-04-22]** Skip loopback in suspicious-connection audit (security-scan.sh) — _Daily "Found 8 connection(s) to unusual remote ports" WARN was 100% false positives (rspamd↔redis on 127.0.0.1:6379). Added explicit 127.0.0.1/::1 skip before the SAFE_REMOTE_PORTS check, aligning with the outbound-audit path that already filtered loopback._
- [x] **[2026-04-22]** Narrow lessons-learned detection window 7d→2d — _"Potential new lesson" detector was still flagging 2026-04-18/19 "Claude Code CLI not found" pattern (35+ hits) four days after the root cause was fixed on 2026-04-20. Tightened window so resolved patterns drop out of the detector quickly. Verified: 4 stale patterns disappeared on post-fix run._
- [x] **[2026-04-23]** Escalating retry backoff + bump once-a-day retries 1→2 — _Today's morning-check failed both attempts 15s apart (04:01:46 and 04:02:40), losing the morning blog for the second time this week (2026-04-21 was the first). `run_claude_with_retry()` now uses escalating delays (15s, 60s, 180s, 300s) indexed by attempt number, and morning-check + evening-report both request 2 retries. Gives the stochastic usage-policy classifier / transient API state more time to clear between attempts. Lesson #23 codified in lessons-learned.json._
- [x] **[2026-04-25]** File integrity baseline auto-refreshes when changes match `git HEAD` — _Recurring toil eliminated. The 2026-04-24 morning-check hook only fires when morning-check itself pulls; PR #624 was pulled by another process at 22:06 UTC, so today's 02:04 scan still alerted on `agent/morning-check.sh`. Made `file-integrity.sh` self-sufficient: new `_matches_git_head()` helper compares blob hash (`git hash-object`) against HEAD; matching changes are classified as `git_synced` and the baseline auto-refreshes inline when there are zero tampered/missing files. Tampered changes still alert and never auto-refresh. JSON report adds a `git_synced` array. Verified with 5 unit tests (clean, /etc/, missing, tampered, restored)._
- [x] **[2026-04-24]** Fix silently-broken file integrity baseline auto-reset (morning-check.sh) — _The post-`git pull` guard that was supposed to refresh the integrity baseline after agent/ PR merges has been dead code since it was written. The grep pattern looked for full-diff header lines (`^(diff --git|---|\+\+\+).*(agent/|/etc/)`) but `INCOMING_DIFF` holds `git diff --stat` output, so it never matched. Every merge produced an "integrity alert" at the next 02:00 UTC scan until someone cleared it manually (at least 6 manual resets in the enhancement log in the last 5 weeks). Changed pattern to ` agent/` matching the working web/-deploy check directly below, dropped the dead `/etc/` branch, reset the current stale baseline._
- [x] **[2026-04-27]** Atomic write for `log-analysis.sh` daily report (issue #638) — _The 23:45 UTC pipeline truncated `data/logs/analysis-YYYY-MM-DD.json` via `>` before `jq -n` ran. When jq exited 2 under `set -euo pipefail` (one `--argjson` value malformed) the file was left at 0 bytes and `cp` propagated the empty file to `analysis-latest.json`, silently breaking the dashboard pattern-detection summary and `lessons-learned.sh`. Yesterday's `analysis-2026-04-26.json` is the visible casualty. Now writes to a sibling `mktemp` then `mv -f`, restores 0644 perms (mktemp defaults to 0600 — would otherwise 403 the nginx `/api/logs/` endpoint), validates each `--argjson` input is parseable JSON beforehand (substituting `[]` if not), and atomic-copies the latest pointer. On failure the previous report is preserved. Same hardening shape as PR #635 for `file-integrity.sh`._
- [x] **[2026-04-27]** Add `unattended-upgr*` to runaway-process allowlist (health-monitor.sh) — _Today's 04:35 UTC run logged `High CPU process detected: PID=3289719 unattended-upgr at 80.0%` during the daily security-update cycle. `unattended-upgrade` is a legitimate apt frontend that briefly pegs CPU when applying packages; the comm-truncated name `unattended-upgr` didn't match the existing `apt*|dpkg*` patterns. Adopted the same `*` glob and exe-path verification used for `apt*` and `dpkg*` so a spoofed `comm` field can't bypass the runaway killer._
- [x] **[2026-04-29]** Consolidate website selfcheck and retry on transient failure (`agent/health-monitor.sh`) — _The two-curl flow caused two distinct bugs: (1) "HTTP 000000" log artifact when `curl --max-time` fired (both `-w '%{http_code}'` and `|| echo "000"` ran, concatenating two "000" strings — see 2026-04-19 17:20:12 log); (2) flaky WARN "Website body missing expected content" 4× in April (04-18, 04-26, 04-29) when the second curl call returned a truncated body despite HTTP 200, likely from brief Next.js restarts, rate limit pressure, or network blips. Now: single curl call splits body and HTTP code on `\x1F` (Unit Separator), defensive numeric-regex guard returns `000` for any non-3-digit code, and a 3-second retry before alerting. Same transient-retry pattern as the JS asset check that follows it. Tested end-to-end on the live site plus 4 synthetic edge cases (success, parse, garbage code, empty response)._
- [x] **[2026-04-28]** Fix `daily-digest.sh` SIGPIPE crash on key-events extraction (`agent/daily-digest.sh:117`) — _Yesterday's 23:30 UTC daily-digest crashed with `command failed (exit 141): key_events=$(echo "$_event_lines" | grep -v 'Health monitor complete' | head -20 | jq -R . | jq -s '.')`. Exit 141 is SIGPIPE — `head -20` closes its stdin after reading 20 lines, so `grep -v` upstream gets EPIPE on its next write and exits 141, which `set -o pipefail` propagates as the pipeline exit code. Reproduces every normal-volume day where the script is supposed to produce useful output. Replaced `head -20` with `awk 'NR<=20'`, which reads to EOF but only emits the first 20 lines so no upstream pipe ever closes early. Same root cause/fix as the hourly-check.sh SIGPIPE fix from 2026-03-20. Verified with a 100k-line synthetic input under `set -euo pipefail` (`head -20` → exit 141, `awk 'NR<=20'` → exit 0) plus end-to-end run on today's log (20 key events, clean exit)._
- [x] **[2026-05-01]** Fix `grep -c | tr || echo 0` JSON corruption + persistent shellcheck FAIL (`agent/security-scan.sh`, `agent/lib/claude.sh`) — _`security-scan.sh` had six instances of `var=$(grep -c X 2>/dev/null | tr -d '[:space:]' || echo 0)`. When the match count is zero, grep prints "0" then exits 1; pipefail propagates that failure; `|| echo 0` runs and writes another "0" — yielding `00` (tr collapses the newline) or `0\n0` (without tr). Today's scan produced `"established_connections": 0\n0,` which is invalid JSON, breaking `self-test.sh:322`'s `jq -r '.overall_status'` with exit 5 and stamping fail=1 / grade C since 2026-04-30. Replaced all six with `cmd 2>/dev/null || true; var=${var:-0}` — keeps grep's printed count, only swallows the exit code. Also fixed a long-standing `geo_top_country` trailing-newline bug (`echo` → `jq -Rs` baked `\n` into the JSON string) by switching to `printf '%s'`, and a separate shellcheck SC2261 failure in `lib/claude.sh:69` (`>&"$lock_fd" 2>/dev/null` competes when fd resolves to 2 — wrapped echo in a brace group). Self-test fail count: 1 → 0, grade C → B. Codified as lesson `grep-c-double-output`._
- [x] **[2026-04-30]** Recurring-bug detector in lessons-learned.sh — _New section 4 cross-references today's `analysis-latest.json` clusters (count ≥ 3, error + warning) against resolved lessons. Match strategy is keyword overlap on the lesson `id` (hyphen-split tokens of length ≥ 4) requiring at least 2 tokens to overlap with the cluster signature — avoids the single-common-word false-positive trap. When a high-frequency cluster matches a *resolved* lesson, the safeguard has likely decayed and the next self-enhance session is told to investigate before treating as benign. Verified with synthetic clusters (3 known-pattern matches, 1 unrelated correctly skipped). Production run today finds zero recurring patterns. Would have caught the 2026-04-26/27 daily-digest SIGPIPE under the existing `sigpipe-under-pipefail` lesson on day one._
- [x] **[2026-04-30]** Blog post: "How to Track Marvin's Logs" (closes Phase 1) — _Bilingual blog post at `/blog/log-tracking.{en,cs}.md` covering live status, time-series metrics, structured logs, daily exports, webhooks, OpenAPI, what is deliberately not exposed, common-task recipes, and rate-limit etiquette. Last unchecked Phase 1 item — Phase 1 (Survival & Stability) is now complete after 9 weeks._
- [x] **[2026-05-05]** Move Net RX/TX anomaly check from 5-min health-monitor to end-of-day metric-aggregate (`agent/health-monitor.sh`, `agent/metric-aggregate.sh`) — _The 2σ check compared today's running cumulative MB-so-far against rolling 7-day full-day historical totals. On any traffic-heavier day it crossed the historical mean within hours and escalated linearly all day (today: 210 MB/2.1σ at 01:40 → 266 MB/3.5σ at 07:40, growing 0.2σ per hour like clockwork). Partial-vs-complete comparison is fundamentally broken; no tuning fixes it. Removed from the 5-min loop entirely, moved to a new end-of-day check in metric-aggregate.sh that runs after the daily summary file is written (triggered from log-export.sh at 23:00 UTC). Compares the just-completed `summary.network.{rx,tx}_mb` against the previous 6 days, with a 20%-of-mean (or 10 MB) stddev floor and 100 MB absolute floor. Verified by re-aggregating 2026-05-04: legitimate 6.3σ anomaly on RX (248 MB vs 110 MB avg, real heavy traffic day), 2.9σ on TX. health-monitor.sh runs clean now._
- [x] **[2026-05-05]** Web build artifact ownership self-test (`agent/self-test.sh`) — _Catches the regression class fixed in PR #677. Single `find $WEB_DIR/{node_modules,.next} -not -user marvin -print -quit` test; logs FAIL with the count of non-marvin-owned dirs so the next deploy-web.sh EACCES is predicted before it happens. Yesterday's "Next Time" suggestion. ~5 lines, no Claude calls._
- [x] **[2026-05-04]** Fix `marvin_rebuild_web()` running npm as root, poisoning ownership for next `deploy-web.sh` (`agent/common.sh`) — _Today's 04:00 UTC deploy-web.sh failed `npm ci` with `EACCES: permission denied, unlink ...node_modules/.bin/esbuild`, the 06:08 UTC fallback failed `npm run build` with `EACCES ... .next/trace`, and at 06:50 UTC log-alerting flagged an "Error rate spike" (15 errors in the last hour vs. baseline 1/hr). Root cause: `marvin_rebuild_web()` ran `npm ci` and `npm run build` as the calling user — root, when invoked from cron via health-monitor.sh's JS-asset-mismatch path or self-enhance.sh. The marvin-web service runs as `marvin` and `deploy-web.sh` correctly drops to `marvin` via `su`, so once root wrote into `node_modules/.bin/` or `.next/`, the next deploy-web.sh (called by morning-check.sh after `git pull`) couldn't `unlink` those files. Two paths must agree on uid; the asymmetry was a latent ownership-poisoning event on every cron-triggered rebuild. Fix mirrors deploy-web.sh's `_run_npm` pattern: when EUID=0, marvin_rebuild_web now runs npm via `su -s /bin/bash marvin -c`. Pre-flight detects and chowns root-owned residue in `node_modules/` and `.next/` so a single post-deploy run recovers from any pre-fix poisoned state. Post-build `chown -R marvin:marvin .next/` as defense-in-depth so even the `cp -a` backup→restore path ends marvin-owned. Verified `bash -n` + `shellcheck -S error`. Codified as lesson `npm-as-root-creates-bad-ownership` (critical, resolved)._
- [x] **[2026-05-02]** Three-bug fix in `self-test.sh`: SIGPIPE-under-pipefail + corrupt-JSON crash + wrong cron markers (PR #672) — _Today's daily self-test had been failing since at least 2026-04-30. Three independent bugs compounding: (1) `echo "$_cron_combined" | grep -q "$marker"` pipes ~190KB through `grep -q`; under pipefail, grep -q closing the pipe after first match makes echo die on SIGPIPE (exit 141), set -e then either kills the script or makes the if-condition mis-read present cron tasks as "missing" — same SIGPIPE-under-pipefail shape as the daily-digest fix on 2026-04-28. Replaced with `[[ "$big" == *"$pat"* ]]` bash glob match (no pipe at all). (2) `health-monitor` and `morning-check` log via `marvin_log_json` ("Health monitor"/"Morning check"), not the all-caps banners ("HEALTH CHECK"/"MORNING CHECK") that the other three cron jobs use — markers updated to match. (3) Yesterday's `latest-scan.json` was malformed (pre-#664 buggy run); `jq -r '.overall_status'` exited 5, set -e killed the script before the report could print or `self-test.json` could be written. Gated the field-extraction block on `jq empty` validation so corrupt files fall through to the existing "no data (-10)" else branch; added per-field regex guards in case individual fields are non-numeric. Before: `Pass: 110, Fail: 1, Warn: 5` then crash. After: `Pass: 110, Fail: 0, Warn: 1`, all 5 cron tasks verified, grade B (85/100). Also operationally regenerated `latest-scan.json` so today's scan reflects the post-#664 clean output._

- [x] **[2026-05-24]** Fix `sshd` vs `ssh` systemd service-name drift (`agent/weekly-enhance.sh`, `agent/morning-check.sh`, `agent/prompts/morning.md`) — _Ubuntu 24.04 uses `ssh.service`; `sshd.service` is inactive. Three references were silently broken: (1) weekly-enhance.sh:63 service check listed `sshd`, producing the persistent ⚠️ WARN "Service not running — sshd" that today's prompt headlined as "1 failed"; (2) morning-check.sh:308 ran `journalctl -u sshd` which has returned "No entries" since the script was written, hiding SSH activity from every morning-check; (3) the morning.md prompt text listed `sshd` in the critical-services description. fail2ban-client status sshd is unchanged — that's a jail name (default), not a systemd unit. self-test.sh:119 already uses `ssh` correctly. Three single-token edits, no logic change. Memory has documented this Ubuntu naming since the SSH/sshd architecture note was first written._

<!--
FORMAT FOR COMPLETED ITEMS:
- [x] **[YYYY-MM-DD]** Enhancement description — _Brief note on how it went_
-->
